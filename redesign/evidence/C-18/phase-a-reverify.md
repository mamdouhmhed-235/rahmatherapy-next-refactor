# C-18 Phase A — RE-VERIFICATION (post three fix rounds)

VERDICT: FAIL

**Repo `master` @ `7d83d38`. Commits re-verified: `6ef68a7` (original), `60114d3`, `a5b5d9c`, `7d83d38` (three fix rounds). Verifier role: read-only. No writes made anywhere except this file. Git limited to `log`/`diff`/`show`/`status`. No server started/stopped; curled the Owner's warm dev server at `localhost:3000` only. `src/lib/maintenance.ts` was read-only checked (`MAINTENANCE_MODE = false`, unmodified), never touched.**

All nine previously-identified false claims are now confirmed corrected and accurate against source and both evidence files (detail below). But a **tenth defect survives**: one card's heading was never updated when its own body copy and button were corrected in the same commit, leaving a present-tense, capability-implying heading sitting directly above text that says the opposite. This is exactly the class of error the dispatch asked me to hunt for (headings/button labels as a proven blind spot) and it is real, narrow, and precisely locatable. That is enough to hold the freeze on its own stated purpose, independent of the otherwise-clean gates below.

---

## CHECK 1 — is the rendered page TRUE? (hostile-reader, claim-by-claim)

Read via `curl -s -L http://localhost:3000/cookies`, full rendered HTML, not source.

| # | Statement (rendered) | Code reality (`file:line`) | Verdict |
|---|---|---|---|
| 1 | `<title>Cookies & Site Storage \| Rahma Therapy</title>` | Purely descriptive, no claim of fact | TRUE |
| 2 | `<meta name="description">` — "How Rahma Therapy uses cookies and browser storage on rahmatherapy.uk — what each one is for and how long it lasts." | `page.tsx:7-10`. Matches page content (purpose + duration given for all 5 entries); the earlier "...and how to change your choices" clause was removed in `7d83d38` since no such control exists | TRUE |
| 3 | H1 "What we store on your device, and why" | Descriptive heading, matches section content | TRUE |
| 4 | Intro: "This page lists every cookie and browser-storage item rahmatherapy.uk uses when you visit as a member of the public or use our booking system... We keep this list in one place so it always matches what your browser actually receives." | `page.tsx:33`, unchanged since `6ef68a7`. "One list, no drift" is TRUE (Check 4). "Lists every... item" is the registry's 5-of-12-found scope; both evidence files (`cookie-inventory-source.md` §6.5/§6.6, `cookie-inventory-browser.md` §4) explicitly leave Cloudflare platform cookies and OpenNext-Cloudflare build-pipeline storage as **unresolved, not confirmed absent** | **UNSUPPORTED, not fail-causing** — see note below |
| 5 | "How we'll record your consent" (H2) + body, all future-tense ("Once one ships... will create... We'll keep... It will never include") | `page.tsx:41,44-51`. No banner exists (`grep -r banner src/` finds none); matches brief §4 design (`consent_events`: no IP, no PII, pseudonymous id) | TRUE — correctly hedged as future, not present, fact |
| 6 | **"Change your choices" (H2, `page.tsx:54-56`)** | Body directly below (`:57-64`) says "There's no live control for this yet"; button directly below that (`:65-71`) was corrected in `7d83d38` from "Change your choices" to "Not available yet" **for exactly this reason**. The H2 is the one element in this card the fix round did not touch — it still reads as a present-tense, imperative capability label ("[you can] change your choices") sitting directly above text and a button both saying the opposite | **FALSE / misleading** — see "Finding" below |
| 7 | Essential group: "Always on — can't be switched off here" + "Needed for a function you specifically asked for — the site does not work as requested without these. You can't opt out of these here." | `CookieRegistryGroups.tsx:80-82`; `cookie-registry.ts:220-221`. No opt-out control exists anywhere on the site, so trivially and literally true; "cookie banner" reference (previously present, implying a banner exists) was removed in `7d83d38` | TRUE |
| 8 | Functional/Analytics group badge: "Currently on — no cookie choice yet" | `CookieRegistryGroups.tsx:84-86`, changed from "Off unless you say yes" in `7d83d38`. `rahma-booking-contact-v1` writes unconditionally on submit (`returning-customer.ts:39`), `_ga` loads on env+production gate alone (`GoogleAnalytics.tsx:6`), `sentryReplaySession` starts unconditionally (`sentry.client.config.ts:38-47,81-106`) — all three genuinely "on" with no gate | TRUE |
| 9 | Functional/Analytics group descriptions: "...stored automatically today... don't yet wait for you to say yes" / "...load or run automatically today... don't yet wait for you to say yes" | `cookie-registry.ts:222-225` | TRUE, matches #8's evidence |
| 10 | `zam-therapy-booking-draft-v3` — description, "Set by: Rahma Therapy", duration "No fixed expiry — stays on this device even after you submit a booking. It's only cleared when you click \"Start a new request\"... or when you clear your browser's site data yourself" | `booking-store.ts:72-76` (`resetDraft`); only caller is `startOver` (`BookingExperience.tsx:522-523`), wired to `onStartOver` on `SuccessScreen` (`:689`), which fires only from the "Start a new request" button (`SuccessScreen.tsx:105-110`, exact label match). Submit-success path (`BookingExperience.tsx` around the `submitBookingRequest` call) never calls `resetDraft`/`clearPackages` | TRUE — verified independently, not just against evidence |
| 11 | `maintenance-modal-seen` — "Not currently active" badge + "This item is part of a feature that is switched off today..." | `src/lib/maintenance.ts:2` → `MAINTENANCE_MODE = false` (read-only checked, current) | TRUE |
| 12 | `rahma-booking-contact-v1` — description lists "your name, phone number, email address, gender, home address (house/street, town, area and postcode), and any access or parking notes" | `returning-customer.ts:10-21` (`storedContactSchema`): `fullName, phone, email, clientGender, city, area, postcode, address, accessNotes, parkingNotes` = 10 fields. Counted 1:1 against the description: name→fullName, phone→phone, email→email, gender→clientGender, house/street→address ("Home visit address", placeholder "House number and street", `AboutYouStep.tsx:538-549`), town→city ("City / Town" label, `:499`), area→area ("Area / County" label, `:512`), postcode→postcode, access notes→accessNotes, parking notes→parkingNotes | TRUE — all 10 fields disclosed, none omitted |
| 13 | `rahma-booking-contact-v1` — "Under review" note + `functional` purpose | `cookie-registry.ts:109,113-114` | TRUE, matches `provisionalNote` |
| 14 | `_ga / _ga_*` — description: "It currently loads automatically in production whenever analytics is configured for this site — it does not yet wait for a cookie choice." | `GoogleAnalytics.tsx:6` — gated only on `GA_ID` + `NODE_ENV === "production"`, comment marks the future consent insertion point, no consent check exists | TRUE |
| 15 | `_ga / _ga_*` — duration: "Up to 13 months (Google's documented default...; this site does not set a custom expiry, and this figure has not been independently verified in production)" | Matches `cookie-inventory-source.md` §1/§4 item 4 exactly ("commonly documented as ~13 months... not verified here") | TRUE, correctly hedged (this was a soft finding in the prior FULL verify; now fixed) |
| 16 | `sentryReplaySession` — "Every visit is recorded; what varies is whether that recording is sent to us: about 10% of visits are sent automatically, and any visit where an error occurs is sent too... It starts automatically for every visitor today — it does not yet wait for a cookie choice." | `sentry.client.config.ts:42-43` (`replaysSessionSampleRate: 0.1`, `replaysOnErrorSampleRate: 1.0`); `cookie-inventory-browser.md` §2 confirmed by live observation: the sessionStorage write is unconditional on every page load, before any sample outcome is known | TRUE, matches numbers exactly |
| 17 | "Last updated: 16 July 2026 (policy version 2026-07-16.1)." | `CONSENT_BANNER_VERSION = "2026-07-16.1"` is the **brief-locked** value (`redesign/briefs/C-18-cookie-consent-brief.md:50,178`: "Q8.2 — Banner version string format... `2026-07-16.1` locked"), pinned by test (`registry-completeness.test.ts:109-111`). Not a per-commit timestamp — it's version 1 of the disclosure, fixed by spec before this Phase A implementation round began | TRUE (considered and ruled out as a false claim — see reasoning below) |

**Note on #4 (completeness claim):** I considered failing the page on "lists every cookie and browser-storage item" given both evidence files leave Cloudflare-platform and build-pipeline storage explicitly unresolved. I did not make this fail-causing because: (a) it is a pre-existing characteristic of the whole C-18 registry's scope (5-of-12-found, established before Phase A started, not something introduced or touched by any of the three fix commits), (b) both evidence files are transparent about the gap rather than concealing it, and (c) this matches ordinary industry practice for cookie-disclosure pages built from a code audit. I'm flagging it as a residual risk the orchestrator should know about, not as a blocking defect.

**Note on #17 (last-updated date):** I initially flagged this as impossible — the page's very first commit is dated after the "16 July 2026" it claims as its last-update date. Checked the brief and found `2026-07-16.1` explicitly specified as the **locked starting version string** for the whole consent system (not a commit-date field), so this is intentional and matches spec. Ruled out.

### Finding — the tenth defect

**`src/app/(public)/cookies/page.tsx:54-56`** — the "Change your choices" H2 heading was not updated when `7d83d38` corrected the button beneath it (same card) from "Change your choices" to "Not available yet" and added "There's no live control for this yet" to the body copy. The sibling card's heading ("How we record your consent" → "How we'll record your consent") *was* correctly re-tensed in the same commit — this card's heading was missed. Read on its own, "Change your choices" is an imperative, present-tense capability label — precisely the "implying a control exists" pattern the dispatch named as a checklist item, and precisely the kind of heading-level miss the dispatch flagged as having already caused two of the nine prior defects. A skimming visitor reading only headings would come away believing a working control is here.

---

## CHECK 2 — registry vs evidence, entry by entry

- **`zam-therapy-booking-draft-v3`** — confirmed independently (not just against evidence): `resetDraft`'s only caller is `startOver`, wired only to `SuccessScreen`'s "Start a new request" button (`BookingExperience.tsx:522-523,689`; `SuccessScreen.tsx:105-110`). The corrected claim ("persists after submit, cleared only via that button or clearing site data") is accurate. `clearPrefill` (a *different*, unrelated function for `rahma-booking-contact-v1`) is wired to a real button in `AboutYouStep.tsx:219` — so the sibling entry's "until you clear it" is also a real, working mechanism, not just "clear all site data."
- **`rahma-booking-contact-v1`** — all ten fields counted and matched 1:1 against `returning-customer.ts:10-21` (detail in Check 1 row 12). `functional` classification and `provisionalNote` intact (`cookie-registry.ts:109,113-114`), pinned by test.
- **`_ga`/`_ga_*`** — duration hedged as Google's documented default with an explicit "not independently verified" caveat, matching the source evidence's own language exactly rather than overclaiming certainty.
- **`sentryReplaySession`** — "every visit is recorded; what varies is whether it's sent" is accurate for `replaysSessionSampleRate: 0.1` + `replaysOnErrorSampleRate: 1.0`, confirmed against live browser evidence (unconditional write on every page load, sample outcome decided after).
- **`maintenance-modal-seen`** — dormant, accurately described, `MAINTENANCE_MODE` confirmed still `false`.

---

## CHECK 3 — the Phase D obligation

Present at `cookie-registry.ts:195-218`, directly above `PURPOSE_DESCRIPTIONS` (where a Phase D implementer editing that exact object would see it first). Names all five: `PURPOSE_DESCRIPTIONS.functional`, `PURPOSE_DESCRIPTIONS.analytics`, the non-essential group badge in `CookieRegistryGroups.tsx`, the `_ga` entry description, the `sentryReplaySession` entry description. Explicitly forbids flipping any of the five back to present-tense "gated" language without also shipping the real gate AND a test that reads the actual gating code (not a string-coexistence pin) — naming the earlier rejected pin (`a5b5d9c`) as the anti-pattern not to repeat.

**My own judgement:** a code comment is the best available option today — there is genuinely nothing to test yet, since the gate doesn't exist (a behavioral test can't assert the absence of a future feature). But a comment is not an enforcement mechanism: nothing fails CI if Phase D ships the gate and forgets the copy, or if Phase D is silently descoped and the comment is simply never revisited. This is the same category of gap the original FULL verify identified in the now-removed string-pin (Check 3, that report) — the removal was correct (a pin that only checks two strings coexist proves nothing about the world), but no replacement mechanism exists, by design, because none can yet. I'd recommend the orchestrator carry this forward as an explicit Phase D dispatch checklist item ("read this comment block before touching consent gating") rather than relying on the comment alone being rediscovered organically.

---

## CHECK 4 — single source of truth, and the seam

`groupRegistryByPurpose()` (`cookie-registry.ts:245-254`) is the only grouping function; its only non-test caller is `CookieRegistryGroups.tsx:69` (grepped `groupRegistryByPurpose` — two hits: definition + this call, outside the test file). `page.tsx` holds zero literal cookie names/durations/descriptions — it imports only `CONSENT_BANNER_VERSION`/`formatBannerVersionDate` for the last-updated line and delegates all entry rendering to `<CookieRegistryGroups />`.

`?cookie-settings=1` seam: `href={COOKIE_SETTINGS_HREF}` and `data-cookie-settings-trigger="true"` both survive on the link (`page.tsx:65-67`), documented in the code comment above (`:16-24`) for the future Phase C panel to hook into via the same pattern `BookingExperienceLoader` already uses. Grepped `cookie-settings` repo-wide: only this file references it — nothing currently reads the query param or the data attribute, consistent with an honest no-op. The control cannot be mistaken for working: it's a plain relative-href anchor labelled "Not available yet," not styled or worded as an active CTA. (The heading directly above it is a separate matter — see the Check 1 finding.)

---

## CHECK 5 — tests and gates

- **`registry-completeness.test.ts`**: 18 tests (was 20; two pins removed in `a5b5d9c`), ran in isolation — `npx vitest run src/lib/consent/__tests__/registry-completeness.test.ts` → **1 file, 18/18 passed**. Bidirectional parity intact: "every inventoried item has a registry entry" + "has no registry entry beyond the inventoried set (symmetric check)" + "has exactly the 5 visitor-facing entries" are three independent cross-checks (over-registration, under-registration, count), none removed. `INVENTORY_NAMES` remains a literal array transcribed from the source inventory, not derived from the registry — non-circular. The two removed tests were the rejected `PHASE_D_CLAIM`/`PHASE_D_MARKER` string-coexistence pins (confirmed via `git show a5b5d9c` diff, 35 lines removed from this file, matching the commit message) — removing them did not weaken parity checking, since they never tested parity, only string coexistence in one file.
- **`git show --stat` on all four commits**: confirmed touched files across the full set are exactly `src/app/(public)/cookies/CookieRegistryGroups.tsx`, `src/app/(public)/cookies/page.tsx`, `src/lib/consent/__tests__/registry-completeness.test.ts`, `src/lib/consent/cookie-registry.ts` — nothing else, in any of the four commits (`git diff --name-only 2cb2949 7d83d38` confirms the same four-file set). `GoogleAnalytics.tsx`, `SentryProvider.tsx`, `sentry.client.config.ts`, `BookingExperience.tsx`, `returning-customer.ts`, `(public)/layout.tsx`, `SiteFooter.tsx`, root layout, `src/app/admin/**`, middleware, build configs, `wrangler.jsonc` — untouched (confirmed absent from all four diffs).
- **`npx tsc --noEmit`** → 0 errors (no output).
- **`npx vitest run`** (full suite) → tail:
  ```
  Test Files  2 failed | 191 passed (193)
       Tests  5 failed | 1845 passed (1850)
  ```
  Failures by identity: `src/lib/auth/admin-access.test.ts` ×2 ("gives Owner broad access...", "gives Admin broad operational access...") + `src/app/admin/bookings/new/ManualBookingForm.test.tsx` ×3 ("renders step 1 on first load", "moves focus to the first invalid field...", "shows the consent error..."). Matches inherited baseline identity exactly — no new failures, no swapped failures.
- **`npx eslint .`** → `66 problems (59 errors, 7 warnings)`, structured JSON output parsed per-file: `design_handoff_area_pages/prototype/area-page.jsx` (48 err/1 warn), `.../shared.jsx` (2 err/5 warn), `.../site-chrome.jsx` (5 err/0 warn), `src/features/booking/BookingExperience.tsx` (3 err/0 warn), `.../BookingExperienceLoader.tsx` (1 err/0 warn), `.../utils/returning-customer.ts` (0 err/1 warn) — exactly the six baseline files, none of the four C-18 Phase A files appear.
- **Style**: `grep -rn "border-l-4\|oklch(" "src/app/(public)/cookies" "src/lib/consent"` → zero matches. Responsive classes throughout; `SectionContainer`/`SectionHeading` are established shared components (used across `about`, `area-pages`, `faqs-aftercare`), so the page matches the existing `(public)` pattern.

---

## Summary

**Fail-causing:** the "Change your choices" H2 heading (`page.tsx:54-56`) is a stale, present-tense, capability-implying label left behind when its own card's body copy and button were correctly fixed in `7d83d38`. One-line fix: retitle the heading to something that doesn't read as an active CTA (e.g. a neutral topic label), consistent with how the sibling card's heading was already handled in the same commit.

**Confirmed accurate, all nine prior defects:** the booking-draft persistence-after-submit claim, the full ten-field disclosure for `rahma-booking-contact-v1`, the "every visit is recorded" Replay rewrite, the `_ga`/Replay "does not yet wait for a cookie choice" corrections, the de-hedged `_ga` duration, the purpose-group "currently on" badge and descriptions, the essential description's removed cookie-banner reference, and the "How we'll record your consent" future-tensing — all verified independently against source, not just re-read against the evidence files.

**Everything else clean:** registry-vs-evidence field counts, `functional`/provisional classification, single-source-of-truth architecture, the `?cookie-settings=1` seam, the Phase D obligation comment (present, complete, correctly located — though a comment-only enforcement mechanism is a residual risk worth carrying forward explicitly, per Check 3), all four commits' file scope, `tsc`/`vitest`/`eslint` baseline identity, and style rules.

**Residual risk flagged, not fail-causing:** the intro's unqualified "lists every cookie and browser-storage item" claim, given both evidence files' own acknowledged gaps around Cloudflare-platform and build-pipeline storage (Check 1, row 4).

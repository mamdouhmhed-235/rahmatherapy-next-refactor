# Deferrals — reports

Generated: 2026-05-16 (Step 13 of /redesign/per-page-recipes/reports-recipe.md)

(no deferrals — Phase 6 closed cleanly for reports)

Brief Phase 7 / Phase 8 follow-ups captured separately:

- **HARDEN-RECS-reports.md "Open items"** — loading state via Suspense (`AdminSkeleton`), CSV download failure toast, chart-render-failure inline fallback. All three either require restructuring server components or modifying RECON §5 untouchable code; deferred to Phase 7 or Phase 8.
- **PER-PAGE-SCORES.md "reports — critique" carry-forwards** — five polish residue items (AdminStat resting shadow vs Tonal Lift Rule; section H2 type step at 1.5rem rather than 1.778rem; Section C "Outstanding vs collected" duplicates headline numbers; metric-definitions 4×2 chip grid edges toward identical-grid anti-reference; per-chart `<ChartErrorBoundary>` not wired). All are Phase 7 `audit` re-scan territory or `polish` follow-ups.
- **PER-PAGE-SCORES.md "reports — audit" P2/P3** — raw OKLCH literals for Restricted/Hover-Moss/Cancelled families (`page.tsx:389`, `:629`, `:839`, `:840`, `:939`) follow the established `admin-ui.tsx` convention but would benefit from token consolidation in Phase 8 `extract`. Section H2 + AdminPanel H2 stacking is a `00-shared-components` semantic-nesting fix that affects more pages than just reports.

None of the items above block reports' Phase 6 sign-off; the Phase 7 gauntlet will re-scan and rank them globally.

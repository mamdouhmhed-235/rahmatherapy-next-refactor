# Band C — Owner Action Backlog

Consolidated tracking of every item across the programme that only the Owner can perform — because it needs admin sign-in (no agent may authenticate; password entry is prohibited) or is a Zone-2 action awaiting explicit chat approval. Introduced at drift checkpoint #1 (2026-07-29, `redesign/plans/C-phase/DRIFT-CHECKPOINTS.md`) to replace five separate progress-file appendices with one running list. Each entry links back to its plan's progress file for full detail — this file is the index, not the source of truth.

**How to use this:** items don't block a plan's ✅ shipped status (established precedent: C-06, C-22, C-04a, C-05 all shipped with items here still open) — they're independently-tracked follow-up work. Check items off in place (change ⬜ to ✅) as the Owner completes them; leave the row for audit trail rather than deleting it.

---

> **2026-08-09 — ALL SIX DECISIONS ANSWERED BY THE OWNER IN CHAT, AND ALL SIX ARE NOW DISCHARGED.** The options as offered are preserved at the top of §2 of **`redesign/HANDOFF-2026-08-09-ORCHESTRATOR.md`**. Answers and outcomes:
> **(1)** Maps consent classification → **A, functional-on-interaction.** Implemented `ded190b`; filed `purpose:"essential"` for the reason recorded in `C-20-…-progress.md` §2. **C-20 → ✅.**
> **(2)** C-14 Phase A live round-trip → **B, the FULL test with a real break** (the Owner overrode the safe-variant recommendation). Performed through the admin UI on the Owner's own session, verified customer-side, and reverted; 0 emails sent. Detail in `C-14-…-progress.md`.
> **(3)** C-14 Phase C migration → **approved, then re-approved as corrected SQL.** The originally-approved `DROP INDEX` **cannot execute** — the object is a UNIQUE *constraint* owning its index (`contype='u'`, `pg_depend.deptype='i'`), so Postgres refuses. Re-presented as `ALTER TABLE … DROP CONSTRAINT IF EXISTS` and applied as `c14_override_breaks`. Both uniques and their indexes gone; PK, time CHECK and staff FK intact; **zero data change** (both override tables were empty).
> **(4)** C-23's two unrunnable gates → **A, accept the code-level proof.** C-23's gate tally is now 10 of 10; see `C-23-…-progress.md` §5.1.
> **(5)** Delete the three C-23 test bookings → **delete.** Done and verified; Badar's `9d55ce2a` intact. See `C-23-…-progress.md` §5.2.
> **(6)** Recurring-series email defect → **fix.** Landed `72670e1`; the recurring path now honours the "Send confirmation email to client" checkbox, mirroring the single-booking path.
>
> **Three further decisions were raised and answered the same day, all outside any plan's original scope:** adding `src/app/admin/availability/page.tsx` to C-14's files-touched list (rule 6(b)) to fix three multi-row consumers; raising the shared admin mobile bottom padding for C-10; and fixing `/admin/me`, which the C-10 sweep found **completely broken** in production code (see the new row below).

## ⛔ Zone-2 — awaiting explicit chat approval (not just sign-in)

| Item | From | Status | Detail |
|---|---|---|---|
| ⬜ Cloudflare Workers deploy | C-22, C-04a, C-01, **C-02** | **ANSWERED 2026-07-30 — deferred to END OF PROGRAMME by Owner decision. Not a pending question; do not re-raise it per-plan.** | **FOUR-in-one as of 2026-08-02** (was three; C-02 Phase G added the fourth): applies C-22's `RateLimiter` Durable Object migration, activates C-04a's `* * * * *` scheduled-emails cron, activates C-01's `*/15 * * * *` review-email cron, **and activates C-02's `0 3 * * *` horizon-extension cron — without which a recurring series materialises its first batch (12 weekly / 6 fortnightly / 3 monthly) and then silently stops extending forever.** All four triggers are already committed in `wrangler.jsonc` (`["0 8 * * *", "* * * * *", "*/15 * * * *", "0 3 * * *"]`), so code and triggers ship together — there is no window where code deploys without its cron. **Owner's route: everything is local today; the site goes to GitHub and deploys once all 22 plans are implemented.** ⚠️ **Correction to this row's earlier framing:** it previously claimed queued cancellation emails "do NOT reach customers" until this deploy. That is **wrong while the live Cloudflare site runs the older, pre-C-04a code** — the queue-and-drain behaviour exists only in this undeployed local tree, so live cancellations still send on the old immediate path and no customer is silently missing email today. The claim only becomes true if the new code were ever deployed *without* its cron trigger, which `wrangler.jsonc` makes impossible. **No agent attempts this deploy at any point in the programme.** |
| ⬜ `src/lib/maintenance.ts` restore to `MAINTENANCE_MODE = true` | Standing (protocol §3b) | Not yet — deliberately left `false` all programme, Owner-authorized. | **Must happen before any deploy**, including the end-of-programme deploy above — i.e. before the GitHub push that ships all 22 plans. Never staged/committed mid-programme by design. The final programme report must state this file's state. **2026-08-04:** this working-copy/HEAD split has now caused a real defect, not just a staging hazard — every C-18 Phase A pass read the working copy (`false`) rather than HEAD (`true`) and so shipped a `dormant` flag on `/cookies` telling visitors a storage item was switched off when any deploy would write it. Caught at closeout. **For any claim about deployed behaviour, read `git show HEAD:<path>`, never the working tree.** |
| ⬜ **Sentry console — ingest-side Advanced Data Scrubbing rule** | **C-18 Phase 0** | **OPEN — the only thing that stops NEW captures before the deploy.** | C-18 found and fixed an **active production credential leak**: `/booking/manage?token=<bearer>` carries a customer's booking-management credential in the query string, and Session Replay was transmitting it in four channels. The code fix (`09b2e26`) only takes effect **when deployed**, and the deploy is deferred to end-of-programme — so captures continue until then. An **ingest-side** scrubbing rule, unlike `beforeSend`, *does* cover replay payloads. Also for the Owner's decision: already-stored replays containing live tokens, replay retention, and whether to disable Replay ingestion at project level. **External console — Owner-only; no agent acts on Sentry's console at any point.** Detail: `C-18-cookie-consent-progress.md` §2. |
| ⬜ Prune `consent_events` test rows — **now 7 rows / 5 consent ids** | C-18 §5.2 | Open — real rows in production. **Count corrected 2026-08-09 (drift checkpoint #4):** the figure below was stale. | Created deliberately through the app's own route to prove the consent-proof log works end to end (the route always returns 204, so a failing insert would have been indistinguishable from a working one). `consent_id`s, **re-counted live 2026-08-09**: `760bef18-8d64-4694-a2c9-5ce2cf931b9e` (1 row), `3ae8e616-cd37-4c6b-a4d6-a5d75ccf3bfc` (2), `f846ffc4-9eee-46bf-8eb9-a558088b2ed4` (2), `da8a97c4-fe70-4b9f-b507-e4cbe3129d0c` (1) — all 2026-08-04 — plus **`30c34cae-edb9-4d48-8b4d-ee394d595aad` (1 row, 2026-08-09 09:05)**, created by a public page load during the C-19/C-20 verification work in this session. **Total 7 rows across 5 consent ids**, all `banner_version = 2026-07-16.1`. No PII by design. The earlier "6 rows / 4 ids" figure was already stale when written and drifted further; re-count immediately before pruning rather than trusting this list, since any public page load can add one. |
| ⬜ `NEXT_PUBLIC_GA_MEASUREMENT_ID` in the Cloudflare **BUILD** environment | C-17 | Open, rides the deploy. | `NEXT_PUBLIC_*` is **inlined at build time**, so a runtime-only dashboard setting is insufficient — it must be set in the build environment or GA never loads regardless of consent. |
| ⬜ **C-23 pre-flight #4 — three real admin bookings** | **C-23** | **BLOCKING C-23. Raised in chat 2026-08-04, awaiting decision.** | Captures the behavioural baseline that C-23's "no discrepancies" guarantee (blocking gate §3.3) is measured against: one booking with availability, one with override on, one mixed-gender, plus step-3 screenshots in all three branch states. **Creates three real booking rows in production** via the live admin form — no SQL. Guard: `*.example.test` clients + standard test staff only; row ids recorded for cleanup; Badar's `9d55ce2a` untouched. **Needs an authenticated session — no agent may sign in.** Open question raised with it: does completing an admin booking send a real email? If so that is a second Zone-2 trigger. |
| ✅ **C-20 — Google Maps API key SUPPLIED** · ✅ **rotation decision ANSWERED — DO NOT ROTATE** | **C-20** | **CLOSED 2026-08-09.** Key supplied 2026-08-04; **Owner decided in chat 2026-08-09 not to rotate.** Gate §3.5 is now **fully satisfied** (its referrer/API half already stood DONE from 2026-07-16). Decision is defensible on the facts: a `NEXT_PUBLIC_*` Maps key is inlined into client bundles and public by nature, so the load-bearing control is the **referrer restriction** — already correct — not secrecy. Residual risk recorded once, not re-argued: referrer headers are spoofable outside a browser, so §3.6's post-deploy Metrics check (usage inside the free allowance, **not** merely a £0 invoice — the 90-day trial credit can mask overage) remains on this list. | The Owner added `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` to `.env` on 2026-08-04 — verified present, 39 chars, `AIza` prefix, not a placeholder; `.env` confirmed gitignored (`.gitignore:44`) and untracked. C-20's pre-flight #3 will now pass. **🔴 ROTATE BEFORE DEPLOY:** during this session the Owner pasted Google's reference snippet into chat **with a live key embedded twice** (`AIzaSyBZ…`), so that key has now been exposed in at least two contexts beyond the original plaintext sharing the plan already flagged. Whether the key currently in `.env` is that same one is **unconfirmed** — the orchestrator never read the value. **Recommended: create a fresh key, apply the same restrictions (referrers `https://rahmatherapy.uk/*`, `https://*.rahmatherapy.uk/*`, `http://localhost:3000/*`; APIs Maps JavaScript + Places + Places New), delete the old one, put the new value in `.env`.** Still outstanding regardless: the Cloudflare **BUILD** env var (`NEXT_PUBLIC_*` is inlined at build time) and two post-deploy cost-posture checks on the Maps Platform Metrics/SKU page. External console — Owner-only. |
| ✅ **C-19 — controller identity ANSWERED + VERIFIED** | **C-19** | **RESOLVED 2026-08-04. C-19 is fully unblocked and needs no further Owner input.** | Controller is **`RAHMATHERAPY LIMITED`, company number `16769945`**. **Independently verified against Companies House** rather than taken on trust, because it becomes a public legal statement of who controls visitors' data: register returns an exact match — status **Active**, **Private limited company**, incorporated **7 October 2025**. All three ⏸ inputs are now closed: **(a)** publish email `rahmatherapy@outlook.com` + phone `07798 897222`, exactly as `src/content/site/contact.ts` already publishes them, **no postal address**; **(b)** retention 7 years for booking/treatment records, ~12 months for non-converting enquiries; **(c)** the above. Full detail incl. two recorded caveats — the "insurance-standard" basis was NOT independently verified, and **no code enforces the retention period** (a `src/` sweep found no pruning process for bookings, clients or enquiries; the only booking delete is a rollback path in the recurring-horizons cron) — in `C-19-privacy-policy-page-progress.md` §1. Still flagged: the brief's **ICO Tier 1 £52** figure assumed the smallest tier — check the band against the company now that it is known to be incorporated. |

## Playwright role sweeps + screenshots (agent cannot authenticate)

| Plan | Item | Fixture(s) | Progress file |
|---|---|---|---|
| ⬜ C-06 | §3.2e/f/g — irreversible sensitive-note deletion, bulk delete, privacy `deletion_review` completion (skipped by Owner decision — consumes fixtures later plans need; covered by unit specs instead) | `Audit Test Client *` | `C-06-client-crud-hardening-progress.md` (top-of-file callout) |
| ⬜ C-22 | §3.2 + §3.7 (need production writes + real emails) · §3.5/§3.5a (structurally impossible pre-deploy — limiter fails open under `next dev`; must run post-deploy) | n/a (rate-limit/honeypot live checks) | `C-22-booking-form-abuse-protection-progress.md` |
| ⬜ C-04a | §3.2 (4-role × 4-viewport sweep, 14 steps) + §3.3 (5 screenshots) | `d8a61721` (both email fields NULL — zero email risk) | `C-04a-cancellation-restore-progress.md` §0k |
| ⬜ C-05 | §3.2 (16-step sweep) + §3.4 (screenshots) | `eaafbb1a` (cancelled), `1d503d3b` (past-dated, B-171 repro) | `C-05-cancelled-bookings-inert-progress.md` §3 |
| ⬜ C-01 | §3.5 (4-role sweep — Templates tab visibility, 16-field editor UX, override round-trip) + §3.6 (screenshots) + §3.3 (Resend dashboard spot-check). **Pipeline mechanism itself already proved live** (§3 of the progress file) — this sweep is RBAC/UX verification only, not correctness-critical. | `77f90d24`, `ae9bb5bd` (both example.test, both currently "handled") | `C-01-review-request-email-progress.md` §4 |
| ⬜ **C-11** | **Full verification pack — §3.2 role×theme×viewport, §3.5 WCAG AA per admin section, §3.6 print, §3.7 FOUC ×5-10, §3.8 ~35 screenshots.** Tier 1 first (only a human eye can close these): byte-identical dashboard render for Owner/Admin/Coordinator at 375+1280; B-01 zero-state marquee reading as a clear "0"; Therapist render sites + restored hero eyebrow; **theme toggle** (dark default → light → system, persisting across navigation and sign-out/in); **portalled UI in dark mode** (dropdown, dialog, toast); print renders light. Full ordered pack in the progress file §4. | All 12 staff rows have `theme_preference = NULL`, so every account should show DARK on first sign-in — that IS the dark-default test | `C-11-dashboard-variants-design-system-progress.md` §4 |
| ⬜ **C-10** | **Phase A discovery is the whole plan's input and it needs sign-in.** A Playwright walk at 375 + 768 + 1280 across the ~25 **admin** surfaces in brief §2.1 (dashboard, bookings list/new/detail, clients, enquiries, emails, operations, privacy, roles, services, settings, staff, calendar, reports…), cataloguing bottom-spacing and footer-overlap defects. Phase B then fixes each catalogued entry. Its ⛔ (halt unless C-16 is merged) is **already satisfied** — C-16 shipped `d22ab37`. | n/a — read-only sweep, no fixtures created | `C-10-bottom-spacing-footer-overlap-plan.md` §0-1 |
| ⬜ **C-23** | §3.9 — 375 + 1280 screenshots of all three date-branch states; §3.3's before/after payload comparison; §3.5 month↔day equivalence over ≥5 sampled dates; §3.6 admin-relaxation checks with public booking paused. All need the admin form behind sign-in. | `*.example.test` clients + standard test staff only | `C-23-admin-availability-calendar-plan.md` §3 |

**Offer standing (2026-08-04, restated at session end):** the Owner authorised agents to use admin logins directly — **three times, with increasing emphasis**. That cannot be accepted: entering passwords to authenticate is prohibited for the agent regardless of who authorises it, which protocol §3b records independently. Repetition does not change it, and no agent should treat a later instruction as superseding this.

**The workable substitute, offered and still open: the Owner signs in once in their own Chrome, and the orchestrator drives the already-authenticated session.** Operating a session the Owner created is not credential entry. This converts every row in this table from "the Owner performs it" into "the Owner clicks login once".

**Status at session end 2026-08-04: NO SESSION EXISTS.** The Owner reported logging in, but a probe of `/admin/dashboard/` in the same browser context still redirected to `/admin/login/?redirectTo=…`. The cause was found: the tab they were on was `http://localhost:3000/home/admin/` — i.e. `admin` appended to `/home/`, which 404s. **The correct URL is `http://localhost:3000/admin/login/`**, and a tab was left open there. Sign in as an **Owner**-role account (`rahmatherapy@outlook.com` or `phase10.owner@example.test`).

**One session covers both queued jobs** — C-10's overlap catalogue is layout, not permissions, and C-23's baseline is the booking form. A browser holds one session at a time, so a genuine multi-role RBAC comparison still needs the Owner to switch accounts between passes; those sweeps are already deferred here anyway.

**Also note: the dev server was DOWN at session end** (`curl http://localhost:3000/home/` → 000). It needs `pnpm dev` before any browser work. Agent-run production builds knocked it over twice during the C-18 run; builds are now banned in every dispatch and run once by the orchestrator, last.
| ⬜ **C-08** | **Phase C verify checkpoint — per-row Resend.** (1) Owner → `/admin/emails` → Delivery tab → Resend buttons on non-skipped rows. (2) Resend a `booking_confirmation` row → modal → confirm → success toast + new delivery row. **Expect a duplicate admin notification too — that is current behaviour, not a defect** (see below). (3) Re-click within 60 s → rate-limit toast. (4) Coordinator + Therapist → buttons ARE visible (intended H11 scoping, **not** a bug); Therapist acts only on their own assigned bookings. (5) Edit a template's copy, then resend an older event of that type → the new send reflects the edit (proves Phase B's wiring reaches the resend path). (6) Resend a `claim` / `client_assigned_therapist` row if one exists → a clear "isn't supported" error, never a false-success toast. | any non-skipped delivery row with a recipient; **never** booking `9d55ce2a` | `C-08-email-automation-expansion-progress.md` §2.6b |
| ⬜ **C-08** | **Phase D verify checkpoint — business notifications.** (1) Owner → `/admin/me` → Notifications card present; master toggle checked (seeded); **all five alert types checked** — that is the `types`-absent case behaving correctly, not a bug. (2) Set a notification email, untick "Slot claimed", save → toast; reload → persisted. (3) Admin → card visible, self-only. (4) Coordinator + Therapist → card absent. (5) As Admin, log a test enquiry (`*.example.test` only) → opted-in Owner receives `enquiry_logged` **at the notification address**; the logging Admin does not. (6) With "Slot claimed" unticked, Therapist claims a test slot → no email to Owner; delivery row `all_recipients_opted_out` if nobody else opted in. (7) Public booking request on a test fixture → one `admin_booking_notification` delivery row **per** opted-in recipient. | test fixtures only; **never** booking `9d55ce2a` | `C-08-email-automation-expansion-progress.md` §3.3 |
| ⬜ **C-13** | **⛔ CREATE THE GROUP-BOOKING FIXTURES FIRST — nothing else in this row can be checked without them.** Live queries confirm **zero bookings anywhere in the database have more than one participant**, so every group-rendering path C-13 shipped is unexercised by any fixture. Create via `/admin/bookings/new` (so the real RPC writes participants, assignments and snapshots consistently — **not** by SQL insert): (a) a **same-gender** group of 2–3, (b) a **mixed-gender** group (e.g. 2 female + 1 male), (c) one **fully assigned**, (d) one **unassigned**. Use `*.example.test` clients only. Then check: bookings list nested group card with per-participant sub-rows, main contact first · gender chip reads *"Needs 2 female + 1 male"* not *"Same-gender required"* · *"N of M therapists assigned"* badge and its warning→success tone · booking detail composite identity in the header description · browser tab title carries the composite identity · calendar tiles at all three sites (day/week row, month pill, sidebar) · dashboard Today panel shows *"Unassigned · Mixed group"* for the mixed one · the three emails carry their group blocks (`staff_assignment` must show **one** participant list, not two) · 375 px reflow · dark **and** light. | create fresh; `*.example.test` only; **never** booking `9d55ce2a` | `C-13-group-bookings-and-gender-clarity-progress.md` §0.2 |
| ⬜ **C-15** | **Full studio verification pack.** (1) `/admin/emails` → Templates tab: 16 cards grouped by audience, badges read Default. (2) Open `booking_confirmation` → the subject field's default now reads **"Rahma Therapy booking request received"** — the real one. (3) Type in a body field → preview updates ~500 ms, no iframe reload; insert a chip mid-sentence → token lands **at the cursor**. (4) "Show what's editable" → outlines appear, no network request. (5) **Edit a subject, save, then trigger a real send on a `*.example.test` fixture → the customer subject reflects the edit.** This is the AC2 fix and the single most valuable check in the pack. (6) "Send me a test" → `[Test] …` at your notification address; click again within 60 s → rate-limit error. (7) Reset → confirm dialog → badge flips to Default. (8) Coordinator + Therapist → read-only, no Save/chips/Reset/Send-test. (9) 375 px + dark/light. | `*.example.test` only | `C-15-email-template-studio-progress.md` §7.7 |
| ⬜ **C-02** | **Includes Phase B Step 5, deliberately routed here.** Step 5 wanted a live integration test creating 1 template + 12 bookings + 12 participants + 12 items + 12 assignments + 1 audit row. No agent can run it — the RPC's `auth.role() = 'service_role'` guard rejects `execute_sql` (which runs as `postgres`), and the real-client route means a Zone-2 production write. **Owner decision 2026-08-02: route (a) — discharge it through this sweep instead**, which exercises the identical RPC path for real. Plan §3.2 critical paths: (1) Owner creates a **weekly / until_cancelled** series → **exactly 12** occurrences materialise + the `recurring_series_created_client` email sends + the series view renders. (2) Admin creates **fortnightly / after_count=6** → 6 occurrences. (3) Coordinator creates **monthly / until_date** → capped at the end date (expect **3** in the first batch — the plan's old "4" was never true at any anchor). (4) Therapist cannot reach the recurring section. (5) Cancel a series with future occurrences → all future ones cancelled, past untouched, template stamped, audit row written, **and the client receives a `recurring_series_cancelled_client` email naming how many upcoming visits were cancelled** (Owner decision 2026-08-02 — plan Step 7's cascade sent nothing, which made the confirm modal's promise false; Phase Fb added the email rather than reword the modal). Check the delivery row appears in `/admin/emails`. (6) Monthly + a day-29 first date → rejected before the RPC. Also confirm the consent checkbox blocks a recurring submission when unticked.<br><br>**(7) Single-occurrence cancel (§3.2 #6):** cancel ONE visit inside a live series via the normal booking cancel flow → that visit cancels, the series continues, and the horizon still extends later. Distinct from cancelling the whole series.<br><br>**⏳ POST-DEPLOY ONLY — these FOUR cannot run until the Cloudflare deploy activates `0 3 * * *`** (structurally impossible pre-deploy, same category as C-22's limiter checks): **(8)** §3.2 #10 — deactivate a therapist bound to a series → the *next* occurrences are created **unassigned**. *(Note: the create RPC **rejects** an inactive bound therapist outright at creation time, so the graceful unassigned fallback exists only in the extension cron — which makes this a cron test, not a form test.)*; **(9)** §3.2 #7 — a series whose horizon is inside the window gets extended, and **verify the new dates land on the series' own weekday** (this is the trap Phase G exists to avoid); **(10)** §3.2 #8 — a cancelled template is skipped by the cron; **(11)** §3.2 #9 — an `after_count`-complete series is a cron no-op.<br><br>**§3.5 screenshots — 8 required, ZERO captured** (`redesign/evidence/C-02/` holds only the Phase B evidence doc): ManualBookingForm step 4 with RecurringSection expanded at **1280** and **375**; series view at **1280** and **375**; cancel-series confirm modal at 1280; calendar with recurring badges at 1280; bookings list with the Series filter active at 1280; `/admin/services` edit form showing the allow_recurrence toggle at 1280.<br><br>**§3.6 WCAG / responsive:** series view usable at all four viewports; recurring-badge contrast in **both dark and light** (dark is the default — every staff row has `theme_preference = NULL`); recurring form-section validation messages legible. | create fresh, `*.example.test` clients only; **never** booking `9d55ce2a` | `C-02-recurring-bookings-progress.md` §4.6 |
| ⬜ **C-03** | **Enquiry→booking conversion verification (plan §3).** Nothing here is Zone-2 — the one migration is already applied. **⚠️ Start here, because it also creates the fixture the rest needs:** production has **3 enquiries, 0 converted**, so no already-converted enquiry exists. **(1)** Open an enquiry → convert it to a booking through the admin UI. That single action creates the fixture *and* exercises the whole flow. Confirm on the way: the service was **pre-selected** from the enquiry's free text (and that you could still change it); the success toast reads differently from a scratch booking; the booking detail page then shows an **Origin panel** linking back to the enquiry, plus a "created from enquiry" toast. **(2) The B-106 guard — the reason the fixture matters.** Now re-visit `/admin/bookings/new?enquiryId=<that same enquiry>`. It must **redirect to the booking that already exists**, not let you create a second one. (Owner decision 2026-08-03: this end-to-end check was deferred here rather than planting a fixture by SQL.) **(3) Fuzzy pre-select is deliberately conservative** — a bare "massage" enquiry matches *nothing* (it ties between the 60- and 30-minute services) and must leave the field **empty**, not guess. "1 hour massage" also yields no match, because the live service is spelled "1-Hour Massage Therapy". Both are safe by design; confirm neither silently picks a wrong service. **(4) Cancel routing:** from a converted-from-enquiry form, Cancel should return you sensibly, and the Leave dialog must agree with the Cancel button. **(5)** Convert a second enquiry while the first form is open in another tab — drafts must not clobber each other (they are now scoped per enquiry). **Known and expected, not a bug:** on the just-converted redirect you will see **two** toasts; that is the plan's own B-105 finding, deferred to C-12+. | `*.example.test` enquiries only; **never** booking `9d55ce2a` | `C-03-enquiry-to-booking-conversion-progress.md` §4 |
| ⬜ **C-09** | **Cache-invalidation and filter round-trip verification (plan §3.3).** Nothing here is Zone-2 — no migration, no production write, no deploy — but every surface is behind admin sign-in. **(1) Per-mutation invalidation:** perform one mutation of each resource type (create a client · update a booking · change a setting · assign a booking · log an enquiry) and confirm the affected list/detail page reflects it **immediately**, not after ~60 s. An update that only appears after a minute means its `updateTag` is missing or wrong. **(2) Cross-surface invalidation:** create a client → `/admin/clients` shows the new row at once; cancel a booking → both `/admin/bookings` and `/admin/calendar` update. **(3) Filter round-trips on the five swept surfaces** (enquiries · staff · operations · emails delivery · privacy): set each filter, confirm the URL carries it, the results actually narrow, the controls reflect the URL on reload, and the empty state reads "no results for these filters" rather than "no data yet". **(4) Two regression checks from the fix round:** on `/admin/staff`, filter by **Bookable alone** — deactivated staff must NOT appear (this was broken and fixed); and visit `/admin/emails?tab=delivery&range=custom&from=x` and `/admin/privacy?range=custom&from=x` — each must render normally, ignoring the bad date, **not** 500 (also broken and fixed). **(5)** Search a name containing a comma or bracket (e.g. `Smith, John`) on enquiries and on the emails delivery feed — results must come back, not silently empty. | `*.example.test` clients only; **never** booking `9d55ce2a` | `C-09-cache-invalidation-filter-cleanup-progress.md` §5 |
| ⬜ **C-18** | **🔴 SENTRY CONSOLE — the code fix cannot reach production until you deploy, so these are the only things that help today.** Sentry Session Replay was transmitting the `/booking/manage` booking-management **bearer token** to Sentry (~10% of sessions, **100% of sessions with an error**); `NEXT_PUBLIC_SENTRY_DSN` is confirmed set in production, so this was live. Fixed in code at `09b2e26`, but that ships only with the deploy. **(1)** Add an **Advanced Data Scrubbing** rule in the Sentry project that strips the `token` query parameter — unlike the app's `beforeSend`, ingest-side rules **do** cover replay payloads, so this stops new captures immediately. **(2)** Consider **already-stored replays**: any captured from `/booking/manage` hold live booking credentials in a third-party system for the length of your retention period — purging the affected replays and/or shortening retention is worth a decision. **(3)** Disabling Session Replay ingestion at project level is the blunt option and leaves error monitoring intact. **All three are external-console actions — Owner-only; no agent will touch your Sentry console.** | n/a — external console | `C-18-cookie-consent-progress.md` §2 |
| ⬜ **C-18** | **⚠️ Upgrade hazard: one leak channel is verified but not pinned by a test.** The fix closes four URL-bearing channels in Sentry Replay. Three are pinned by regression tests. The fourth (`Meta.data.href`, embedded in every DOM snapshot) **has no hook to intercept it** — it is closed by argument, not by code: in session mode no further full snapshot occurs after the initial one because `_experiments.continuousCheckout` defaults undefined, and in buffer mode `stop()` computes `forceFlush: false` and destroys the buffer unsent. Both facts were verified in `@sentry/*@10.51.0` source. | C-18 Phase 0 verification 2026-08-04 | **A future Sentry SDK upgrade could silently reopen this channel** — no test would fail. Worth re-checking those two defaults whenever `@sentry/nextjs` is bumped. Recorded because "verified today" and "will stay true" are different claims. |
| ⬜ **C-17** | **⛔ Two Zone-2 items + one confirmation, all riding the deferred deploy.** **(1) Set `NEXT_PUBLIC_GA_MEASUREMENT_ID=G-WM8BCYG060` in the Cloudflare project.** ⚠️ `NEXT_PUBLIC_*` vars are inlined at **BUILD** time — the var must be present in the **build** environment, not only the runtime env. A dashboard-only runtime setting will silently produce a build with **no analytics at all**, and the failure looks exactly like "nobody visited". Verify how the OpenNext/Cloudflare pipeline injects build env before relying on it. **(2) Post-deploy live GA verification** (plan §3 item 3, needs your GA Realtime access — no offline substitute): (a) page_view on a hard load; (b) page_view on a **client-side nav** between two public pages — this is the enhanced-measurement check, and if it does NOT appear, the brief §2.3 SPA-pageview fallback must be implemented and re-verified; (c) **exactly one** `booking_request_submitted` after a test booking on a `*.example.test` fixture, then clean the fixture up. **(3) ⏸ Confirm `G-WM8BCYG060` is the intended production property.** **Also needs a browser: the ad-blocker pass** (plan §3 item 4) — complete a booking with gtag blocked and confirm the flow finishes with **zero console errors**; the event call is optional-chained specifically so a blocked GA cannot break a real customer's confirmation screen. | `*.example.test` only; **never** booking `9d55ce2a` | `C-17-google-analytics-progress.md` §4 |
| ⬜ **C-16** | **§3.5 4-role × 4-viewport sweep + §3.3 multi-page proof + the roles visual pass.** Nothing here is Zone-2. **(1) The multi-page proof is the one check no unit test can replace:** temporarily set `LIST_PAGE_SIZE = 3` in `src/lib/pagination.ts` (dev session only — **never commit it**), then walk bookings, clients and enquiries: Prev/Next, the "Showing X–Y of Z" readout, page reset when a filter/search/sort/view changes, a stale `?page=99` clamping to the last page rather than 404-ing or rendering empty, and the empty-search state. Restore the constant and re-run the suite. **(2) Per role:** confirm the pager appears and works on every converted surface (bookings, clients, enquiries, emails delivery, privacy requests, operations), that the Therapist scoped bookings path is unchanged, and that the bookings **chip counts match the list** when you open each chip — a count that disagrees with its own view is the one defect the whole predicate design exists to prevent. **(3) Cap+view-all surfaces** — privacy sensitive notes, password requests, client notes rail, staff/clinic blocked dates, staff past assignments: confirm each states the true total and that "view all" never appears when it would do nothing. **(4) The roles page** — follow `redesign/evidence/C-16/roles-visual-checklist.md` at 375 and 1280; no code changed, so this is a confirmation pass, not a before/after diff. | `*.example.test` only; **never** booking `9d55ce2a` | `C-16-data-growth-pagination-progress.md` §10 |
| ⬜ **C-16** | **Two Zone-2 follow-ups, neither urgent, both recorded with reasoning.** **(a) `bookings` indexes before real volume arrives** — see the dedicated row in the technical-items table below. **(b) PostgREST aggregates are DISABLED on this project** (`PGRST123`, independently confirmed by four agents). That is why `/admin/clients` ships **capped at 1000** (6000 via view-all) rather than truly paginated: its lifecycle, payment-standing and last-visit sort derive from a per-client bookings summary computed in memory, and pushing those into SQL needs an aggregate, a view, an RPC or a derived column — all migrations. **Only act on this if you want the clients directory paginated rather than capped**; the cap is honest (it names itself and states that the count, the stats and every page cover only the rows read). | n/a — schema/config decision | `C-16-data-growth-pagination-progress.md` §10 |
| ⬜ **C-07** | **§3.2 per-role × per-viewport sweep (18 numbered items) + §3.4 screenshots.** No Zone-2 action, no migration — every item is behind admin sign-in only. **Owner/Admin:** client detail shows exactly **one** "Book again" CTA (header) · `/admin/me` Quick Links present and role-appropriate · booking detail with assigned staff → staff name is a **link** · create a manual booking with no prefill → land on detail with **one** "Booking created." toast (a second toast here would be a regression — A2's double-toast was fixed in `b8f2f5d`) · type "Bedford" in the city field → inline warning · `/admin/dashboard` reads **"Mine"**, not "Personal" · **Yesterday** chip present in the filter strip · change the date via a chip → the form's date inputs sync, and vice versa · **Team/Mine toggle** narrows and restores the tiles · `/admin/bookings` defaults to **Attention** · apply a filter combo → save it → click the saved chip → the URL updates and the list matches. **Also worth one explicit check:** after saving a view, sign out and sign in **as a different staff member** — the first person's saved views must **not** appear (this is `f038b4f`'s privacy fix; it is the one thing no unit test can prove end-to-end). **Coordinator:** items 1–5 as Admin, **no scope toggle visible**, saved views work. **Therapist:** `/admin/me` shows Therapist-specific links · `/admin/bookings` with no query defaults to **Today** (this was live-broken before B3 — the tab said Today while the list was filtered by attention) · "Open to claim" shows a **next-7-days** window with clarifying copy · staff names on booking detail render **unlinked**. | `*.example.test` only; **never** booking `9d55ce2a` | `C-07-routing-and-per-role-defaults-progress.md` §1.11 |
| ⬜ C-FIELDWORK | §3.2/§3.4 (4-role sweep + screenshots) — booking-detail dual-view, dashboard mounts, Mark-complete temporal guard. **No safe fixture currently exists** — needs a fresh test booking assigned to a test staff account created via the admin UI first (recommended: `test.therapist@rahmatherapy.example.test`), since production has none assigned to any test/Owner practitioner account today. | none currently — Owner creates one first | `C-FIELDWORK-EXPERIENCE-progress.md` §4 |

**Never use in any sweep:** booking `9d55ce2a-7a76-42ed-9166-a33fa66ee7fe` (Badar — real customer, real email) or the Owner's own `rahmatherapy@outlook.com` in any email-test path.

## Logged-not-fixed technical items (no live affordance, no urgency, informational)

| Item | From | Why it's not urgent |
|---|---|---|
| **⚠️ The privacy REQUEST QUEUE has never had a row cap of any kind, and C-16's audit misattributed it.** The audit recorded `/admin/privacy` as "capped at 25, no pager". That 25 (`PRIVACY_NOTES_LIMIT`) belongs to the **sensitive-notes rail** (`client_notes`), not the request queue. Confirmed by diffing pre-C-09 code (`git show 9b53902`): the `client_privacy_requests` query never carried a `.limit()` before C-09's extraction and does not now. | C-16 pre-flight prep 2026-08-03 | Worse than the audit stated, and the surface is GDPR-facing. Harmless at current volume, but it is the one list nobody has ever bounded. **C-16 Phase D owns the fix** — flagged here so the correction is not lost if C-16's Phase A inventory re-copies the audit's original claim. |
| **The clients list issues TWO unbounded reads per page load, while looking paginated.** `getClientsListData` selects every client row, and `clients-list-data.ts` then unconditionally selects **every row of `bookings`** (no filter, no range parameter exists for that second query at all) purely to build the client→bookings map. `clients/page.tsx` has its own 50-row **in-memory** pager, so the UI appears solved while the query cost is identical to the bookings list's. | C-16 pre-flight prep 2026-08-03 | The in-memory pager is why this never looked urgent. **C-16 Phase C owns it** — noted here because "clients already has a pager" is a natural and wrong conclusion for anyone triaging by eye. |
| ~~**⚠️ Saved bookings views are not namespaced per staff member, and are never cleared on sign-out.**~~ **✅ FIXED — C-07 B4, commit `f038b4f` (2026-08-03).** The key is now `rahma.admin.bookings.saved-views.v2.${staffId}`; the legacy global key is purged on load and **deliberately not migrated** (migrating would hand the previous user's saved searches — which can carry a client name via `search=` — to whoever signs in next, i.e. perform the exact leak). Existing saved views are lost once, by design; B-161 is explicitly a v1 localStorage feature. 9 specs added, incl. cross-staff isolation, verified non-vacuous. | C-07 B4 gap analysis 2026-08-03 → closed same day | Row kept for audit trail per this file's own convention. Detail: `C-07-routing-and-per-role-defaults-progress.md` §1.9. |
| **Saved bookings views have no mobile save affordance.** The create flow lives only in the `hidden md:block` desktop bar, so a Coordinator on a phone can apply a saved view but never make one. | C-07 B4 gap analysis 2026-08-03 (gap 4) | **Owner decision 2026-08-03: deferred to C-12+.** Lowest priority of the four B4 gaps — Coordinators are desk-based more often than Therapists, and applying (the common case) already works on mobile. Related, also Owner-decided: the desktop bar **stays always-visible** rather than hiding when empty, an accepted deviation from the plan's B4 checkpoint, chosen for discoverability. |
| **⚠️ `scripts/measure-admin-bundles.mjs` cannot answer the bundle-budget question any plan asks it, and NINE plans have now been hit** — C-04a, C-05, C-FIELDWORK, C-08, C-15, C-13, C-02, C-03, C-07. Its hardcoded `ROUTES` list carries **no `/admin/bookings*` route at all**, and its only baseline (`bundle-pre-B1.json`) was captured **2026-05-24 at `d2e6512`, before Band B** — so every delta it reports is cumulative across Band B *and* Band C rather than attributable to the plan under test. | C-07 closeout 2026-08-03; **count corrected 4 → 9 by drift checkpoint #3** the same day (the programme's running tallies disagreed with each other and undercounted) | **Needs an Owner decision, not an implementer's judgement**, because the fix is tooling scope no plan owns: add the missing routes and re-baseline at a known SHA. Until then every plan whose ceiling names `/admin/bookings*` must record the gate **NOT RUN** rather than passed — the precedent C-03 set and C-07 followed. Absolutes captured at C-07 closeout so a future comparison has a real anchor: `/admin/dashboard` 482.22 kB gzip · `/admin/clients/[clientId]` 338.56 kB · `/admin/me` 448.31 kB. |
| **📋 DECISION A — 98 files / 679 occurrences carry theme-blind `oklch(…)` colour literals, and no plan owns fixing them.** 3 files/33 occurrences in shared primitives (`src/components/ui/{badge,button,input}.tsx`), 95 files/646 in `src/app/admin/`, **zero in the public site**. **11 brand-new files created during this programme carry the debt from their first commit**, each citing rule 11's match-the-source requirement — the pattern is actively being replanted. | Drift checkpoint #3 groundwork 2026-08-03, **superseded by the formal checkpoint the same day** | **Three corrections to this row's earlier framing.** (1) The long-quoted **"68 files" figure does not reproduce** under any of five grep methodologies (range 54–98); 98/679 is the defensible count at HEAD. (2) **"C-11 never touched them" is wrong** — C-11 ran a deliberate, Owner-approved, narrowly-scoped sweep and explicitly logged the ~90-file remainder as deferred. A recorded deferral, not an oversight. (3) **The fix is mostly mechanical:** the dark-mode-correct token values already exist in `tokens.css` (e.g. `--admin-status-cancelled-text`) and are **byte-identical** to the hardcoded literals — find-and-replace, not new design work. **One item deserves fixing on its own merits regardless of the decision:** `src/components/ui/input.tsx:116,143` renders the shared form-field error text and required-asterisk with a ~4-point lightness gap on the dark panel — **functionally invisible, not merely low-contrast**, in a shared primitive that appears wherever admin forms do, with dark the default for all 12 staff accounts. **Recommendation: a one-sentence Part 0 exception permitting token substitution when a file is already being edited**, rather than a dedicated retrofit plan. |
| **📋 DECISION B — ⚠️ the dashboard's preset chips and the server's range resolver disagree, and the masking is INCOMPLETE — there is a live hole.** `buildPresets()` (client) knows `yesterday` and `last_30`; `getRangeDefaults()` in `reporting.ts` knows neither. | Drift checkpoint #3 groundwork 2026-08-03, **materially corrected by the formal checkpoint** the same day | **The groundwork said C-07 B1's fix "masked" this. It does not fully mask it.** `/admin/me`, `/admin/staff/[staffId]/performance` and `/admin/reports`/`export` all call `parseReportFilters` on **raw, undefaulted searchParams** — only `/admin/dashboard` is safe, and only via an unrelated pre-programme `?? today` fallback. A hand-typed or bookmarked `?range=yesterday` on those three surfaces silently returns **current-month-to-+30-business-days**, i.e. the same window as the default month view, so it looks like it worked. Two of them auto-generate onward links (`me/page.tsx:156`, `performance-helpers.ts:125,128`) that **re-propagate the bad key with no dates**. **Fix (i)** add two cases to `reporting.ts` — smaller and safer long-term, needs the Part 0 exception. **Fix (ii)** guard at 4+ call sites — avoids the exception but recreates the very duplicate-logic pattern this checkpoint tracks. **Recommendation: (i)** — and note `reporting.ts:966-969` shows this function **already shipped this exact class of bug once**, caught by an audit. |
| ~~**The post-deploy-only check list spans five plans with no consolidated owner.**~~ **✅ CONSOLIDATED — `redesign/evidence/checkpoint-3/post-deploy-runbook.md`, 13 items in run order.** | Drift checkpoint #3 formal 2026-08-03 (checkpoint #1 first flagged the trend) | Each item carries what to check, its source plan, why it cannot run pre-deploy (**structurally impossible** vs merely inconvenient), what a failure would mean, and whether it is customer-facing. **Two items were found already obsolete:** C-08's "expect 14" histogram target is stale (C-02 registered two more email event types afterwards), and the `d8a61721` fixture recommended for C-04a's queue-drain check is unusable for it — `sendTrackedEmail` (`notifications.ts:493-504`) skips the queue insert entirely when the recipient email is null, and that fixture has both email fields NULL. **Also a correction to the maintenance-flag framing:** HEAD's *committed* `src/lib/maintenance.ts` already reads `MAINTENANCE_MODE = true` (`git show HEAD:src/lib/maintenance.ts`), and the deploy ships committed code — so the risk is not "remember to flip it back" but **"never let the working copy's uncommitted `false` be swept into a commit"**. |
| **⛔ PRE-LAUNCH (Zone-2): `bookings` needs indexes before the table grows.** C-16's chip counts fan out **11 `count:"exact",head:true` queries per clinic-wide page render** (`getBookingViewCounts`, `bookings-list-data.ts:950-967`), and `bookings` carries **only** a PK, a partial `(client_id,status) WHERE status='completed'`, and a partial on `recurring_template_id` — **nothing on `booking_date`, `start_time`, unqualified `status`, `assignment_status`, `reschedule_status`, `customer_cancelled_at` or `payment_status`** (verified directly against `pg_indexes`, 2026-08-03). Every chip predicate is a `Seq Scan`, whose cost is O(table size). 15 rows today; the brief projects 10–15k. | C-16 Steps 6–7 FULL verification 2026-08-03; independently re-verified by the orchestrator | **Real, but smaller and later than it first reads.** (a) **Not a regression — a net saving.** The pre-C-16 page fetched every booking row and filtered in memory; the counts are `head:true` and transfer nothing, so even at 15k rows this shape is far cheaper than what it replaced. (b) **The genuine amplifier is cache invalidation, not query cost:** all 11 counts cache 60 s on `TAGS.BOOKINGS`, and every booking create/update/assign/cancel fires `updateTag(BOOKINGS)` and invalidates all 11 — in a working clinic they may rarely serve from cache at all. That is the thing to measure. (c) The join-bearing chips (`claimable`, `assigned`) are half-protected: `booking_assignments` **is** indexed on `(assigned_staff_id,status)` and `(booking_id)`. **Recommendation: add the indexes around the four-in-one deploy, before real volume arrives — an index on `booking_date` also helps the list query's `ORDER BY` + `.range()`, a wider win than the chips alone. Do NOT take the pre-approved Q9.5 fallback** (active chip + total only): it would degrade a working feature to pre-empt a problem that does not exist yet, and the counts are the whole UX value of Step 6. Migration ⇒ Zone-2 ⇒ needs explicit chat approval; C-16 stays zero-migration per its own §9.4. |
| **⚠️ The emails delivery feed's `unstable_cache` has never hit, and it mints a new cache entry per request.** `resolveDeliveryDateBounds` (`src/app/admin/emails/emails-data.ts:334-343`) derives `fromIso` from `Date.now()` at **millisecond precision**, and that string is part of the cache key (`:397`). For `today`, `last_7_days` and the default `last_30_days`, every single request therefore produces a unique key — the cache is 100% ineffective and accumulates one entry per request. Surfaced as an intermittent failure of `emails-data.test.ts > "does not re-run the fetcher on a cache hit"`, which is not a flaky test but the defect reporting itself. | Found during C-16 Phase C Steps 6–7, 2026-08-03; independently confirmed by the orchestrator at `emails-data.ts:334-343,397` | Pre-existing C-09 Phase D Step 11 code. **Being fixed inside C-16 Phase D Step 9 rather than deferred** — not as opportunistic widening, but because Step 9 adds a count query beside the rows query, and two calls resolving `Date.now()` a millisecond apart would make the pager's total disagree with its rows. Step 9 resolves the bounds **once** and threads them into both queries; the stable cache key follows for free. |
| **`visibleBookingViews` and `BookingsChrome`'s chip arrays are a necessary second source.** `bookings-list-data.ts:916` must name the same chips as `BookingsChrome`'s `FULL_*`/`THERAPIST_*` arrays, because a Server Component cannot import a value out of a `"use client"` module. Currently pinned equal **by test** rather than shared. | C-16 Phase C Step 6, 2026-08-03 | The seventh instance of this programme's "two things that should be one thing" pattern — but the first that is *structurally* forced rather than accidental. The natural single home is `bookings/_helpers.ts` (explicitly free of server-only imports); a one-line move would collapse them. Left in place because it was outside the implementer's scope and the test pin is genuinely load-bearing. **Offer, not a defect:** say the word and the next plan touching bookings collapses it. |
| **D1's presumed owner is wrong — the duplicate `MISSING_COLUMN_CODES`/`hasErrorCode` shims will stay unclaimed after C-14 ships.** Checkpoint #1 assigned them to "whichever of C-02/C-08/C-14 next needs the shape"; C-02 and C-08 have both passed without needing it. | Drift checkpoint #3 formal 2026-08-03 | **C-14 does not need it either** — it uses an atomic co-deploy migration strategy, the opposite of the tolerate-a-missing-column pattern D1 exists for. Byte-identical, doubly dead, and now with no candidate owner left in the programme. Needs either a deliberate claim or an explicit "stays as-is" decision; it is dead code, so the cost of leaving it is documentation confusion rather than risk. |
| **The dashboard's "failed emails" and "open errors" attention tiles go stale for up to 60 s.** `dashboard-data.ts` reads `email_delivery_events` and `operational_events` but its `unstable_cache` tag list carries neither `EMAILS` nor `AUDIT`, so the emails actions' tags don't intersect it. Their `revalidatePath("/admin/dashboard")` does **not** compensate — `revalidatePath` and `updateTag` are separate cache layers. (Gap D from C-09's exhaustive cache matrix; Owner-scoped out of the second addendum.) | C-09 closeout 2026-08-03 | Bounded to the 60 s revalidate window, and both tiles are informational rather than transactional — nobody acts on a stale "3 failed emails" count differently than on a fresh one within a minute. Fix is adding two tags to one array; natural work for whichever plan next touches the dashboard. |
| **A fire-and-forget `recordOperationalEvent` path writes with no cache tag at all.** `emails/actions.ts`'s scope-refusal early-returns write an `operational_events` row via `.catch(() => undefined)` and tag nothing — not even `AUDIT`, since no `audit_logs` row is written on that path. (Gap E from the same matrix.) | C-09 closeout 2026-08-03 | Lowest severity of the set: an internal warning-log event, not a business record, written only when a staff member hits a permission scope refusal. Leaves `/admin/operations` and the dashboard's operational tile stale up to 60 s. |
| **⚠️ The review-request cron will ask a standing client for a review EVERY WEEK, indefinitely.** `review-emails/route.ts` selects on `status='completed'` + `review_email_sent_at IS NULL` with no dedup by client or series, and `review_email_sent_at` lives on the **booking**, not the template — so a weekly `until_cancelled` series produces one review request per completed visit, forever. There is no per-client review-ask frequency cap anywhere on that path. | C-02 closeout 2026-08-02 | Zero recurring bookings exist today, so nothing is being over-mailed right now — and each individual send is functionally correct. It becomes real the moment the first standing client completes their second visit. **Needs a product decision (cap per client per N months? once per series? only the first visit?) before recurring bookings are promoted to clients**, not a code fix in isolation. The single most consequential finding of C-02's closeout. |
| **The Dashboard's Source filter cannot select "recurring".** `dashboard-variant-shared.tsx`'s `sourceOptions` is a hardcoded eight-value list. `/admin/reports` derives its options live from the data and picks `recurring` up automatically; only the Dashboard variant is static. | C-02 closeout 2026-08-02 | One array entry. Invisible until recurring bookings exist, and Reports already covers the analytical need. The underlying query works fine if the value reaches it. |
| **The admin form's required "Booking source" is silently discarded for recurring submissions.** Step 1's field is required and always rendered, but `recurringSchema` takes no such parameter and the RPC hardcodes `booking_source = 'recurring'`. The operator fills a required field whose value is thrown away, with no UI hint. | C-02 closeout 2026-08-02 | No data risk — `recurring` is the correct source for these bookings and reporting groups on it correctly. Purely a misleading-UI issue: either hide the field in recurring mode or note that it does not apply. |
| **Correction: C-06's recurring-cascade (decision D1) is NOT load-bearing, contrary to what the programme has believed since 2026-07-26.** The cascade exists and is correct, and the FK really is `ON DELETE RESTRICT` — but `deleteClient` is a **soft** delete (`UPDATE ... SET deleted_at`), and `ON DELETE RESTRICT` is only evaluated on a real `DELETE`. No `.from("clients").delete()` exists anywhere in `src/`. | C-02 closeout 2026-08-02 | Nothing to fix — the cascade is harmless, sensible insurance and cancelling templates for a deleted client is right regardless. Recorded only so the *reasoning* in the ledger and D1 is not carried forward as fact: the FK it was written to guard against cannot fire on any path this app has. |
| **Four FK columns on `recurring_booking_templates` are unindexed** (`bound_therapist_id`, `cancelled_by`, `created_by`, `service_id`) — a new `unindexed_foreign_keys` advisor finding. | C-02 closeout 2026-08-02 | The table holds one row per active series; at that cardinality the planner will never care. Worth folding into a future migration only if query patterns change. The four "unused index" advisories on the same table are expected at zero rows. |
| **`scripts/measure-admin-bundles.mjs` cannot measure the routes three plans have now needed it for.** Its `ROUTES` array covers only six dashboard/reports/staff routes; C-02's, C-15's and C-08's new routes are all outside it, and `bundle-pre-B1.json` holds no baseline for them either. | C-02 closeout 2026-08-02 (C-04a, C-08, C-15 hit it first) | No plan's bundle ceiling that touches `/admin/bookings/*` or `/admin/calendar` has ever actually been verified — those gates are **never-run, not passed**. C-02 captured current absolute sizes (progress §4.2) so a future baseline exists. Extending the script is a one-off programme chore, best done before C-10, which measures final page heights. |
| **The business-notification opt-in is gated on a ROLE in the resolver but on a PERMISSION in the UI.** `resolveBusinessNotificationRecipients` filters opted-in staff by `role?.name === "Owner" \|\| role?.name === "Admin"` (`src/lib/email/notifications.ts:341-345`), while `saveNotificationSettings` admits anyone passing `canManageEmailTemplates(profile)` (`src/app/admin/me/actions.ts:47-49`). The two gates are not the same set. **Verified in code 2026-08-02.** | Owner independent audit 2026-08-02 | Latent, not live: today the permission and the two role names coincide on the seeded rows. It bites the moment a custom role (or a Coordinator) is granted `manage_email_templates` — that person can tick the opt-in, see it persist, and never receive a single alert, with no error anywhere. The mirror case is an Owner/Admin who loses the permission: still targeted by the resolver, but locked out of the card that would let them opt out. Fix is one decision — make the resolver permission-driven, or the UI role-driven — not two code changes. |
| **`resolveBusinessNotificationRecipients` has no email-level dedup.** It maps each opted-in staff row to `notification_email ?? email` with no uniqueness pass, and `sendToBusinessRecipients` sends one tracked email + one delivery row per element. **Verified in code 2026-08-02.** | Owner independent audit 2026-08-02 | Two staff sharing a notification address — or one person whose `notification_email` equals another row's `email` — receive N copies of every business alert, and `/admin/emails` shows N delivery rows for one logical send. No data loss and nothing customer-facing (these are all clinic-internal alerts), but it makes the delivery log misreport and is a one-line `Map`-by-lowercased-email collapse at the end of the resolver. |
| **Resend can resurrect a cancellation that C-04a's Undo deliberately suppressed.** The Resend button renders on `cancelled_by_restore` rows, and `dispatchResend` re-sends the customer leg plus the staff fan-out with no booking-status check. | Drift checkpoint #2 (F-3) | Requires an Owner to deliberately resend a row for a booking that was cancelled and then restored — narrow, but the outcome is a customer receiving a cancellation notice for a booking that is currently live. A status guard in `dispatchResend` is the whole fix. |
| **C-15's closeout undercounts its own eslint suppressions** — it states four; the true count is **seven** (`LivePreview` 1, `TemplateEditor` 4, `TemplateGallery` 2), and the file contradicts itself two sections apart. The three `exhaustive-deps` suppressions were never individually argued. | Drift checkpoint #2 (F-5) | Documentation accuracy only — no code defect. Matters because the suppression count is the metric the drift checkpoints use to judge lint erosion across the programme; an understated figure in a progress file is the one input those checkpoints can't independently sanity-check without re-counting. |
| **Four migration files carry filename versions absent from the live `supabase_migrations` ledger** (normal `apply_migration` behaviour — the MCP registers a different version than the local filename). | Drift checkpoint #2 (F-9), conditional | Unreachable today: no `supabase/config.toml`, no CI, and nothing runs `supabase db push`. It becomes live at exactly the moment the Owner's stated end-of-programme route executes — a GitHub push where a `db push` would try to **re-apply C-06's 698-line DDL** against a database that already has it. Worth reconciling filenames-to-ledger before that push, not before. |
| **`bookings.contact_email` is frozen at creation** — no UI field edits it, and every send resolves `contact_email \|\| clients.email`, so correcting a client's address does NOT fix their existing bookings. **Recommended fix: make the field editable on the booking management form + action (additive, zero behaviour change).** Explicitly do NOT flip the resolution order — phone-only walk-ins can legitimately have a NULL client email, so that "fix" could stop mail reaching people it currently reaches. Needs a plan that touches `BookingManagementForm.tsx`. | C-08 security review 2026-07-31 | **Measured: 15 bookings with a linked client, `0` mismatched, `0` upcoming.** Entirely preventive — nothing is misdirected today. |
| **`html_payload`/`text_payload` are never nulled after send** — written only on C-04a's delayed-cancellation path; the drain cron flips `delivery_status` on the same row without clearing the body, so rows retain the full rendered email (customer name, address, appointment details) indefinitely. Worth a retention/cleanup decision. | C-08 security review 2026-07-31 | The read path is now closed by the `e91c09c` RLS tightening, so exposure is much reduced. Storage-side minimisation remains open. |
| **`booking_restored_client` has no `templates-data.ts` entry** — C-04a added the restore email without registering it, so its copy is the one customer-facing email the Templates tab cannot reach. | C-08 Phase B 2026-07-31 | Harmless: the override wiring is correct and simply finds nothing. Natural fit for **C-15** (email template studio), which owns the registry. |
| **The dashboard Today panel shows a group's main contact only**, not composite identity — `CoordinatorDashboard.tsx`'s `appointment.title` is still `contact_full_name`, unlike the list, detail and calendar surfaces. | C-13 closeout 2026-08-01 | **Decided, not overlooked.** Brief AC4 lists this site conditionally; the blocker is that `getReportData` carries no `booking_participants` join and `reporting.ts` is untouchable. Phase E proved a bounded local SELECT solves it, but adding another query to the highest-traffic admin surface at the end of a plan, for a conditional criterion no live data can exercise, was the wrong trade. Fix is one query + the already-tested `composeBookingIdentity` for whichever plan next owns the dashboard. |
| **`PractitionerTodaySection.tsx` has no group or gender signal at all.** | C-13 closeout 2026-08-01 | Not a regression — C-13 never touched it (C-FIELDWORK's component, not on C-13's files list). Harmless today; once a real group booking exists, a Therapist's own daily card is the one surface giving them no indication it's a group. |
| **The claim queue and the Today panel disagree about mixed groups.** `blocks/ClaimQueueStripe.tsx:85` renders a flat generic `"Same-gender"` badge and reads the same `requiredGenderByBooking` map C-13 Phase H fixed, so a booking needing both a female and a male therapist reads **"Same-gender"** in the claim queue while the Today panel correctly reads **"Unassigned · Mixed group"** for the identical booking. | C-13 Phase H 2026-08-01 | A Coordinator glancing at the claim queue gets actively misleading information about what the booking needs. `ClaimQueueStripe.tsx` is a **C-11 block, not on C-13's files-touched list**, so fixing it would have been unsanctioned scope-widening (rule 6b). Correctly logged, not fixed. Small fix for whichever plan next owns `dashboard/blocks/**`. |
| **C-15 left the `providedOverrides` seam unwired on five templates.** C-15 added the optional parameter to all six async renderers but only passed it from `sendReviewRequestEmail` (fixed in `34e45da`); the other five still resolve overrides twice per send. | Drift checkpoint #2 (F-6) | Not merely wasteful: `resolveTemplateOverrides` swallows errors and returns `{}`, so a first-read failure with a second-read success ships an email whose subject and text part carry edited copy while the HTML body carries factory defaults. One-argument change per site. |
| **`booking_plain_text` is an editable, resettable gallery card that reaches no email.** All nine plain-text legs render with their sibling HTML template's overrides. | Drift checkpoint #2 (F-7) | Edit its footer, watch the preview update and the badge flip to "Customised" — and every real send keeps the old text. The UI asserts the opposite of what happens. |
| **Five event types have a Resend button that can never work** (`review_request_client`, `booking_restored_client`, `admin_booking_notification`, `booking_reschedule_request_admin`, `enquiry_logged`). Compounded for review requests: `sendReviewRequestEmail` discards the send status and stamps `review_email_sent_at` regardless, so the cron never retries. | Drift checkpoint #2 (F-8) | A hard-bounced review request is unrecoverable and the only visible affordance is a button that errors. |
| **Nine hardcoded light-only colour literals in three new C-15 admin files** (`TokenTextField`, `LivePreview`, `TemplateEditor`) — including `border-[oklch(40%_0.14_25)]`, verbatim the *light* arm of `--admin-danger-solid`. | Drift checkpoint #2 (F-4) | Dark is the default (all 12 staff rows have `theme_preference = NULL`), so a subject-overrun error renders at ~1.2:1 against the panel — effectively invisible. C-15's own Phase E file is token-clean, making this an inconsistency within one plan. |
| **`dispatchResend`'s cancellation case does not thread `actorStaffId`.** Admin A cancels a booking (correctly skip-self'd, no alert to A), later resends that cancellation's admin-alert row, and now *does* receive a copy of their own alert. `src/app/admin/emails/actions.ts:310-315`. | C-08 Phase D 2026-07-31 | **The Phase D verifier and the closeout reviewer reached opposite verdicts** — the verifier judged it semantically right (a resend has a different actor, and the original canceller's identity is never recorded on the delivery row), the reviewer judged it an inconsistency. **Left as-is deliberately: a design question, not a defect.** "Fixing" it means inventing provenance that was never stored. |
| **Resend success toast can overstate delivery.** If the recipient named on the original delivery row has since opted out of that alert type, the resend's newest-row lookup finds nothing, yet the toast still reads "Resent to {email}". `src/app/admin/emails/components/ResendButton.tsx:45`. | C-08 closeout review 2026-07-31 | Narrow — needs an opt-in change between the original send and the resend. No delivery is lost; only the confirmation copy is wrong. |
| **Double `resolveTemplateOverrides` fetch on six templates** — the HTML renderer resolves internally while the plain-text leg resolves the same id again in `notifications.ts`. | C-08 closeout review 2026-07-31 | Inherited verbatim from C-01's `sendReviewRequestEmail`, not introduced by C-08; the eight Phase-B legacy senders use the cleaner resolve-once shape. **Same sync/async split that threatens C-15's live draft preview** — see `C-15-email-template-studio-progress.md` §0.1. Fix belongs to C-15. |
| **Stale admin-facing `trigger` copy in four `templates-data.ts` entries** — `claim` still says Phase D "will" reroute it (it has); the three `admin_*` entries still say "sent to the owner" though they are now multi-recipient. | C-08 closeout review 2026-07-31 | Cosmetic; visible only to Owner/Admin editing templates. **C-15** owns the registry. |
| **Resending a `booking_confirmation` also re-sends the admin notification.** `sendBookingCreatedEmails` fires both legs, and `sendBookingCancellationEmails` likewise re-notifies all currently-assigned staff — so an Owner resending a confirmation "because the customer never got it" also drops a fresh *New booking request* into the admin inbox. The confirm-modal copy ("will be sent to {recipientEmail}") does not mention it. **Fix options: per-leg dispatch, or modal copy that names every recipient.** Needs a plan that owns `notifications.ts` send-fn signatures. | C-08 Phase C 2026-07-31 | **This is byte-for-byte the dispatch target plan C-08 Step 8's own sketch specifies** — not a regression. Cosmetic/UX, no data or delivery risk; the duplicate goes to the clinic's own inbox, never a customer. |
| **`EMAIL_EVENT_TYPES` in `src/app/admin/emails/format.ts` is stale** — the Delivery-tab filter dropdown still lists only the original 9 event types, never updated for C-08 Phase A's 4 new ones, `review_request_client` (C-01) or `booking_restored_client` (C-04a). | C-08 Phase C 2026-07-31 | Filter-strip completeness only; the rows themselves render fine. Natural fit for **C-15** (which reworks this surface) or C-16's filter pass. |
| **Plan C-08 §3.3's RBAC expectation is wrong** — it says Coordinator/Therapist should see no Resend button; production grants `resend_booking_emails` to both, and `emails/page.tsx:158-175` already implements the intended "H11 middle path" scoping. **Must not be "corrected" into the code.** | C-08 pre-flight 2026-07-31 | Documented so a future implementer doesn't break intentional prior design to match stale plan text. |
| Direct-POST-only state transitions unguarded (`cancelled→confirmed`, `cancelled→no_show`, `X→X` — 5 of 7 informally-implied sites) | C-04a note → C-05 (out of C-05's actual scope) | No live UI path reaches these; hand-crafted POST only |
| `updateBookingManagement`/`quickUpdateBooking`/`respondToCustomerReschedule` don't consult `bookings.deleted_at` | C-06 §8 → C-05 (narrowed to 2 of 11 sites) | Unreachable — C-06's cascade already forces soft-deleted bookings to `cancelled`/`completed`, both inert via existing status checks |
| Two duplicate `MISSING_COLUMN_CODES`/`hasErrorCode` shims (C-06's + C-04a's), both dead | Drift checkpoint #1 | Inert pre-migration guards; share+retire when C-02/C-08/C-14 next need the shape |
| `staff_permission_overrides` upsert lacks `service_role` UPDATE grant, breaks per-staff permission saves (0 rows ever written) | C-04a §0f | Owner explicitly scoped the C-04a grant fix to `email_delivery_events` only; this table's fix deferred |
| `resend_booking_emails` permission-row existence unverified | Migration ledger | Read-only spot-check scheduled at C-08's own pre-flight |
| **🔴 The "Send confirmation email to client" checkbox is silently ignored on the RECURRING submit path.** `recurringSchema` (`src/app/admin/bookings/recurring-actions.ts`) carries `consent_acknowledged` but **no `send_confirmation_email` field at all**, and `sendRecurringSeriesCreatedEmail` is called **unconditionally** at `:197` (only `.catch()`-guarded). The single-booking path honours the checkbox correctly (`admin/bookings/actions.ts:1689`). So an admin who unticks the box and creates a **repeat series** still sends the customer an email. | **Drift checkpoint #4, 2026-08-09** — independently re-confirmed by the orchestrator against the schema and the call site | **Latent today** (no live series exists) but it **ships on the deploy**. Two reasons it matters more than its size: the two admin submit paths disagree about the same visible control, which is this programme's recurring defect class — *the interface asserting something untrue about what happens beneath it*; and it undermines the exact technique C-23 relied on to guarantee "zero emails" during its Zone-2 baseline. **C-23's zero-email claim is unaffected and still holds** — that baseline used the single-booking path (the Repeat-visits control was disabled: "Repeat visits need an existing client"), and `email_delivery_events` was independently re-verified at 0. **No remaining plan owns this file** (C-20, C-14 and C-10 all touch elsewhere), so under protocol rule 6a it was logged, not fixed. **✅ FIXED 2026-08-09 under Owner decision #6 — commit `72670e1`, independently verified.** `send_confirmation_email` added to `recurringSchema`, parsed exactly as `consent_acknowledged` is, and the send gated on it, mirroring the single-booking path. **The form needed no change** — verification established that `ManualBookingForm.tsx` renders ONE physical `<form>` whose `action` merely swaps between the manual and recurring server actions, and the `send_confirmation_email` hidden input is in the shared fragment, so the field was already being submitted on the recurring path all along; the defect was entirely server-side. Three new specs prove sent-when-ticked, not-sent-when-unticked, and not-sent-when-the-field-is-absent, with the mailer mocked — no test path can send a real email. One disclosed behavioural difference from the single-booking path: recurring has no posted email string to double-check (the client is referenced by `client_id`), so the no-email-address case is handled by `sendRecurringSeriesCreatedEmail`'s own internal throw guard (`notifications.ts:834-837`, verified present), which produces one extra `console.error` where the single-booking path skips silently. |
| **Sentry's `SENSITIVE_KEY_PATTERN` does not key-match `notes` / `participantNotes` / gender fields.** `src/lib/observability/sentry-scrubbing.ts:4` matches `admin.*note` and `customer.*note` but not a bare `notes` or `participantNotes` key, so those fall through to `redactText`, which only strips structured patterns (email, UK postcode, phone, long tokens) from string content — never prose. Free-text health notes describing a condition would pass through unredacted. | C-19 closeout, truthfulness + adversarial dimensions 2026-08-09 (both confirmed independently) | **Latent, not firing.** All 7 `Sentry.capture*` call sites were enumerated and none attaches booking `notes`/`participantNotes`/`healthNotes`; `sendDefaultPii: false` also keeps the parsed request body out of the automatic `onRequestError` path. So the new privacy page's claim that "personal information is scrubbed before it reaches Sentry" is **accurate about what actually reaches Sentry today** — which is why the copy was left alone. It becomes false the moment any future capture attaches those fields. Fix is a one-line regex widening plus a test; out of C-19's one-file lock, so logged rather than fixed. Pairs naturally with the open Sentry-console item above. |
| **No `<h1>` on the legal/utility public pages** (`/privacy`, `/cookies`). Their top-level heading is the `<h2>` hardcoded at `src/components/shared/SectionHeading.tsx:60`; only the seven `*Hero` components carry an `<h1>`, and legal pages have no Hero. | C-19 closeout, a11y dimension 2026-08-09 (live DOM query) | **Not a C-19 regression** — `/cookies` has the identical gap via the same shared component, and C-19's plan explicitly asked for the "existing public heading pattern". A real WCAG best-practice gap (one `<h1>` per page) but a **sitewide** one whose fix is in a shared component outside C-19's Owner-locked one-file scope. Everything else in that dimension passed, including measured contrast (body 7.09:1, links 5.18:1), the 65ch measure holding at both 375 and 1280, and zero horizontal overflow. |
| **The privacy page's `<title>` uses an em-dash separator where every other public page uses a pipe.** `src/app/(public)/privacy/page.tsx:21` is `"Privacy Policy — Rahma Therapy"`; `about`, `cookies`, `faqs-aftercare`, `home`, `reviews` and `services` all use `" \| "`. | C-19 closeout, adversarial dimension 2026-08-09 | **Attributable to the plan, not the implementer** — `C-19-privacy-policy-page-plan.md:32` specifies that exact title string verbatim, and the implementer followed it. Left unchanged rather than silently overridden: it is a one-word Owner call on sitewide `<title>` consistency, and changing plan-mandated copy without asking is the deviation this programme guards against. |
| **⚠️ Override list caps and totals count segment ROWS, not distinct DATES — a direct consequence of C-14 Phase C.** `AVAILABILITY_PAST_CAP = 25` / `_VIEW_ALL_CAP = 200` / `_UPCOMING_DEFENSIVE_CAP = 500` (`src/app/admin/availability/availability-data.ts`) and the `STAFF_AVAILABILITY_*` equivalents (`src/app/admin/staff/[staffId]/availability/lib.ts`) were written when a unique constraint guaranteed one row per override date. **C-14 Phase C dropped that constraint**, so a date carrying a break now consumes two or more slots of the cap and of the displayed total — "25 past adjustments" can mean as few as ~12 actual dates. | C-14 Phase C implementer, 2026-08-09; independently confirmed by the orchestrator | **Fully latent today — both override tables hold 0 rows**, so nothing is currently mis-counted. The Owner approved fixing it and an implementer **deliberately halted** rather than force it, for reasons that were checked and stand: (a) those two files hold only numeric constants and a unit-agnostic banner helper — the bug actually lives in query code elsewhere; (b) a true `COUNT(DISTINCT override_date)` is **not obtainable through PostgREST** (no distinct-count option) without either a view/RPC — Zone-2 DDL — or an unbounded row read, which would reintroduce precisely the anti-pattern C-16 was written to eliminate, and the file's own header explicitly promises a *true* past total rather than an approximation; and (c) shipping the totals half alone can make the "view all N" link **silently fail to appear** when older dates genuinely exist beyond the cap — trading a visible overcount for a worse, invisible undercount. **The right fix is a small view or RPC plus matching changes in both override managers, i.e. a scoped follow-up with its own ⛔, not a patch.** The `weekAdjustments` chip on `/admin/availability` was the one piece fixable in place and **was** fixed (`0bc2a02`). |
| **`AvailabilityOverridesManager.tsx` compares a row count against a date-based total** (~line 153): `resolveAvailabilityBannerState({ pastTotal, pastShown: past.length, … })` where `pastDays.length` is already computed on the line above and is the by-date figure. Same shape in `StaffAvailabilityOverridesManager.tsx`. | C-14 Phase C follow-up, 2026-08-09 | One line each, and part of the same cluster as the row above — worth doing **with** that fix rather than before it, since fixing this alone changes which of two inconsistent units the banner uses without making either correct. Latent while both override tables are empty. |
| **The override "past"/"upcoming" pagination queries have no secondary sort by `start_time`.** Both the global (`src/app/admin/availability/page.tsx`) and staff override queries order by `override_date` only. With one row per date that was total ordering; since C-14 Phase C dropped the uniques it is not, so a `.limit()` boundary can in principle split a single date's break segments across the cap and render that date with the wrong hours. | **C-14 adversarial closeout review, 2026-08-09** — a genuinely new finding, not previously named by any phase verification | Same cluster as the two rows above, and same remedy window. **Unreachable today** (0 override rows), and it needs both a multi-segment date *and* enough total dates to hit a cap before it can bite. The one-line half — adding the secondary sort — is safe on its own and could ship with the caps fix; the capacity strip's equivalent ordering gap **was** already fixed in `5e79506`. |

---

*Updated at each drift checkpoint (#1: 2026-07-29; #2: 2026-08-01) and whenever a plan ships with new deferred items. **2026-08-02:** checkpoint #2's remaining unrouted findings (F-3, F-5, F-9) folded in, plus two C-08 notification-resolver gaps from the Owner's independent audit, both re-verified in code before recording. See `redesign/plans/C-phase/DRIFT-CHECKPOINTS.md` for the checkpoint that introduced this file.*

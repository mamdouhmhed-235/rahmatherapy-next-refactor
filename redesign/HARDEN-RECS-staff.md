# Harden recommendations — staff

Generated by `/impeccable harden staff` (Phase 6 / Step 9). Each state from brief §6 Key States and §6 Backend error states is audited against the current implementation in `src/app/admin/staff/page.tsx` and `src/app/admin/staff/NewStaffForm.tsx`.

| State | Status before harden | Recommendation | Action |
|---|---|---|---|
| Empty (admin, no staff) | Covered | None | — |
| Empty (coordinator, no bookable pool) | Covered | None | — |
| Empty (therapist, no same-gender team) | Covered | None | — |
| Filtered-to-empty (any scope) | Covered | None | — |
| Loading skeleton | Not built — Next.js framework default (blank during fetch) | Add `loading.tsx` skeleton (page header + workload-strip + filter strip + 5 row skeletons per brief). **DEFERRED** — adds a new file outside the recipe scope ("Files to edit: page.tsx + NewStaffForm.tsx"); the cold-compile blink is short and acceptable for Phase 6. Note in deferrals; Phase 7 lands the framework-convention file. | Defer (added to staff-deferrals.md) |
| Inactive disclosure (collapsed default, admin) | Covered via native `<details>` | Brief asks for "smooth height transition" — native `<details>` is instant. Polyfill animations adds a JS component out of scope. **DEFERRED.** | Defer (added to staff-deferrals.md earlier) |
| Workload pill ladder (0 / 1–4 / 5–7 / 8+) | Covered (Restricted / Confirmed / Pending / Attention tones) | None | — |
| `roleId` malformed-UUID | Covered (UUID regex + chip "Role: [invalid]" with × dismiss) | None | — |
| Staff list data load failure | NOT covered — error path silently fell through to empty state | Add `staffLoadError` flag; if any Supabase select returns an error, render Cancelled-family `role="alert" aria-live="polite"` region with "Couldn't load the team. Try refreshing." + "Try again" Ghost. Page header + filter strip remain visible per brief. | Applied |
| `workload=zero` / `onboarding=incomplete` returns zero matches | Covered (filtered-to-empty EmptyState with "Clear filters" CTA) | None | — |
| 50-character `name` at 375 | Covered (member name span uses `break-words`, no `truncate` class) | None | — |
| Workload pill renders all 4 tints | Covered via WorkloadPill `tone` computation | None | — |
| Therapist "You" chip without overflow | Covered (chip sits inline with status on H2 row, flex-wrap kicks in if needed) | None | — |
| `AdminAccessDenied` (scope: none) — no raw `view_staff` | Covered (page passes only `title` + `message` per brief; `permission` prop dropped) | None | — |
| `NewStaffForm` form-level errors | Covered (Cancelled-family alert wrapped in `role="alert" aria-live="polite" aria-atomic="true"`) | None | — |
| `NewStaffForm` required `*` markers | Covered (visible `*` in Cancelled text colour, `aria-hidden="true"`) | None | — |
| `NewStaffForm` per-field error region | Covered (each field's error sits inside its own `role="alert"` region with XCircle icon) | None | — |

## Summary

- States already covered: 13 of 15 (87%).
- States added during harden: 1 (staff list data load failure).
- States deferred to Phase 7 via `redesign/per-page-deferrals/staff-deferrals.md`: 2 (loading skeleton, smooth inactive-disclosure transition) — both blocked by scope/platform constraints, not design decisions.

The staff page is production-ready for Phase 6 closure. The one critical state (data load failure) is now wired with the brief-specified Cancelled-family alert + "Try again" Ghost.

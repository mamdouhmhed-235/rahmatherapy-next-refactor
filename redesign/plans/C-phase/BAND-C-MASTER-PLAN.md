# Band C — Audit, Polish & Feature Completeness — Master Plan

**Purpose:** the post-Band-B programme. Where Band B was a focused rebuild of four surfaces (Dashboard, Reports, Performance, Client detail) on top of a foundation primitives + metric-backend base, Band C is a **comprehensive audit of every admin surface as-is**, followed by **targeted fixes for the gaps that audit surfaces**, plus **a fixed slate of 11 user-prioritised feature + UX items** that close known gaps from the post-Band-B review.

**Programme status:** ⏳ scoping — this file. Per-item briefs + plans + the implementation checklist follow once the audit phase completes.

**Working branch:** `master` *(updated 2026-07-26 — `redesign/start-state` merged into master at `ea97932`; single-branch sequential discipline unchanged)*.

**Predecessor:** Band B completed 2026-05-25 at commit `a4c71cf` (B-6) + doc commits `e6ffcd7` + `6072284`. All 7 Band B phases ✅. See `redesign/plans/B-phase/BAND-B-MASTER-CHECKLIST.md` for full closure detail.

**Reference snapshot:** post-Band-B audit findings (this file Part 2) were captured 2026-05-25 from a fresh-eyes Owner Playwright sweep + 3 parallel research agents (CRM competitor landscape, UK compliance, internal code-level audit). Findings are the baseline; this programme acts on them.

---

## Programme intent

Three working phases:

1. **C-A — Audit pass.** Touch every admin surface individually, then in pairs/workflows, then as a cohesive system across every role. Catalogue everything: bugs, gaps, discrepancies, broken flows, design issues, missing affordances, role-vs-role inconsistencies, business-workflow holes. **Output:** per-page + per-workflow + per-role audit files, each with a categorised findings list.
2. **C-B — Plan writing.** For every audit finding + each of the 11 user-prioritised items below, write a brief + plan (Band B format: scope, layout strategy, key states, files, verification). Bundle related findings under a single brief where it reads cleaner; split when scope diverges.
3. **C-C — Implementation.** Execute plans phase-by-phase against a master implementation checklist (see §"Master implementation checklist" stub at the bottom).

**Sequence:** C-A runs first end-to-end (no fixes during audit — pure discovery to keep the baseline clean). C-B writes plans for the union of C-A findings + the 11 user items. C-C ships in order against the checklist with the same per-phase loop Band B used (pre-flight → read → implement → static gates → Playwright sweep → commit + log).

---

## Part 0 — Operating discipline (credentials, tooling, MCPs)

**This section applies to every C-phase plan file, audit file, and progress log. Every future C-A / C-B / C-C file MUST either embed this section verbatim or reference it by `redesign/plans/C-phase/BAND-C-MASTER-PLAN.md#part-0-operating-discipline`.** Pre-flight steps in per-item plans must verify all of the below are in place before any code or audit work.

### Project root + branch

- Working directory: `C:\Users\mamdo\Desktop\rahmatherapy - Copy\rahmatherapy-next-refactor`
- Branch: `master` *(updated 2026-07-26 — post-merge `ea97932`; single-branch sequential — no worktrees)*
- Predecessor commit at programme start: `6072284` (last Band B doc commit, 2026-05-25)

### Dev server

- **Always run on `http://localhost:3000`** via `pnpm dev` in a separate terminal before any Playwright sweep.
- If a port-3000 collision message appears (`Port 3000 is in use by process N, using available port 3001 instead`), **the existing dev server is still running on 3000** — verify with `curl -I http://localhost:3000/admin/login/` returning `HTTP/1.1 200 OK`. Don't kill it; reuse it. (Per HANDOFF §4.1 dev-environment gotcha confirmed end-of-Band-B.)
- The user's own dev server typically already runs on 3000. Don't spawn a duplicate — verify with curl first.

### Login credentials (used across every role-sweep)

These are the canonical Band B credentials. **Every Band C audit + plan + Playwright sweep uses these accounts** — do not invent new ones, do not change passwords, do not create additional test accounts without Zone-2 user confirmation.

| Role | Email | Password | Notes |
|---|---|---|---|
| Owner | `rahmatherapy@outlook.com` | `Password123` | Main owner account. Profile id `01582c5d-bd75-4c49-b207-6f5597e15218`. Per scope clarification 1 below: **Owner takes bookings too.** |
| Admin | `test.admin@rahmatherapy.example.test` | `AdminTest123!` | Practice-Manager scope. |
| Coordinator | `test.coordinator@rahmatherapy.example.test` | `CoordinatorTest123!` | Booking-Coordinator scope. |
| Therapist | `test.therapist@rahmatherapy.example.test` | `TherapistTest123!` | Has assignments + history. Profile id `884311b1-e9d0-44b9-91f3-14188a3baf59`. |
| Therapist (fresh) | `test.therapist.fresh@rahmatherapy.example.test` | `TherapistFresh123!` | Active Therapist with zero assignments / bookings / claimable. Empty-state verification account. Profile id `87e01c11-9d0d-4b52-bf3e-2af16f0f03d5`. |
| Inactive | `test.inactive@rahmatherapy.example.test` | `InactiveTest123!` | Blocked at middleware. Profile id `58784433-cb42-4773-9b22-b792c24b852d`. |

### MCP usage

Both MCPs are available and should be used **where relevant** to the audit / plan / implementation task. Pick the right MCP for the job; don't reach for them unnecessarily.

**`mcp__supabase__*`** (project id `twzutkfgqclqurvkmvqz`, production):
- `mcp__supabase__execute_sql` — read-only queries, schema introspection, DB-state checks during pre-flight + verification.
- `mcp__supabase__apply_migration` — **Zone-2 only.** Requires explicit user confirmation per migration. Band C will need migrations for items like C-02 (recurring bookings — likely `bookings.recurrence_rule` column + companion table) and C-08 (additional `email_template` rows).
- `mcp__supabase__list_tables` / `list_extensions` / `get_advisors` / `get_logs` — diagnostic; safe to use freely during audit.
- `mcp__supabase__generate_typescript_types` — useful after schema changes.

**`mcp__playwright__*`** — canonical browser harness for the audit + post-implementation sweeps. Run against `http://localhost:3000` (the user's own dev server). NOT `preview_start` — that's a different harness the project prompt explicitly de-prioritises.

**Playwright sign-in pattern** (the form schema rejects the standard `browser_fill_form` shape; use `browser_evaluate` to set inputs natively):

```js
() => {
  const ev = (el, val) => {
    const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set;
    setter.call(el, val);
    el.dispatchEvent(new Event('input', { bubbles: true }));
  };
  ev(document.querySelector('input[type="email"]'), '<email>');
  ev(document.querySelector('input[type="password"]'), '<password>');
  document.querySelector('button[type="submit"]').click();
}
```

Wait 2–3 seconds after the click for the navigation to settle.

**Playwright sign-out:** POST `/admin/signout`. Use `mcp__playwright__browser_evaluate` with `fetch('/admin/signout', { method: 'POST', credentials: 'include' })`.

**Alternative MCPs available but rarely needed:**
- `mcp__chrome-devtools__*` — alternative browser drive. Use Playwright unless a specific Chrome-only API (performance trace, lighthouse) is needed.

### Hard rules carried forward from Band B

These were programme-wide discipline in Band B and continue to apply throughout Band C. **Per-item plans must list any rule they're at risk of violating in their pre-flight section.**

- **No `pnpm install` / `pnpm add` / `npx <pkg>` / `npm i`** without Zone-2 user confirmation. Band B shipped 0 new deps across 7 phases; Band C should hold the same line unless a plan explicitly justifies a new dep.
- **Stage files explicitly** for every commit — `git add <path>`, never `git add .` / `git add -A`.
- **No `border-l-4` anywhere** (DESIGN.md ban).
- **Honour `prefers-reduced-motion`** in any animated component (use `src/app/admin/components/use-reduced-motion.ts`).
- **`updateTag(tag)` not `revalidateTag(tag, profile)`** for server-action cache invalidation (Next 16).
- **`createSupabaseAdminClient()` after `getStaffProfile()` auth check** — the RBAC pattern for every server action.
- **RECON §5 untouchables**: `reporting.ts` core exports (additive only), `dashboard-helpers.ts`, RBAC matrix, middleware, build configs, B-1 chart + tile primitives in `src/app/admin/components/charts|tiles/**` (import only).
- **SHARED-NOTES §15 (cache hazards)**: never put `Set<>` / `Map<>` / `Date` through `unstable_cache`.
- **SHARED-NOTES §17 (chart fills vs text tokens)**: use `statusChartFillForKey` from `src/app/admin/reports/ReportsCharts.tsx` for chart fills, not `theme.statusFillForName`.
- **SHARED-NOTES §18 (filter-vs-data discipline)**: run the 5-step audit checklist before merging any new filter-equipped surface.
- **Mobile-first.** Every UI change must read cleanly at 375 px. The B-5 mobile-truncation bug is the precedent — don't repeat it.
- **No unbounded list queries (standing rule, 2026-07-16 — C-16; relocated here 2026-08-04).** Every surface rendering a list backed by a growing table ships with either the shared pager (`PaginationBar` + `src/lib/pagination.ts`, live since C-16) or an **explicit cap + "view all" path — decided consciously, never defaulted.** Every plan's verification gate includes a bounded-lists check for list surfaces it adds or touches. *Full statement in Part 6; duplicated here because the plan and brief both cite it as a "Part 0 rule" and C-16's closeout found it was only in Part 6, where a plan following Part 0 would never see it.* **Two C-16 corollaries, learned the hard way:** a cap is only honest if the surface also surfaces the **true total** and names the cap **actually in force**; and a "view all" affordance must never become a dead link once the total exceeds the view-all cap — evaluate a `cappedOut` state **before** a `hidden` state. That bug shipped three times in C-16 on three different surfaces.
- **Knowingly out of scope for the rule above:** `reporting.ts`'s unfiltered reads (Owner decision 2026-08-03 — it is a Part 0 untouchable and remains unbounded), and `audit/queries.ts` (the cursor reference implementation). Anyone citing the standing rule as "all admin lists are bounded" is overstating it.

### Static + verification gates (every implementation phase in C-C)

Mirrors Band B. Per-item plans must run these before commit:

1. `pnpm lint` — no NEW errors vs the 59-error baseline *(updated 2026-07-26: 55 pre-existing from untracked `design_handoff_area_pages/prototype/*.jsx` + 4 in `src/features/booking/` — see `BAND-C-REFINEMENT-2026-07-26.md` convention #7)*.
2. `npx tsc --noEmit` — 0 errors.
3. `pnpm vitest run` — new specs pass; **6 pre-existing baseline failures preserved** per HANDOFF §4.5 (`createBookingTransaction` × 1, `admin-access` × 2, `ManualBookingForm` × 3). Band C start baseline: 485 / 491 passing.
4. `pnpm build` — clean.
5. `node scripts/measure-admin-bundles.mjs` — bundle delta within budget per SHARED-NOTES §5 (Band B cumulative deltas are the floor; Band C plans must specify their own delta budget).
6. Playwright role sweep per the plan's verification gate (see Band B master checklist's reusable sweep recipe — steps 6 cache-hit + 7 mutation-flow are mandatory).
7. Screenshot evidence at 375 / 768 / 1280 / 1440 for surfaces with meaningful mobile reflow; 1280 for the rest.

### Site URL

Dev: http://localhost:3000

---

## Scope clarifications (read before any audit / plan work)

1. **Owner takes bookings too.** The Personal Contribution Stripe on `/admin/dashboard` showing all-zeros for the Owner account in the baseline sweep is **correct behaviour for an Owner who happens to have zero bookings in the active period**, not a non-treating-Owner false-state. Any UX redesign that assumes "Owner doesn't take bookings" is wrong. Sub-line "Across N visits" semantics still apply.
2. **All four user roles** must be considered as bookable practitioners where the workflow makes sense: Owner, Admin, Coordinator, Therapist. Clients always see whoever is assigned as their "therapist" regardless of underlying role.
3. **No Zone-2 ops during C-A** (no migrations, no auth.users writes, no Resend sends). Pure read-only audit work. Zone-2 returns case-by-case in C-C as plans require it.
4. **Single-branch sequential.** No worktrees this programme either (HANDOFF §4.1).
5. **Programme target audience.** A Luton-based UK clinic running massage + hijama with one Owner + a handful of self-employed therapists. UK-GDPR + ICO-aware. Cupping/hijama-specific compliance (sterilisation logs, single-use blade traceability, Luton BC skin-piercing licensing) is **out of Band C scope** — those land in a separate Tier-A compliance programme once the operational status of those licences is confirmed with the user.

---

## Part 1 — Baseline: production-readiness of current admin surfaces

Captured 2026-05-25 from the post-Band-B audit. This is the **as-is** state before any Band C work begins. Use as the starting reference for C-A.

| Surface | Verdict | Notes |
|---|---|---|
| `/admin/dashboard` (all variants) | ✅ READY | Polished after B-5 + 4 follow-up fix waves. Real UX observations captured below in Part 3 — three redundant urgency representations, no quick-add CTA, Personal Stripe reads zeros for Owner without bookings (correct per scope clarification 1). |
| `/admin/reports` | ✅ READY | Insight dismissal lacks optimistic UI (small). +2.65 kB over budget (within SHARED-NOTES §5 tolerance). |
| `/admin/bookings` (list) | ✅ READY | Comprehensive — attention/today/upcoming/claimable tabs, filter strip, status pills. Pre-existing `caret-color:transparent` hydration warning on autofilled filter inputs (HANDOFF §1.10). |
| `/admin/bookings/[bookingId]` | ✅ EXCEPTIONAL | The audit agent called it "exceptionally thorough" — reschedule accept/decline, next-action strip, email timeline, audit log. |
| `/admin/clients/[clientId]` | ✅ READY | B-6 ribbon just shipped; underlying page is comprehensive (1528 LOC). |
| `/admin/calendar` | ✅ READY | 1942 LOC — full-featured. |
| `/admin/staff/[staffId]` | ✅ READY | 1115 LOC, profile completeness checker, performance scorecard. |
| `/admin/operations` | ✅ READY | Filtering, presets, severity tiers. |
| `/admin/emails` | ✅ READY (per audit) — but item 8 below contests this | 985 LOC delivery log. **Item 8 marks this as incomplete; needs deeper audit before status holds.** |
| `/admin/services` | ✅ READY | Catalog with in-use guards. |
| `/admin/me` + `/admin/staff/[staffId]/performance` | ✅ READY (Band B) — but item 9 contests | **Recent Activity timeline grows unboundedly on `/admin/me` for Owner — pagination missing per item 9.** Status drops to ⚠️ pending pagination plan. |
| `/admin/bookings/new` | ⚠️ PARTIAL | Page is 102 LOC; the actual form is in `ManualBookingForm`. Form-submission UX, error handling, success redirect not verified end-to-end. Headline revenue mutation. |
| `/admin/clients/new` | ⚠️ STUB-ish | 54 LOC wrapper; form is in a subcomponent. Same verification needed: validation, duplicate-client check, success toast. |
| `/admin/enquiries` | ⚠️ PARTIAL | List + tabs + sort visible. **No verified "convert enquiry → booking" one-click flow** — item 3 below. |
| `/admin/staff/[staffId]/availability` | ⚠️ PARTIAL | 299 LOC — rules UI exists. Verify add/edit/delete and blackout date picker work end-to-end. |
| `/admin/availability` (global) | ⚠️ PARTIAL | 635 LOC. Verify the global form persists, shows affected-staff count, and warns on unsaved changes. |
| `/admin/settings` | ⚠️ PARTIAL | Form wrapping visible; verify validation + audit-log integration + concurrent-edit handling. |
| `/admin/roles` + `/admin/roles/[roleId]` | ⚠️ PARTIAL | 406 + 462 LOC. Verify create/edit/delete + cascade behaviour when archiving a role with assigned staff. |
| `/admin/privacy` | ⚠️ PARTIAL | **Updated 2026-05-25 via #22 audit.** Surface IS implemented (838 LOC). Triage works. But FULFILMENT is missing — see audit `22-privacy-audit.md` for P0 GDPR gaps: no SAR export, no cascade delete on completion, no 30-day SLA timer, no ICO breach workflow. The "Completed" button is a UI lie. |

---

## Part 2 — Band C scope (11 user-prioritised items)

These are the user-specified additions and fixes that Band C must address. Per-item briefs + plans get written in C-B, then implementation lands in C-C against the master checklist. **Preserved in the user's voice** as the source of truth for intent.

> **Updated 2026-05-26 — all 11 user-direction decisions locked.** See `redesign/plans/C-phase/C-B-DECISIONS.md` for full reasoning per question + per-plan final scope. The per-item Status lines below reflect the post-decisions verdict (scope + adjacent fold-ins). The original intent paragraphs are preserved in the user's voice.

### C-01 — Google Business review email after appointment completion

Send a Google Business review email to clients **2 hours after their appointment is marked completed**. Includes:
- A new email template (admin can review + edit pre-send).
- **Service-aware pre-filled review text** — depending on what service the client had, the Google review link should carry a ready-made message that's relevant to that service. Makes it easier for the client to leave a review and makes the resulting reviews more service-specific (and therefore more effective for local-search SEO).
- The clinic has a Google Business profile. The implementer should ask for the Google review link + any other relevant assets before writing the plan.

**Status (post-decisions 2026-05-26):** scope locked — URL `https://g.page/r/Ccfwk27JycKDEBM/review`. 10 variants (5 massage + 5 cupping) editable via existing `email_template_overrides` infrastructure (no new schema). 3 picked randomly per email, `{city}` injected from `clients.city`. Trigger `bookings.status='completed'` + 2h via pg_cron. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q4 + §3 C-01.

### C-02 — Recurring / standing bookings

Where relevant, admin users with the appropriate roles can set a booking as **recurring** for different time periods (weekly, monthly, etc.). Because hijama clients often book on longer cadences (the lunar / Sunnah cycle pattern), the recurrence options should be **comprehensive** — not just "weekly / monthly" but custom intervals and end-conditions too. This should be an **option where relevant** (some services + clients don't suit recurring bookings).

**Implementer to do before writing the plan:**
- Review the codebase end-to-end for the existing booking flow.
- Ask the user questions about: which services should support recurrence, which roles should be allowed to set it, what cadence options to offer (weekly / fortnightly / monthly / custom / lunar-cycle), end-conditions (forever / N occurrences / until date), what happens when a single occurrence is cancelled, whether reschedules cascade.
- Make suggestions on the right shape.
- Confirm direction with the user before writing the implementation plan.

**Status (post-decisions 2026-05-26):** scope locked — weekly/fortnightly/monthly cadences only (no Hijri/Sunnah, dropped); all services on by default; Owner+Admin+Coord set; Therapist cannot; per-occurrence cancel/reschedule default; therapist binding locked with "Open to any therapist" toggle; hybrid 12-week generation; `recurring_booking_templates` table + RPC + cron; single plan. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q3 + §3 C-02.

### C-03 — Enquiry → Booking one-click conversion

Add a one-click flow from an enquiry to a booking. Currently the conversion is manual re-entry in the new-booking form. The best way possible + easy.

**Status (post-decisions 2026-05-26):** narrow scope confirmed (~half-day fix). Service fuzzy-match + cross-page bug bundle from W01. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §3 C-03.

### C-04 — Cancellation restore / undo

A cancelled booking can be **restored** (un-cancelled) instead of having to recreate it from scratch. Pairs with C-05.

**Status (post-decisions 2026-05-26):** **split — C-04a only, C-04b DROPPED.** Payment is in-person, no in-app refund tracking. C-04a scope: Restore button + restore email + state-machine guard + no-show quick action + assigned-practitioner auto-promote + hygiene tail (remove dead refunded/waived filters + 1-char `\|\|`→`??` reports fix). **Must ship before/with C-05** (Restore is load-bearing). ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q8 + §3 C-04a.

### C-05 — Bug: cancelled bookings can't be assigned or claimed

Fix the issue where bookings that have been cancelled cannot be assigned or claimed unless they get restored first. (Today they're in a dead state.) Pairs with C-04.

**Status (post-decisions 2026-05-26):** vantage locked — **lock down** (cancelled/no_show inert for all roles). `ensureBookingActive` helper at 7 edit points (6 from W05 §10 + B-171 past-dated). Restore-first via C-04a. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q1 + §3 C-05.

### C-06 — Delete + bulk delete where relevant

Add delete and bulk-delete options where appropriate — clients, bookings, and whatever else is relevant. **Done in the best way possible suitable for the website as it is now, without causing other issues.** Implementer must consider:
- What stays soft-delete vs hard-delete (audit trail vs storage).
- Cascade behaviour (deleting a client with bookings; deleting a booking with assignments).
- GDPR right-to-erasure compatibility.
- RBAC — who can delete what.
- Undo window if any.

**Status (post-decisions 2026-05-26):** scope EXPANDED to 11 steps — absorbs B-110/B-131 destructive-overwrite fix + B-34 client edit route (Q6 fold) + privacy GDPR honesty fix (Q2 fold). One `deleteClient` primitive shared by privacy workflow and admin Delete button. No undo window, no booking hard-delete. Largest plan alongside C-02. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q5/Q6/Q2 + §3 C-06.

### C-07 — Proper routing between pages

Improve cross-page navigation. Right now it feels **rudimentary** — too many flows force the user back to a list page and into another detail page, rather than jumping directly. Audit cross-page navigation patterns across the admin and propose a coherent routing model (drill-link patterns, breadcrumb behaviour, back-link semantics, modal-vs-route decisions).

**Status (post-decisions 2026-05-26):** scope grew significantly via C-A.3. Recommend split: C-07a (routing primitives) + C-07b (per-role defaults). Bundle of bugs from C-A.3 (B-108, W01-V-1, B-134, B-140, W05-V-2, W02-V-2, B-139, W08-V-1, W02-E-1, B-154/155/157, B-161, B-167, B-170). ⏳ pending brief + plan. See `C-B-DECISIONS.md` §3 C-07.

### C-08 — More email templates + automated email sends

The emails surface feels incomplete. Audit + expand. Templates + automation for both admin users and clients. Examples called out:
- When a booking is **assigned** to an admin user (or any role-bearing practitioner).
- When a practitioner **claims** a booking.
- When a client is assigned a therapist (or any role-bearing practitioner — clients see them as "therapist" regardless of underlying role per scope clarification 2).
- When a booking is **cancelled** for a client.
- (More to be surfaced during audit.)

Make `/admin/emails` properly production-ready. **Drops the page's verdict from ✅ READY to ⚠️ pending audit.**

**Status (post-decisions 2026-05-26):** scope locked — **5 templates ship in C-08:** assignment, client_assigned_therapist, booking_confirmed_client, staff_unassignment, claim. Plus per-row Resend button on `/admin/emails` delivery log. Drop review_request_client (C-01), booking_restored_client (C-04a), refund_issued (C-04b dropped), booking_completed_client (redundant). Uses existing `email_template_overrides` infrastructure. Single plan. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q7 + §3 C-08.

### C-09 — Pagination + scale-aware design

Add pagination + other design treatments to pages and sections where unbounded lists ruin the visual design or break design principles at scale. **Headline example: the Recent Activity panel on `/admin/me` for the Owner role** — currently a long list that extends down the page indefinitely, ruining the entire visual hierarchy. Audit the whole admin for similar issues and plan fixes.

**Status (post-decisions 2026-05-26):** scope EXPANDED to cache-invalidation theme + filter-query FAKE cleanup. Tag-based pragmatic approach with 7 resource-level tags (clients, bookings, staff, enquiries, settings, audit, emails). Plus ~10 filter-query FAKE markers wired (enquiries, staff list, operations, emails, privacy). Non-filter FAKEs distribute to C-12+. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q9 + §3 C-09.

### C-10 — Bottom-of-page spacing / footer overlap fix

Some pages have the bottom section sitting too close to the footer, so the bottom content can't be seen properly. Fix spacing + alignment across **all pages and variants, both mobile and desktop**, wherever the issue exists.

**Status:** unchanged. Need Playwright 375 audit pass to catalogue, then fix. ⏳ pending audit findings + plan.

### C-11 — Dark mode (default on, toggle to light)

Introduce dark mode as the **default**, with a user-switchable toggle to light mode. Persists per user. Honours `prefers-color-scheme` only as a tertiary fallback (default-on means the explicit default beats system preference unless the user toggles).

**Status (post-decisions 2026-05-26):** scope EXPANDED to dashboard variants + design system. **3 variant files matching existing `DashboardVariant` taxonomy** (Business / Coordinator / Therapist) — NOT 4 (Owner+Admin lumped, no current divergence). Shared building blocks + R05 pattern lifts + `PractitionerTodaySection` integration from C-FIELDWORK + dark mode + motion-reduce pass. Single phased plan. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q11 + §3 C-11.

### C-FIELDWORK-EXPERIENCE — Fieldwork ergonomics (NEW)

Mobile fieldwork experience for any user fulfilling bookings (Owner / Admin / Coord / Therapist — capability-keyed via `staff_profiles.can_take_bookings`, NOT role-keyed). Surfaced from R04 audit findings + user clarification 2026-05-26.

Bundles: booking-detail mobile sidebar reorder (client phone + address prominent), `tel:` deep-link, maps deep-link, mobile-sticky complete action, `PractitionerTodaySection` drop-in component (used by C-11 dashboard variants), time-of-day greeting extraction, pull-to-refresh.

**Status (post-decisions 2026-05-26):** scope locked. Capability-keyed via `isViewerAssignedPractitioner(booking, viewerStaffId, viewerCanTakeBookings)` predicate. Booking detail page renders two ways per viewer (practitioner view vs admin-curator view). Out of scope: offline cache, PWA, native app, push notifications. ⏳ pending brief + plan. See `C-B-DECISIONS.md` §2 Q10 + §3 C-FIELDWORK-EXPERIENCE.

---

## Part 3 — Additional findings carried over from the post-Band-B audit (not yet user-prioritised)

These came out of the 2026-05-25 audit and are not in the 11 above. They live here so they're not lost. The user decides during C-B planning whether to fold any into Band C or defer to a later band.

**From the live Owner Dashboard sweep:**
- Three redundant urgency representations on the Owner Dashboard (Snapshot · Today / Needs your attention / Operations Health) — visually nearly identical, conceptually different. High cognitive load.
- No quick-add CTAs on the dashboard ("Add booking", "Add client", "Add enquiry"). Every CRM in the comparison set puts these in the header.
- Pre-existing `caret-color:transparent` hydration warning on the bookings filter strip (HANDOFF §1.10) — browser-autofill induced; React 19 logs it as a noisy error each page load. Workaround would be `suppressHydrationWarning` on the filter inputs.
- Production DB visibly contains test data — `Audit Test Client 5`, `Phase10 E2E Claim Client`, `Test Therapist Fresh` show up in the bookings list. Either the dev DB is doubling as prod, or the test seed never got cleaned up. Hygiene issue before public launch.

**From the cross-system / operational gaps in the audit:**
- No refund workflow as an atomic action — currently distributed (mark `amount_paid=0` + manual note).
- No bulk-reassignment when a therapist gets sick.
- No conversations/messages surface — client communication happens outside the admin (WhatsApp etc.) so institutional memory is lost.

**From the CRM-competitor comparison (industry table-stakes we don't ship):**
- Online booking widget for the public site (likely exists separately — needs audit).
- Packages / memberships / gift cards sold + redeemed at checkout.
- Deposits + automatic no-show charging (Stripe-backed).
- Therapist commission tracking + payout statements.
- Marketing automation beyond C-08's transactional emails (birthday, re-engagement, post-visit rebook prompt — overlaps C-01).
- Two-way SMS + AI receptionist (longer-term).
- Recurring bookings (overlaps C-02).

**From the UK-compliance research (operational/legal — likely outside Band C, flagged for the user's separate compliance track):**
- Luton BC skin-piercing premises + practitioner registration (mandatory for wet hijama).
- ICO data-controller registration (mandatory for storing health data).
- UK-GDPR Art.9(2)(h) handling for health data + appropriate-policy document on file.
- SAR full-export within 1 month — `/admin/privacy` needs verification this actually works end-to-end.
- 72h breach notification workflow.
- Sharps + clinical-waste consignment-note log.
- Autoclave cycle + monthly spore test log.
- Single-use blade batch traceability per session.
- Therapist credentials register (cert + indemnity + first-aid + IPC + hep-B vaccination expiries).
- HMRC-compliant invoices retained 6 years.
- PECR-compliant marketing consent state per client.

**From the hijama-specific differentiation opportunity:**
- Wet-hijama-specific consent variant.
- Body-map marking with point/dot annotation + comparison view.
- Before/after bruise photo log.
- Hijri-calendar awareness (peak demand on 17/19/21 of lunar month — Sunnah days).
- Religious/cultural fields (gender preference, prayer-time avoidance).
- Multilingual consent (Arabic/Urdu).

---

## Part 4 — C-A audit strategy (how we'll do the audit pass)

The audit runs as three concentric sweeps before any plan-writing or fixing. **No fixes during C-A** — pure discovery to keep the baseline clean.

### C-A.1 — Per-page audit (every admin surface, every role)

For each surface in Part 1's table, do:

1. **Code-level read:** page.tsx + colocated form/component files. Note TODOs / FIXMEs / stub branches / unhandled error paths.
2. **Live visual sweep** via Playwright **per role** (Owner / Admin / Coordinator / Therapist / Therapist-Fresh / Inactive where it applies):
   - 4 viewports for the surfaces that have meaningful mobile design (Dashboard, Bookings list, Client detail, Reports). 1280 for the rest.
   - Empty-state render (no data) + populated render + error-state where reachable.
   - Console: 0 errors, 0 new warnings.
   - Network: no failed requests.
3. **Cross-role parity check:** does the same surface behave consistently for each role's scope? Does the RBAC narrowing happen at data-fetch (correct) or at render (wrong, leaks)?
4. **Findings catalogue:** one Markdown file per surface, e.g. `redesign/audits/C-A/01-dashboard-audit.md`, with sections: Bugs found · Visual issues · Empty/edge states · Cross-role inconsistencies · Cross-viewport issues · Console/network issues · Pre-existing items the audit accepts · Items for plans.

**Surfaces to cover** (numbered for file-naming convention):

| # | Surface | Roles to sweep | Priority |
|---|---|---|---|
| 01 | `/admin/dashboard` | Owner / Admin / Coord / Therapist / Therapist-Fresh | high |
| 02 | `/admin/bookings` (list) | Owner / Admin / Coord / Therapist | high |
| 03 | `/admin/bookings/new` | Owner / Admin / Coord | high (item C-05 bug repro) |
| 04 | `/admin/bookings/[bookingId]` | Owner / Admin / Coord / Therapist | high |
| 05 | `/admin/clients` (list) | Owner / Admin / Coord / Therapist | medium |
| 06 | `/admin/clients/new` | Owner / Admin / Coord | medium |
| 07 | `/admin/clients/[clientId]` | Owner / Admin / Coord / Therapist | medium |
| 08 | `/admin/enquiries` | Owner / Admin / Coord | high (item C-03) |
| 09 | `/admin/calendar` | Owner / Admin / Coord / Therapist | medium |
| 10 | `/admin/staff` (list) | Owner / Admin | medium |
| 11 | `/admin/staff/[staffId]` | Owner / Admin | medium |
| 12 | `/admin/staff/[staffId]/availability` | Owner / Admin | medium |
| 13 | `/admin/staff/[staffId]/performance` | Owner / Admin | medium |
| 14 | `/admin/me` | every role | high (item C-09 repro) |
| 15 | `/admin/availability` (global) | Owner / Admin | medium |
| 16 | `/admin/services` | Owner / Admin | low |
| 17 | `/admin/settings` | Owner / Admin | medium |
| 18 | `/admin/operations` | Owner / Admin / Coord | low |
| 19 | `/admin/emails` | Owner / Admin | high (item C-08) |
| 20 | `/admin/email-templates/preview/[id]` | Owner / Admin | high (item C-08) |
| 21 | `/admin/roles` + `/[roleId]` | Owner / Admin | medium |
| 22 | `/admin/privacy` | Owner / Admin | high (GDPR — unknown status) |
| 23 | `/admin/account-password-requests` | Owner / Admin | low |
| 24 | `/admin/audit` (if linked) | Owner | low |
| 25 | `/admin/reports` | Owner / Admin / Coord / Therapist | medium |

### C-A.2 — Cross-page workflow audit (page-pairs and short flows)

After per-page audit completes, audit the **flows that span multiple pages**. One Markdown file per workflow, e.g. `redesign/audits/C-A/W01-enquiry-to-booking-flow.md`.

**Workflows to cover:**

| # | Workflow | Pages involved | Hits item(s) |
|---|---|---|---|
| W01 | Enquiry → booking conversion | enquiries → bookings/new → bookings/[id] | C-03 |
| W02 | New booking creation end-to-end | dashboard → bookings/new → confirmation → email | C-08 |
| W03 | Booking lifecycle: pending → confirmed → completed → review | bookings/[id] → emails (review send) | C-01, C-08 |
| W04 | Booking cancellation + restore | bookings/[id] cancel → restore | C-04, C-05 |
| W05 | Booking assignment / claim / reassign | bookings/[id] → staff/[id] → emails | C-08 |
| W06 | Client creation + first booking | clients/new → clients/[id] → bookings/new | C-06 (delete pair) |
| W07 | Therapist availability + recurring booking | staff/[id]/availability → bookings/new (recurring) | C-02 |
| W08 | Owner switching scope (own bookings vs team) | dashboard → me → reports?scope=personal | scope clarification 1 |
| W09 | Refund + payment correction | bookings/[id] → audit | (carried from Part 3) |
| W10 | Settings edit + downstream impact | settings → bookings/new (booking window etc.) | |

### C-A.3 — Whole-system synergy audit (one file per role)

Final pass: as each role, **walk the day** end-to-end. Sign in → do the role's primary daily tasks in sequence. Note: friction, dead-ends, missing context handoffs, mode mismatches between surfaces, navigation rudimentaries (item C-07).

| File | Role | Day walked |
|---|---|---|
| `R01-owner-day.md` | Owner | Morning dashboard check → review yesterday → handle today's bookings → respond to enquiries → review reports → handle a refund |
| `R02-admin-day.md` | Admin | Practice-Manager workflow: bookings + staff + reports + emails |
| `R03-coordinator-day.md` | Coordinator | Triage queue: unassigned bookings + enquiry follow-up + active queues |
| `R04-therapist-day.md` | Therapist | Worker app: today's visits + next visit + claim a slot + complete a booking + add a note |
| `R05-therapist-fresh-day.md` | Therapist-Fresh | First-time experience: zero-state ladder, what nudges them into productive work |

---

## Part 5 — C-B plan-writing strategy

Once C-A audit files are written, C-B converts findings into briefs + plans.

**Per-item file layout** (mirrors Band B):

- `redesign/briefs/C-NN-{slug}-brief.md` — design + scope + content + states
- `redesign/plans/C-phase/C-NN-{slug}-plan.md` — execution steps + files + verification gates
- `redesign/per-page-progress/C-NN-{slug}-progress.md` — step-by-step log filled in during C-C

**Numbering convention:**
- `C-01` through `C-11` reserved for the 11 user-prioritised items (Part 2).
- `C-12+` for audit-surfaced items that the user picks up after reviewing C-A findings.
- Letter suffixes for sub-items if an item splits during planning (e.g. `C-08a-email-templates-brief.md` + `C-08b-email-automation-brief.md`).

**Brief ↔ plan ↔ progress file expectations** (same as Band B):
- Brief = "what we're building + why + how it looks". Scope, layout strategy, key states, content requirements, recommended references, open questions.
- Plan = "how we build it safely". Safe implementation order, what can go wrong, verification gate (commands + pass criteria), files touched, undo procedure.
- Progress = "what actually happened". Filled in during C-C — step-by-step COMPLETE lines, deviations from plan, audit findings folded in.

---

## Part 6 — C-C implementation strategy

Once all C-B plans are written:

1. Build the **master implementation checklist** (see stub below) — every plan listed with state ⏳/🔨/✅, commit SHA column, blocked-by column.
2. Execute in dependency order (a plan that adds an `is_recurring` column to `bookings` blocks any plan that consumes it).
3. Same per-phase loop Band B used: pre-flight → read brief + plan → implement → static gates (lint + tsc + vitest + build + bundle delta) → Playwright role sweep → commit + update master checklist + per-phase log + HANDOFF.

**Discipline carried forward from Band B (re-read before C-C starts):**
- `SHARED-IMPLEMENTATION-NOTES.md` §15 (cache hazards), §17 (chart fills vs text tokens), §18 (filter-vs-data discipline). All still apply.
- No `border-l-4` anywhere (DESIGN.md ban).
- Honour `prefers-reduced-motion` in any animated component.
- Server-action RLS pattern: `createSupabaseAdminClient()` after `getStaffProfile()` auth check.
- Cache invalidation in server actions uses `updateTag(tag)` not `revalidateTag(tag, profile)` (Next 16).
- Stage files explicitly — never `git add .` / `git add -A`.
- **No unbounded list queries (standing rule, 2026-07-16 — C-16).** Every surface that renders a list backed by a growing table ships with either the shared pager (`PaginationBar` + pagination helpers, once C-16 lands) or an explicit cap + "view all" path — decided consciously, never defaulted. Every plan's verification gate includes a bounded-lists check for list surfaces it adds or touches.
- **Cookie/tag registry discipline (standing rule, 2026-07-16 — C-18).** Any new third-party tag, cookie, or client-side storage on customer pages ships with a `cookie-registry.ts` entry + a `CONSENT_BANNER_VERSION` bump (re-prompting consent), and loads only through the consent gate. No tag ever bypasses it — the C-18 regulator test (zero pre-consent third-party requests) must stay green forever.

---

## Master implementation checklist (stub — filled in once C-B plans are written)

> **2026-07-26 — Full-corpus refinement pass complete (pre-C-C).** All 23 plans + briefs were reconciled against the post-merge build (`master` @ `ea97932`) and made autonomously executable: verified anchors, post-merge premises, grep-able `⛔ HARD-STOP` blocks on every Zone-2/prod-env/console step, `VERIFY-ALREADY-IMPLEMENTED` wrappers for merge-superseded steps (C-23 Phase A, C-14 Phase D), evidence convention `redesign/evidence/<plan-id>/`, and the D1 deleteClient-recurring-cascade now inside C-06 Step 9 (closing the C-02 cross-plan requirement below). 23 commits `docs(redesign): C-NN refinement` (d7584cb…7ab28d1). **Read `BAND-C-REFINEMENT-2026-07-26.md` + `BAND-C-MIGRATION-LEDGER.md` before starting any C-C implementation** — they carry the Owner-approved decisions (D1–D26), new sequencing constraints (incl. C-06→C-05 HARD, C-23-Phase-B-before-C-14), and the consolidated migration sequence. Per-plan rows below are unchanged history; the refined plan files are authoritative.

| # | Plan file | State | Started | Shipped | Commit | Blocked by | Notes |
|---|---|---|---|---|---|---|---|
| C-A.1 | per-page audit files (25 surfaces) | ✅ | 2026-05-25 | 2026-05-25 | bundle | — | **C-A.1 COMPLETE.** All 25 surfaces audited. #21+#23+#24+#25 batched: roles delete is stubbed (PE-58 guard pattern correct), password-requests notes not persisted (data-loss), audit log has proper cursor pagination (C-09 template alongside operations), reports has zero animate-spin (cleanest a11y posture). |
| C-A.2 | workflow audit files (10 flows W01..W10) | ✅ | 2026-05-25 | 2026-05-25 | `46bfa1e` | C-A.1 | **C-A.2 COMPLETE.** All 10 workflows audited. 50 new bugs (B-104..B-153). HEADLINE findings: B-110+B-131 destructive client overwrite (3 paths) → C-06 architecture in W06§10. B-116 C-01 review email fully greenfield → C-01 architecture in W03§11. C-02 fully greenfield → C-02 architecture in W07§10. B-126 6th C-05 edit point + `ensureBookingActive` helper recommendation in W05§10. B-149 settings save only revalidates /admin/settings → C-09 cache pass in W10§10. See `redesign/audits/C-A/C-A-2-SUMMARY.md` for full handoff. |
| C-A.3 | role-day audit files (5 roles) | ✅ | 2026-05-25 | 2026-05-25 | `ac96fe4` | C-A.1, C-A.2 | **C-A.3 COMPLETE.** R01..R05 audited. 20 new bugs (B-154..B-173). HEADLINE: TherapistDashboard.tsx is the strongest empty-state surface — reference template for C-11. B-158 (R02) Admin role-trust on Privacy "Completed" — regulatory exposure. **C-A COMPLETE** (173 bugs across 25 surfaces + 10 workflows + 5 role-days). See `redesign/audits/C-A/C-A-3-SUMMARY.md`. |
| C-B-DECISIONS | answers to 11 open questions blocking C-B | ✅ | 2026-05-26 | 2026-05-26 | (this commit) | C-A | **All 11 questions locked.** See `redesign/plans/C-phase/C-B-DECISIONS.md`. Tier-A Privacy GDPR deferred (folded into C-06). C-04b DROPPED (no in-app refund tracking). C-FIELDWORK-EXPERIENCE added as new plan. C-11 expanded to dashboard variants + design system. C-06 absorbed B-34 client edit + privacy honesty fix. Recommended C-B plan-writing order in §5. |
| C-B | per-item brief + plan writing | ✅ | 2026-05-26 | 2026-05-26 | (this commit) | C-B-DECISIONS | covers **23 plans** (C-04b dropped, C-PRIVACY not written, C-FIELDWORK added, C-13 + C-14 added post-handoff via user direction, **C-15 + C-16 + C-17 + C-18 + C-19 + C-20 + C-21 + C-22 + C-23 added 2026-07-16 via user direction — plan-refinement phase**). Recommended order: C-06 → C-04a → C-05 → C-01 → C-FIELDWORK → C-11 → C-08 → **C-15** → C-13 → C-02 → C-09 → C-03 → C-07 → **C-16** → C-10, with **C-14 + C-17/C-18 + C-19 + C-20 + C-21 + C-22 + C-23** independent (C-14 Phase D customer-picker, the C-17+C-18 GA/consent pair, C-19's privacy page, C-20's address autocomplete, C-21's canonical-domain fix, C-22's booking-form abuse protection, and C-23's admin availability calendar can each ship first; break phases anytime; **C-18 co-ships with C-17**; **C-21 + C-22 are small and worth shipping early**; **C-23 carries its own engine-port prerequisite**). **Progress: 23/23 ✅ COMPLETE** — original 12 landed 2026-05-26; C-13 + C-14 added same day; C-15 + C-16 + C-17 + C-18 + C-19 added 2026-07-16 (alongside the C-08 business-notifications + C-04a S7 amendments). C-C implementation is now unblocked. |
| C-01 | Google review post-completion email | ✅ | 2026-07-29 | 2026-07-29 | `5164d00` | C-B | **SHIPPED 2026-07-29 — C-C plan #6 of 22.** 4 phases (`b82be65` A migration, `3298238` B renderer, `89f997b`+`588d0b2` C send-fn/registry with one fix round, `5164d00` D cron infra), each independently verified; whole-plan closeout diff review PASS (zero blocking findings, all 18 completed steps present, no scope creep, no cross-phase string drift). Gate: tsc 0 · vitest 5 failed/792 passed, the five inherited by identity · lint 59E/7W same six files · build clean. **⛔ Migration applied and Owner-approved in chat** (`c01_review_email_infrastructure` — additive `completed_at`/`review_email_sent_at` columns + trigger + backfill; Badar's booking confirmed untouched). **§3.2 database E2E proved the full pipeline live** (no Playwright needed — pure SQL + local curl, one more Owner-approved Zone-2 reset/backdate on a safe test fixture): candidates→1→sent→1→audit row→delivery row→idempotent re-run→0 candidates, fixture restored after. Phase C's fix round closed a real bug: the plain-text email leg ignored admin-configured review-variant overrides that the HTML leg correctly respected (same sent multipart email, inconsistent content) — fixed, independently re-verified. **§3.5 (Playwright role sweep) and §3.6 (screenshots) DEFERRED to the Owner**, same pattern as C-06/C-22/C-04a/C-05 — checklist in `redesign/per-page-progress/C-01-review-request-email-progress.md` §4. **⛔ Cloudflare deploy still outstanding — now a three-in-one activation (C-22 rate limiter + C-04a email-queue drain + C-01's `*/15` review-email cron), presented to the Owner separately, not a precondition for this ✅.** — item 1 — scope locked (see C-B-DECISIONS §3 C-01). **Scheduler-engine deviation:** plan switches from C-B-DECISIONS Q4's pg_cron (not installed) to Cloudflare Workers cron (existing project pattern at `worker-entrypoint.ts` + `wrangler.jsonc`). Cadence (15 min) preserved. See brief §1.4 + §9.2 + plan §1 for rationale.
| C-02 | Recurring / standing bookings | ✅ | 2026-08-01 | 2026-08-02 | `5dd174b` | C-B, C-01, C-08 | **SHIPPED 2026-08-02 — C-C plan #12 of 22.** Nine phases, each independently verified: A migration `59ccb27` · B RPC verification `24fbe34` · C actions `cb0de35` · D email `1d2ed98` · E form `66afd6b` · F series view `f473005` · **Fb cancellation email `2d6fed7`** (Owner-approved new scope) · G cron `5c7e2d6` · H surfaces `ce5ad07`. Closeout gate: tsc 0 · lint 59E/7W same six files · vitest **5 failed / 1258 passed (1263)**, the five inherited by identity · build clean. ⛔ Migration `c02_recurring_bookings` applied under Owner approval; applied RPC md5 `2aefd9d4b0b042eafbc5689b1319343f` unchanged since. **The headline save is Phase G: both the plan AND the brief instruct resuming the horizon walk from `horizon_through_date`, which would have shifted every future visit off the client's weekday permanently — the horizon sits exactly 6 days (weekly) / 13 days (fortnightly) past the last real occurrence, since 83 mod 7 = 6 and 83 mod 14 = 13 — with no error raised and the `(client, date, start_time)` duplicate check unable to catch it because the dates genuinely differ. The cron walks from the series anchor instead, and a regression spec asserts `p_first_date === ANCHOR && !== STORED_HORIZON`.** Phase B also disproved the plan's occurrence counts by exhaustive replay over 366 anchors × 3 cadences: first batch is **12 weekly / 6 fortnightly / 3 monthly**, min = max on every anchor — the old "monthly yields 4" was never true. **Four Owner decisions taken in chat:** widened files-touched list (the plan named the wrong file for the Series filter); Phase B Step 5's live integration test deferred to the UI sweep; consent gating added (a 12-visit series was being created with weaker consent than a single visit); and — reversing the plan — **series cancellation now emails the client**, because brief §4.3's modal promised an email that plan Step 7's bare cascade never sent. **Three closeout FAILs, all bookkeeping not code**, resolved and re-verified: a stale "NOT STARTED" banner, a stale code comment, and an incomplete Owner checklist. **Bundle gate recorded as NEVER RUN** — `measure-admin-bundles.mjs` covers none of C-02's routes and no pre-C-02 baseline exists (third plan hit by this). **§3.2/§3.4/§3.5/§3.6 Owner-performed by necessity** (all surfaces behind admin sign-in); three cron paths are post-deploy-only. Cron joins the pending Cloudflare deploy, making it **four-in-one**. Nothing has exercised the feature end-to-end — production holds 0 templates, 0 recurring bookings. Full detail + 7 cross-cutting findings (headline: the review-request cron would ask a standing client for a review every week forever) in `redesign/per-page-progress/C-02-recurring-bookings-progress.md` §4. — *Original planning note:* item 2 — needs DB migration. Weekly/fortnightly/monthly only (no Hijri/Sunnah, dropped). Hybrid 12-week generation. **Brief + plan written 2026-05-26** — `redesign/briefs/C-02-recurring-bookings-brief.md` + `redesign/plans/C-phase/C-02-recurring-bookings-plan.md`. Fully greenfield (W07 confirmed). 9-phase implementation order. Largest plan alongside C-11. **Cross-plan update needed:** C-06's `deleteClient` cascade must cancel active recurring templates for the client (plan §4.2 + §8 documents). Implementation ⏳ pending. |
| C-03 | Enquiry → booking conversion | ✅ | 2026-08-03 | 2026-08-03 |  `8864e46` | C-B | **SHIPPED 2026-08-03 — C-C plan #14 of 22.** Four phases, 5 phase commits + 1 migration: A `ba0ad2b` fuzzy-match helper · B `fe7a151`+`7116f82` re-conversion guard, fuzzy wiring, source-aware redirect, B-107 graceful catch · C `073485c` toast copy, pre-select, draft-key scoping, Cancel routing · D `7886084` Origin panel + conversion toasts. Gate: tsc 0 · lint 59E/7W same six files · vitest **5 failed / 1462 passed (1467)**, the five inherited by identity · build clean. ⛔ Conditional index migration `c03_enquiries_converted_booking_index` (version `20260803053525`, commit `3453c0b`) applied under per-action Owner approval after re-verifying the ledger's premise live — it still held, so the conditional resolved to APPLY. **Phase D's trap was caught BEFORE it was written — the first time in this programme.** Step 12 says to put the enquiry reverse-lookup in `booking-detail-data.ts` "if C-09 extracted one"; C-09 had, as an `unstable_cache` wrap tagged BOOKINGS/CLIENTS/STAFF/AUDIT/EMAILS with **no ENQUIRIES** — so the plan's own conditional, written in May, resolved in August into a silent staleness bug. `TAGS.ENQUIRIES` added, and independently verified to be fired by `createManualBooking` in the same block that sets `converted_booking_id`. **Two plan-vs-reality corrections:** Step 3's fuzzy matrix asserts `"1 hour massage"` → `massage-60`, but the live service is "1-**Hour** Massage Therapy" (hyphenated) so the real answer is `null` — asserted to the verified-actual rather than the plan's claim; and a verifier hand-traced ~18 plausible enquirer phrasings against the five live services, finding **no false positives, only safe misses** (structurally: the 0.75 category tier sits below the 0.8 gate and can never produce a lone winner for this catalogue). **⚠️ Sequencing violation, orchestrator's own:** the migration commit is an *ancestor* of C-09's final commit — plan #14's Zone-2 write landed between two of plan #13's closeout-addendum commits, contrary to §1 rule 1. No functional harm (additive migration, disjoint file sets), recorded in the progress file §1.2. **Bundle gate NEVER RUN** — `measure-admin-bundles.mjs` covers neither `/admin/bookings/new` nor `/admin/bookings/[id]`; third plan hit by this, absolutes captured. §3 manual verification Owner-performed by necessity; the B-106 guard's end-to-end check deferred to the sweep for want of a converted enquiry (production: 3 enquiries, 0 converted). Detail in `redesign/per-page-progress/C-03-enquiry-to-booking-conversion-progress.md`. — *Original planning note:* item 3 — narrow (~half-day) scope confirmed. **Brief + plan written 2026-05-26** — `redesign/briefs/C-03-enquiry-to-booking-conversion-brief.md` + `redesign/plans/C-phase/C-03-enquiry-to-booking-conversion-plan.md`. 8-item bug bundle: service fuzzy-match HEADLINE + B-104 toast copy + B-106 re-conversion guard + B-107 graceful-catch + B-108 reverse-link (via reverse-lookup, no schema change) + W01-E-2 sessionStorage scoped key + W01-V-1 Cancel routing + B-157 return-to-enquiries. 4-phase implementation. Conditional 1-line index migration if missing. Implementation ⏳ pending. |
| C-04a | Cancellation restore + delayed-email infra | ✅ | 2026-07-27 | 2026-07-29 | `ad0c50b` | C-B | **SHIPPED 2026-07-29 — C-C plan #4 of 22.** All 8 phases (A–H) implemented and each independently verified; closeout gate run 2026-07-29: independent §3.1 re-verification PASS (tsc 0 · vitest 5 failed/749 passed/754, the five inherited failures by identity · lint 59E/7W same six files · build clean) + adversarial review of the full `e0d4b19..c40adee` diff found one gap (Change 7 audit-log phrasing incomplete — flagged twice in-flight, never closed), fixed in `ad0c50b` and independently re-verified PASS. Two Zone-2 migrations applied and verified: `20260728073903_c04a_scheduled_emails` and `20260728132043_c04a_grant_update_email_delivery_events` (service_role had no UPDATE on `email_delivery_events`, so all three of C-04a's UPDATEs were silent 42501s). **Owner decision 2026-07-29 (same pattern as C-06/C-22): §3.2 (4-role × 4-viewport Playwright sweep) and §3.3 (screenshots) DEFERRED, not run** — both require admin sign-in, which no agent may perform (password entry prohibited); checklist preserved in `redesign/per-page-progress/C-04a-cancellation-restore-progress.md` §0k with a zero-email-risk fixture (`d8a61721`) for whenever the Owner chooses to run it. **⛔ Cloudflare deploy still outstanding — it applies C-22's RateLimiter DO migration, activates the `* * * * *` cron, AND is the only thing that drains the cancellation-email queue; without it no customer receives a cancellation email.** Presented to the Owner for approval separately from this closeout (deploy is Zone-2, per-action). Verification found 3 blockers post-implementation: the Status form leaving `cancelled` without sweeping the queued email; the Undo offered where S6 guarantees refusal; and the row menu's Cancel + Restore both dead and unclickable (Cancel pre-existing, Restore inherited the pattern in Phase G). All fixed. Also note §3.1's bundle gate measures six routes, none of them the booking pages, so C-04a's bundle budget is not covered by its own gate. — item 4 — **must ship before/with C-05** (load-bearing). **AMENDED 2026-05-26**: scope expanded from 9 → 14 changes. Adds row-level Restore (Change 10) + `quickUpdateBooking` restore case (Change 11) + status-aware row menu (Change 12) + delayed-email infrastructure with `email_delivery_events.scheduled_for` column + `/api/cron/scheduled-emails` route at `* * * * *` (Change 13) + Cancel-with-Undo toast 10s window (Change 14). Plus refinements: past-datetime restore disallow (S6) + Restore modal shows prior cancellation reason (S3) + **(S7 — AMENDED 2026-07-16) 28-day restore window** keyed to the cancellation moment: new `bookings.cancelled_at` column + two-source backfill folded into the migration; guard reads `cancelled_at ?? customer_cancelled_at` (customer cancel path untouched); unknown time = expired fail-closed; completed-reopen exempt; shared `isRestoreWindowExpired` helper in `_helpers.ts`; stamping ripples noted in C-02/C-06 cascades + C-05 notice copy. New Zone-2 migration required (scheduled_for + 4 payload columns + cancelled_at). 8 phases (A–H), 10-commit cadence. Plus original hygiene tail (refunded/waived removal + 1-char reports fix). **Brief + plan amended 2026-05-26 + 2026-07-16** — `redesign/briefs/C-04a-cancellation-restore-brief.md` + `redesign/plans/C-phase/C-04a-cancellation-restore-plan.md`. Implementation ⏳ pending. |
| ~~C-04b~~ | ~~Refund modal~~ | **DROPPED** | — | — | — | — | Payment is in-person, no in-app refund tracking. See C-B-DECISIONS §2 Q8. |
| C-05 | Cancelled-bookings lockdown + filter-correctness + strikethrough | ✅ | 2026-07-29 | 2026-07-29 | `61be354` | C-B, C-04a | **SHIPPED 2026-07-29 — C-C plan #5 of 22.** 4 phases (`a81c322` A, `1688967` B, `a8853c1`+`fa54966` C, `61be354` D), each independently verified; Phase C's verifier caught a real bug (inline "booking inactive" notice silently suppressed for fullScope actors — Owner/Admin/Coordinator — on past-dated bookings, leaving `deriveNextAction`'s date-blind "pick from eligible therapists" copy actively misleading once the panel was hidden) — fixed in `fa54966`, independently re-verified. Closeout gate: independent static re-run PASS (lint 59E/7W identity · tsc 0 · vitest 5 failed/773 passed/778, the five inherited by identity · build clean) + adversarial review of the full `73590d8..HEAD` diff — REVIEW PASS, all 16 steps present, both non-negotiable invariants (claimable stays strict; "Any status" hides cancelled/no_show) confirmed holding, neither of the plan's 2 ⛔ HARD-STOPs triggered (existing production fixtures — `1d503d3b`/`eaafbb1a` — covered every phase's needs), zero scope creep, DO-NOT-TOUCH clean. No migration (pure code). **§3.2 (Playwright role sweep) and §3.4 (screenshots) NOT RUN — Owner-performed by necessity (no agent may authenticate), same standing policy as C-06/C-22/C-04a; checklist in `redesign/per-page-progress/C-05-cancelled-bookings-inert-progress.md` §3.** Headline user-facing fix: the "Cancelled" status filter on `/admin/bookings` previously returned 0 rows unconditionally — now works, with strikethrough visual treatment on inert rows (list + client-history cross-surface). — item 5 — lock down via `ensureBookingActive` helper. **AMENDED 2026-05-26**: scope expanded from 7 → 9 edit points. Adds status-aware view filter (Edit Point 8) — `filterBookings` becomes user-status-aware so the "Cancelled" status dropdown actually returns cancelled rows on non-archive views (per S1b: "Any status" stays active-only; explicit `status=cancelled` surfaces them; claimable stays unconditionally strict). Plus cancelled-row strikethrough rendering (Edit Point 9) on both `/admin/bookings` list rows AND `/admin/clients/[id]` BookingHistoryCard. New phase D + 1 extra commit. Plus cross-page sweep findings doc (S9): client history already correct; reports + calendar intentionally exclude cancelled. Tab-promotion DECLINED (Q9.8) — Cancelled/No-show stays under "More" overflow. **Brief + plan amended 2026-05-26** — `redesign/briefs/C-05-cancelled-bookings-inert-brief.md` + `redesign/plans/C-phase/C-05-cancelled-bookings-inert-plan.md`. Implementation ⏳ pending C-04a. |
| C-06 | Delete + bulk delete + client CRUD hardening + optional admin-booking email | ✅ | 2026-07-27 | 2026-07-27 | `88d2a6d` | C-B | **SHIPPED 2026-07-27 — C-C plan #3 of 22. 18 commits** (`d1630af..88d2a6d`), six phases each independently verified. **⛔ Migration APPLIED** (`20260727202424 / c06_client_crud_hardening`) with Owner approval; applied function byte-identical to the reviewed reconstruction (md5 `9ec3ca132042e3d22463de26fdc7008a`); rollback artefact `redesign/evidence/C-06/create_booking_request-BEFORE.sql` (md5 `b44229fac5da168afb60fbd742565164`). §1.3 backup precondition satisfied via option (b) — Owner explicitly accepted applying without a backup. Gate: lint 59E/7W identity · tsc 0 · vitest **5 failed / 612 passed / 617** · build clean · bundle **≤1.24 kB vs a +15 kB ceiling** · RBAC role sweep PASS on every row of brief §3. **Expected shrinkage CONFIRMED** — `createBookingTransaction` removed from the baseline (verified strengthened, not weakened). **NOT RUN by Owner decision:** §3.2e/f/g (irreversible sensitive-note deletion; consumes fixtures later plans need) + §3.2h/i — all covered by specs. **Nine plan defects were found and corrected before they shipped**, every one silent: the duplicate check would have 409'd every returning public customer; a soft-deleted email would have crashed a public booking with a raw Postgres error; `cancellation_reason` does not exist; `cancelled_at` belongs to C-04a; `deleted_at` was in no `.select()`; a failed cascade left health data intact while reporting success; **`CREATE OR REPLACE` with a changed signature would have left the destructive function live in production**; `client_deleted` was in no audit family; and all five cache invalidations were no-ops. See `redesign/per-page-progress/C-06-client-crud-hardening-progress.md` — **§8 carries the deferred soft-delete read-path inventory and four hand-off items for C-04a.** — *Original planning note:* item 6 — **EXPANDED to 13 steps.** Absorbs B-110/B-131 destructive-overwrite fix + B-34 client edit (Q6) + privacy GDPR honesty fix (Q2). **AMENDED 2026-05-26: + Step 13 — optional email on the admin manual-booking flow.** Migration drops `bookings.contact_email NOT NULL`; the `create_booking_request` RPC (already rewritten in Step 1) gains a null-email/phone-fallback client-match branch (email-first, phone-fallback per user direction); `ManualBookingForm` email field made optional (admin-only); `sendConfirmationEmail` hidden when no email; downstream reminder/review crons gain `contact_email IS NOT NULL` guard; booking detail shows "No email — reminders off" indicator. **Public booking flow (`/api/bookings`) UNCHANGED — keeps email required via its own Zod; regression-tested for isolation.** Largest plan alongside C-02. **Brief + plan amended 2026-05-26** — `redesign/briefs/C-06-client-crud-hardening-brief.md` + `redesign/plans/C-phase/C-06-client-crud-hardening-plan.md`. Implementation ⏳ pending. |
| C-07 | Routing between pages | ✅ | 2026-08-03 | 2026-08-03 | `f038b4f` | C-B | **SHIPPED 2026-08-03 — C-C plan #15 of 22.** All 8 phases shipped and independently verified. Commits `526733f` A1 · `5b07851` A2 · `8f76fc6` A3 · `b8f2f5d` A-fix · `4603340` A4 · `4e23053` B1 · `ef1d4b6` B1-fix · `c6c1ec4` B2 · `dd5b497` B2-fix · `838d049` B3 · **`f038b4f` B4**. Gate: tsc 0 · lint 59E/7W same six files · vitest **5 failed / 1494 passed (1499)**, the five inherited by identity · build clean, 52 pages · adversarial diff review of the full `d3b5c90..HEAD` range PASS · B4 verified at FULL tier. No migration, no Zone-2 action. **⚠️ Bundle gate recorded NOT RUN — fourth plan hit:** `measure-admin-bundles.mjs` carries no `/admin/bookings*` route and its only baseline predates Band B, so the plan's "+5 kB across four surfaces" ceiling is unanswerable with that tool; absolutes captured, escalated to the backlog. **⛔ B4 was a plan-vs-reality contradiction, and the Owner cancelled the plan's own steps.** Steps 14–16 asked for a saved-filters bar (`saved-filters.ts` + `SavedFiltersBar.tsx`); that feature **already shipped** in `BookingsChrome.tsx` at `449f722`, **before Band C began**, and neither file the plan names has ever existed — building it as written would have created a second parallel system, the sixth instance of this plan's defining defect. Owner ruling 2026-08-03: close as VERIFY-ALREADY-IMPLEMENTED plus a two-gap in-place fix. **The gap analysis found a real privacy defect nobody had asked about:** the saved-views localStorage key was **not namespaced per staff member**, so on a shared front-desk browser one Coordinator's saved searches — which can carry a client's name in their `search=` param — persisted into the next person's session, and the brief's own "cleared on logout" requirement was unmet. `f038b4f` namespaces the key per staff id and **deliberately does not migrate** the old global key, because migrating would have performed that exact leak; 9 specs added, the isolation and purge cases verified non-vacuous. Two gaps stay open by Owner decision: the always-visible bar (accepted deviation from the written checkpoint, chosen for discoverability) and no mobile save affordance (→ C-12+). **Five defects caught by verification, none visible to any static gate:** the plan's own Step 3 code switched on `profile.role_name` (an *editable* display label, not a slug) and would have rendered Quick Links empty for three of four roles; A3's 7-day claimable filter ran in memory over an array already bounded to today, so it showed no claimable work when work existed; A2 introduced a **new** double-toast on the ordinary booking path while its commit message claimed the shape was pre-existing; B1's Yesterday chip emitted a range key the server resolver did not know, so "Clear all" silently swapped the dashboard to a ~2-month window; and B2's clear-all dropped the brand-new `scope` param — **the same function that had lost `from`/`to` one phase earlier**, because that fix extended an allow-list instead of changing the approach. Now fixed structurally: start from the current params, delete only the advanced-filter keys. **B3 fixed a bug live in production** — `filterBookings` recomputed its own hardcoded default, so a Therapist saw the "Today" tab highlighted while the list was filtered by "attention"; it survived because every existing spec passes an explicit `view` and none reached the fallback branch. Detail in `redesign/per-page-progress/C-07-routing-and-per-role-defaults-progress.md`. — *Original planning note:* item 7 — recommend split C-07a + C-07b. **Brief + plan written 2026-05-26** — `redesign/briefs/C-07-routing-and-per-role-defaults-brief.md` + `redesign/plans/C-phase/C-07-routing-and-per-role-defaults-plan.md`. Two-phase plan (07a routing primitives + 07b per-role defaults) shipped as one brief + plan. Several items absorbed by C-03 (B-108, W01-V-1, B-157); remaining: B-134, B-140, W05-V-2, W02-V-2, W02-E-1, W08-V-1, B-170, customer manage polish, B-139, B-154, B-155, B-161 (localStorage v1; DB persistence → C-12+), B-167. 8-commit cadence. No migration. No new permissions. |
| C-08 | Email templates + automation expansion | ✅ | 2026-07-31 | 2026-07-31 | `302f90e` | C-B | **SHIPPED 2026-07-31 — C-C plan #9 of 22.** 4 phases, each independently verified, no fix round in any of them: A `80a8bc2`→`e522bdd` (4 templates), B `e018d67`+`40a0202`, C `dc742d0` (resend tooling), D `459b9a9`→`302f90e` (business notifications). Registry 10 → **15** templates. Gate: tsc 0 · lint 59E/7W same six files · vitest **5 failed / 991 passed (996)**, the five inherited by identity · build clean. **Adversarial whole-plan review PASS** — all 18 steps present, all 15 `resolveTemplateOverrides` call sites enumerated and correct, escaping confirmed substitute-then-escape throughout. **THE HEADLINE FINDING WAS NOT IN THE PLAN:** a Phase B audit proved the template editor **did not affect real emails** — `resolveTemplateOverrides` was called in 1 of 9 senders, so an admin edited copy, clicked "Send test", saw the edit applied, and every genuine customer email still rendered the original hardcoded text. Owner approved fixing it as an extended Phase B (5 → 14 call sites). Sequencing was deliberate: a security review of those paths ran **before** the wiring went live, and a stale `booking_confirmation` override (`"Hi big boy {client name}"`, left in place 15 months) was deleted as an Owner-approved Zone-2 action **before** the fix could arm it. **Three Zone-2 actions, each approved in chat and each performed by the orchestrator, never a subagent:** that DELETE; migration `e91c09c` (`c08_tighten_email_delivery_events_rls` — a Therapist's own token could read every rendered email body via PostgREST, since `resend_booking_emails` was in the SELECT policy and `authenticated` holds a table grant); migration `c531ea6` (`c08_notification_email_and_metadata`, remote `20260731192911`, seeding exactly the 2 active Owner rows). **Plan §3.3's RBAC expectation is WRONG and must never be corrected into the code** — production deliberately grants resend to Coordinator/Therapist with scoping already implemented. §3.3/§3.4/§3.5 Owner-performed by necessity (checklists in the progress file §2.6b/§3.3). §3.2's histogram is a post-deploy delta (9 types today → 14 expected). Bundle gate is partial by construction: the script measures six routes and `/admin/emails` is not one of them. Five non-blocking items logged not fixed — see `redesign/per-page-progress/C-08-email-automation-expansion-progress.md` §3.4. — *Original planning note:* item 8 — **REFRAMED to 4 NEW + 1 existing-verify**, then **AMENDED 2026-07-16 back to 5 NEW + business-notification routing.** Decisions doc Q7 said "5 new templates"; code inspection confirmed staff_assignment already exists, and the 2026-07-16 amendment added `enquiry_logged` (owner never hears about logged enquiries — verified: `createEnquiry` sends nothing; no public enquiry endpoint exists). C-08 ships 5 new (booking_confirmed_client + staff_unassignment + claim + client_assigned_therapist + enquiry_logged) + verifies existing staff_assignment + per-row Resend tooling. **New Phase D (2026-07-16):** `staff_profiles.notification_email` + `business_notification_prefs` (per-type alert toggles, Owner/Admin opt-in), `resolveBusinessNotificationRecipients` resolver replacing `getAdminRecipient` for internal alerts (skip-self: never emailed about your own action; zero-opt-in fallback preserved), `/admin/me` Notifications section. **Migration now definite Zone-2** (2 staff_profiles columns + Owner seed + conditional metadata). ~11-commit cadence. **Brief + plan amended 2026-07-16.** Implementation ⏳ pending. |
| C-09 | Cache invalidation + pagination + filter FAKE cleanup | ✅ | 2026-08-02 | 2026-08-03 | `880809e` | C-B, C-06, C-04a, C-05, C-01, C-FIELDWORK, C-11, C-08, C-02 | **SHIPPED 2026-08-03 — C-C plan #13 of 22.** 17 commits, 81 files, +7974/−1704. Five phases: A `b652bea` tag taxonomy · B `e45d0d1`+fix `ef231bb` action sweep · C `c0251b5..b615ba1` (7 commits — 13 cached fetchers + 3 extended wraps) · D `bc752d8`/`8c29721`/`e2271c7`/`46d5706`/`a3291cd`+fix `880809e` filter cleanup · E `cf9a436` C-12+ inventory. Gate: tsc 0 · lint 59E/7W same six files · vitest **5 failed / 1421 passed (1426)**, the five inherited by identity · build clean. Zero migrations, zero Zone-2 actions; does not join the Cloudflare bundle. **This was the retroactive plan drift checkpoint #2 warned about, and parallel verification earned its keep — four defects, none visible to tsc/lint/vitest/build:** (1) `updateOperationalEventStatus` was in neither the plan's Phase B matrix nor its files-touched list, while Phase C was about to cache `/admin/operations` on tags it never invalidated — found only by a lens that enumerated every mutating action independently of the plan; rule-6(b) STOP, Owner approved widening to that one file. (2) `/admin/staff`'s **Bookable filter silently lost its `active` conjunct** in the JS→SQL port, presenting deactivated staff as available for work — invisible to its spec, whose fake Supabase client does not evaluate `.eq()` predicates. (3) `new Date(param).toISOString()` unguarded on two surfaces **turned a harmless no-op into a 500** on any malformed `?from=` URL. (4) `escapeLike`'s backslash-escaping of `,()` is **never honoured by PostgREST outside double quotes** — confirmed from the `postgrest-js` source, the URL-grammar docs and a maintainer answer — so searching "Smith, John" returned silently empty. (2)–(4) were caught only because a **malformed verifier verdict** (its entire summary was the word "test") was re-run rather than trusted. Phase C's `unstable_cache` JSON hazard held: all 16 wraps traced for runtime shape, and every one of the 13 new specs asserts `JSON.parse(JSON.stringify(data))` round-trips equal. `settings-data.ts`'s RLS→service-role swap reviewed and cleared (the RLS policy was identical authority to the `MANAGE_SETTINGS` gate that still runs first; no credential columns). **Plus an Owner-approved addendum** `08bee11` + `2d5bcdb`: the rule-6(b) ask had judged four un-swept action files inert *because Phase C does not wrap their pages* — the wrong test. The right one is whether any cached fetcher **reads the tables those actions write**, and two did (`role_permissions` via `staff-detail-data.ts`; `services` via the bookings filter dropdown). Both now tagged. The addendum itself needed two commits: the first scoped to the finding's literal wording and missed `updateRoleMetadata`, which writes `roles.display_label` — read by `dashboard-data.ts` through a **joined** select and cached on `TAGS.STAFF`. `account-password-requests` and `me` confirmed genuinely inert. §3.3 manual verification Owner-performed by necessity. Full detail in `redesign/per-page-progress/C-09-cache-invalidation-filter-cleanup-progress.md`. — *Original planning note:* item 9 — tag-based pragmatic approach (7 resource-level tags) + filter-query FAKE markers wired. **Brief + plan written 2026-05-26** — `redesign/briefs/C-09-cache-invalidation-filter-cleanup-brief.md` + `redesign/plans/C-phase/C-09-cache-invalidation-filter-cleanup-plan.md`. **Discoveries:** existing `unstable_cache` infrastructure already used at 3 sites (extend, not replace); filter-FAKE actual count is ~16 (decisions doc estimated ~10 — undercount). C-12+ docs deliverable for non-filter FAKEs. Pagination explicitly NOT in C-09 (Q9.5). 5-phase implementation. Implementation ⏳ pending C-06/C-04a/C-05/C-01/C-FIELDWORK/C-11/C-08/C-02 merge so the tag sweep covers all new actions. |
| C-10 | Bottom-spacing / footer overlap | ⏳ (brief + plan ✅) | — | — | — | C-B | item 10 — global polish (unchanged scope). **Brief + plan written 2026-05-26** — `redesign/briefs/C-10-bottom-spacing-footer-overlap-brief.md` + `redesign/plans/C-phase/C-10-bottom-spacing-footer-overlap-plan.md`. Smallest C-B plan by scope. 2-phase discovery + remediation. Pure CSS hygiene; root cause from audit #01 CV-02. No migration. No new permissions. **C-10 is the FINAL C-B plan — its completion marked C-B at 12/12 ✅.** |
| C-11 | Dashboard variants + design system + dark mode | ✅ | 2026-07-30 | 2026-07-31 | `3c54e94` | C-B, C-FIELDWORK | **SHIPPED 2026-07-31 — C-C plan #8 of 22.** 7 phases + migration `c11_theme_preference` (`f95c828`). `page.tsx` 1101→410; 3 variant files + 8 shared blocks + `dashboard-variant-shared.tsx`. V-01 reconciled; **B-01's root cause overturned** — the "()" was a Cormorant legibility illusion at the 24-30px zero-state marquee, not a code bug (proved by canvas raster of the loaded webfont + 14× re-crop of the audit screenshot), fixed by rendering that one state in the sans face. Dark mode ships **wrapper-scoped, not on `<html>`** (Owner decision, overriding plan Steps 12/15) after a verifier found `components/ui/input.tsx` styles every `<Input>` with `--admin-*` tokens and `/booking/manage` — a customer route — renders them bare; portals themed by CSS sibling selectors so leak-on-unmount is impossible by construction. Bundle +0.11 kB JS / +2.65 kB CSS (ceilings +20/+5). All 5 closeout gates PASS, zero blocking findings. **Baseline handed on: vitest 5 failed / 881 passed — the drop from 888 is the Owner-approved deletion of two unwired blocks, not erosion.** §3.2/§3.5–§3.8 Owner-performed by necessity. Deferred to C-12+: the revenue row (brief §4.1 vs §8 are contradictory), ~85 lines of variant duplication (bundled with C-09), the ~90-file colour remainder. Prior scope note — item 11 **EXPANDED.** 3 variant files (Business / Coordinator / Therapist) + shared building blocks + R05 pattern lifts + `PractitionerTodaySection` integration + dark mode + motion-reduce pass. **Dark mode applies admin-wide** (per user clarification 2026-05-26 — every page under `/admin/*` themes uniformly via ThemeProvider in `src/app/admin/layout.tsx` + global CSS variables + hardcoded-color sweep). Single phased plan. **Brief + plan written 2026-05-26** (clarified for admin-wide theme scope same day) — `redesign/briefs/C-11-dashboard-variants-design-system-brief.md` + `redesign/plans/C-phase/C-11-dashboard-variants-design-system-plan.md`. **Discoveries during plan-writing:** dark mode is fully greenfield; CSS variable structure flipped to `:root`=light + `[data-theme="dark"]`=dark so public site stays unchanged. Largest C-B plan alongside C-02. 7-phase implementation order (now ~11 commits). Implementation ⏳ pending C-FIELDWORK ship. |
| C-FIELDWORK | Fieldwork ergonomics (capability-keyed) | ✅ | 2026-07-29 | 2026-07-29 | `6314718` | C-B | **SHIPPED 2026-07-29 — C-C plan #7 of 22.** 4 phases (`5127f62` A shared-helpers+predicate, `29ab66e` B booking-detail dual-view, `a2c19f5` C PractitionerTodaySection, `bd58145`+`6314718` D dashboard mounts with one fix round), each independently verified. Phase D's fix closed a real bug: `TherapistDashboard.tsx`'s refactor hardcoded `claimableCount=0` into the shared component, so a Therapist with claimable work but no today/next appointment saw a contradictory "Nothing scheduled" message next to their own real claimable-work strip — fixed via a new `showClaimableStrip` prop, independently re-verified, and the closeout review specifically confirmed the Business/Coordinator mount can't hit the same bug (it never hardcoded the count). Closeout: whole-plan diff review PASS — all 12 steps present, the one file touched outside the original list (`bookings/page.tsx`'s `getScopedBookingIds` export) confirmed justified by brief §9.4, all 11 of brief §10's acceptance criteria MET. Gate: tsc 0 · vitest 5 failed/826 passed, the five inherited by identity · lint 59E/7W same six files · build clean. No migration (pure code); both plan ⛔ capability-flip blocks confirmed never triggered — the real Owner account and a test-admin account already had `can_take_bookings=true`. **§3.2 (Playwright role sweep) and §3.4 (screenshots) NOT RUN — Owner-performed by necessity, checklist in `redesign/per-page-progress/C-FIELDWORK-EXPERIENCE-progress.md` §4** (no safe pre-existing test fixture is currently assigned to a test staff account — Owner needs to create one via the admin UI first). **Documented trade-off:** TherapistDashboard's dynamic hero eyebrow and "Then next visit" preview were dropped in the refactor (gained: a genuinely new Mark-complete button); flagged for C-11, which consumes `PractitionerTodaySection` next. — NEW from Q10 — `C-FIELDWORK-EXPERIENCE-plan.md`. Capability-keyed via `can_take_bookings` + active assignment. Booking-detail dual-view + `PractitionerTodaySection` (consumed by C-11) + `tel:`/maps/mobile-sticky primitives. **Discovery during plan-writing:** tel: + maps + getGreeting all already exist in codebase — C-FIELDWORK lifts as canonical patterns rather than inventing new primitives. |
| C-13 | Group-booking surface + gender-clarity chips | ✅ | 2026-08-01 | 2026-08-01 | `d7d67c3` | C-B | **SHIPPED 2026-08-01 — C-C plan #11 of 22.** 8 phases (`af273e8` A · `4218bd5` B · `b418aa0` C · `a516bd1` D · `dbe0208` E · `2fdcadc`+`c64e6ad` F · `c9b77c3` G · `d7d67c3` H), each independently verified. Gate: tsc 0 · lint 59E/7W same six files · vitest **5 failed / 1181 passed (1186)**, the five inherited by identity · build clean · email render-parity 13/13 with its fixture still at one commit in its whole history. Zero migrations, zero new permissions, zero new server actions — the one plan claim that held up cleanly. **Two phases caught defects that pre-dated them.** Phase F's verifier found a **live PII leak**: the new `generateMetadata` checked `canOpen` but not `claimableOnly`, so a Therapist merely *eligible to claim* an unassigned slot — the exact population the page body redacts to "Claimable booking" with the client cards hidden — got the **real customer's name server-rendered into the browser tab title**, reachable through the Calendar links Phase E had just added. Fixed deny-before-fetch (`c64e6ad`) so the identity columns are never selected on that path, with a spec that counts select calls. Phase H found the dashboard's gender derivation was **first-match-wins** — a `!map.has()` guard meant a booking needing both a female and a male therapist silently displayed whichever came first; now collapses to "mixed". **Every line number in the plan and brief was stale by ten plans**, and three anchors had moved twice (the dashboard one three times — C-11 split `page.tsx` *after* the plan's own C13-02 correction). The plan also named the wrong email renderer, and Step 12's "extend the query" was impossible without violating rule 7. **Owner decisions:** the brief's practitioner-facing claim email doesn't exist, so group context went to the admin-internal `claim` template; group-booking fixtures deferred (no booking anywhere in the DB has >1 participant, so **every group path is proven only by synthetic unit tests** — Owner checklist in the backlog). **The closeout review FAILed on the record, not the code** — a stale status banner and an unrecorded build/bundle gate, both corrected; bundle ceilings measured but undeltable, same blind spot C-08/C-04a disclosed. Four deferred items in `redesign/per-page-progress/C-13-group-bookings-and-gender-clarity-progress.md` §4.3–4.4. — *Original planning note:* **NEW 2026-05-26 (post-handoff amendment)** from user direction — `C-13-group-bookings-and-gender-clarity-plan.md`. 7 changes / 8 phases (A–H). Replaces generic `"Same-gender required"` chip with per-participant gender phrasing (`"Needs 2 female + 1 male"`). Group cards become first-class with nested per-participant sub-rows on `/admin/bookings` list (option c). Composite identity (`"Aisha Khan + 2 others"`) on row headlines, detail page, browser title, calendar tooltips. Per-participant assignment progress (`"1 of 3 therapists assigned"`). Calendar tile gets Users icon + Group chip. Email templates (`staff_assignment`, `staff_claim`, `booking_confirmed_client`) gain conditional group-context block. `BookingCard` extracted as shared component (consumed by C-11 + C-FIELDWORK). **Zero migrations, zero new permissions, zero new server actions** — pure UI render work. **Brief + plan written 2026-05-26** — `redesign/briefs/C-13-group-bookings-and-gender-clarity-brief.md` + `redesign/plans/C-phase/C-13-group-bookings-and-gender-clarity-plan.md`. **Sequencing:** ship after C-FIELDWORK + after C-08 (templates exist before extending) + before C-11 (shared blocks adopts BookingCard). Implementation ⏳ pending. |
| C-14 | Granular working hours (breaks) + customer booking-window date guard | ⏳ (brief + plan ✅) | — | — | — | C-B | **NEW 2026-05-26 (post-handoff amendment)** from user direction — `C-14-granular-working-hours-breaks-plan.md`. 4 phases. Working hours gain breaks (opens / break(s) / closes) across global recurring (`availability_rules`), per-staff recurring (`staff_availability_rules`), and per-date overrides (`availability_overrides` + `staff_availability_overrides`). **Segments storage model — no new tables**; a break is the gap between bookable segment rows. Verified: recurring tables have no day-uniqueness (multiple rows already allowed) + slot engine already multi-window → **zero schema + zero engine change for recurring phases**. Phase C (overrides) is the only migration: drop `availability_overrides_override_date_key` unique (+ verify staff equivalent) + widen the global override fetch from `.maybeSingle()` to array. Phase D (independent, customer-facing): `DatePickerField` disables dates beyond `booking_window_days` + before the `minimum_notice_hours` floor, sharing window math with the server's `isDateInBusinessWindow` (new `getBookingDateBounds` helper). Breaks hidden from customers (no slots across a break). Admin picker stays unbounded. **Brief + plan written 2026-05-26** — `redesign/briefs/C-14-granular-working-hours-breaks-brief.md` + `redesign/plans/C-phase/C-14-granular-working-hours-breaks-plan.md`. **Sequencing:** independent; Phase D can ship first as a quick customer win. Implementation ⏳ pending. |
| C-15 | Email template studio | ✅ | 2026-07-31 | 2026-08-01 | `8851e8c` | C-B, C-08 | **SHIPPED 2026-08-01 — C-C plan #10 of 22.** 6 phases (`0d0a26d` A · `3ab469b` empty-string fix · `b84dd11` B · `caec3f1` C · `84f10fc` D · `ee37aa3` E · `10ca7db` F), each independently verified, plus **two closeout fix rounds** (`289bdcb`, `8851e8c`), each re-verified. Gate: tsc 0 · lint 59E/7W same six files · vitest **5 failed / 1107 passed (1112)**, the five inherited by identity · build clean. Zero migrations, zero Zone-2 actions. Registry 15 → **16** templates (adds `booking_restored_client`, C-04a's unregistered gap). **The adversarial closeout review returned REVIEW FAIL** on brief §10 AC2: subjects were editable and round-tripped through save/preview/reset, but `git diff` on `notifications.ts` across the whole plan was **empty** — every real `Subject:` was still a hardcoded literal, so customers would have kept receiving the old ones forever. Phase A had built the safety (100-char cap + C0 control-char rejection at save *and* render) precisely to allow this; the wiring was simply never assigned to any phase. **The fix round then found the trap:** Phase A had lifted `subjectDefault` from the `SUBJECTS` map, which only the deleted manual-send path ever used — **12 of 16 disagreed with what real sends actually emit**, so a naive wiring would have silently rewritten 12 customer-facing subject lines. Live literal wins, registry corrected to match; the re-verifier reconstructed every pre-fix literal from `git show` and confirmed all 16 zero-override subjects byte-identical (incl. `od`-level checks on the em dash and arrow), and traced every token against the var map its site passes, since an unresolved token renders literally. **The load-bearing render-parity gate held throughout, and `render-parity-baseline.json` has exactly one commit in its entire history — captured once from unmodified code, never regenerated.** Retired 5 components + `sendTemplateManually` (**2184 lines deleted**); all 3 C-15 FAKE markers gone. §3.3/§3.4 Owner-performed by necessity. **Bundle ceilings never verified by anyone** — the measure script's route list covers neither new route and no pre-C-15 snapshot exists; absolute figures captured instead (editor 341.23 kB, `/admin/emails` 346.84 kB gzip, both in the lightest band). Five deferred items in `redesign/per-page-progress/C-15-email-template-studio-progress.md` §7.6. — *Original planning note:* **NEW 2026-07-16 (plan-refinement phase)** from user direction — the Templates tab editing experience is inadequate: thin editable surface (1–3 fields; admin templates footer-only; subjects hardcoded), hand-typed `{variable}` codes, preview shows saved state only (FAKE-marked), reset-to-default undiscoverable. C-15 delivers: registry expansion (defaults + editable subjects + copy-lift + tokens + fixed-part legend), template gallery with Default/Customised badges, full-page editor at `/admin/emails/templates/[id]` with chip-token inserts, **live draft preview** through the real render functions, one-click per-template Reset-to-default + per-field Use-default, "Send me a test" to the actor's notification email (C-08 Phase D column). Retires ManualSendSheet + 4 old editor components; removes 3 non-filter FAKE markers (flag to C-09 inventory). **Zero migrations.** 6 phases / 7 commits. Load-bearing gate: render-parity spec (zero-override output byte-identical pre/post copy-lift). **Brief + plan written 2026-07-16** — `redesign/briefs/C-15-email-template-studio-brief.md` + `redesign/plans/C-phase/C-15-email-template-studio-plan.md`. **Sequencing:** after C-08; before C-13 + C-02 (their template work lands inside the studio). Implementation ⏳ pending C-08. |
| C-16 | Data growth: pagination standard + bounded lists | ✅ | 2026-08-03 | 2026-08-04 | `d22ab37` | C-B, C-09, C-05 | **SHIPPED 2026-08-04 — C-C plan #16 of 22.** All five phases. Commits: A `437c186` inventory · `6e07935` ⛔ checkpoint · B `080279b` helpers+bar · C `ca0cc21` predicates→SQL, `c54df61` chip counts+pager, `241d668`+`24889f4` parity hardening, `2f376f9` clients+enquiries · D `dc26dc0` emails+privacy, `66e9391` operations+password-requests, `6faf895`+`6fa19ce` fixes · E `e822e12` five folded-in surfaces, `f27a9da`+`ed9d31b` critical-note fixes, `4a9bef9` roles verified · closeout `d22ab37`. Gate: tsc 0 · lint 59E/7W same six files · build clean 53 routes · vitest **5 failed / 1812 passed (1817)**, the five inherited by identity · parity spec 35/35. **Bundle gate NOT RUN** — tenth plan hit by the same tooling gap. **⛔ One HARD-STOP (Phase A Step 2), answered: operations = PAGER at 100/page; `reporting.ts` = log-don't-touch; six extra unbounded surfaces folded in; Phase E Step 13 reduced to verify-and-polish.** Zero migrations, zero Zone-2 actions. **The inventory found 69 list regions across 32 pages and corrected eight audit/brief premises** — the audit log is NOT the working cursor reference the plan assumed (it is a forward-only append with no Prev), privacy's "25-cap" belonged to the notes rail while the request queue had never been bounded at all, and the clients list needed restructuring rather than a `.range()`. **Three of five verification rounds FAILED, and every failure was the same class: the interface asserting something untrue about the data beneath it** — a filter labelled "Today" returning 48 hours; a banner naming a hardcoded cap beside a "view all" link pointing at the current URL (**shipped twice independently on two surfaces**); and a clinical "Critical note" scan that could silently miss a flagged note. All fixed and re-verified. **A pre-existing patient-safety defect was found and fixed under Owner authorisation:** `CRITICAL_NOTE_PATTERN`'s trailing `\b` meant `anaphyla` and `contraindic` only matched as standalone words, so *"anaphylactic"* and *"contraindicated"* never raised the banner — dead since `d7c8d0f`, pre-programme. Widening it then introduced false positives ("do nothing" tripping the banner), caught by the closeout review and narrowed to `do not\b`. A mechanical guard now parses the live regex source and fails if any branch loses its SQL keyword cover. **Known residuals, all recorded:** `getClientCandidates` is capped (1000, 6000 via view-all) rather than paginated because PostgREST aggregates are **disabled** on this project (`PGRST123`, confirmed by four agents) — closing it properly needs a view/RPC/derived column, i.e. Zone-2; several internal id-lookups remain unbounded; §3.3 multi-page proof and §3.5 role sweep are Owner-performed by necessity. Detail in `redesign/per-page-progress/C-16-data-growth-pagination-progress.md`. — *Original planning note:* **NEW 2026-07-16 (plan-refinement phase)** from user direction — every admin list must survive 5 years of data. Audit findings: bookings list fetches EVERY row ever (no limit) then filters in memory; clients + enquiries unbounded; emails (100) / privacy (25) / operations (300) silently hard-capped with no pager; audit log is the one surface done right (cursor, 100/page — the reference pattern). 5 phases: A inventory (every surface classified: growth class + verdict, user checkpoint) → B shared `PaginationBar` + pagination helpers (URL-driven, 25/page lists, 100/page logs) → C heavy hitters (bookings view-predicates→SQL with a parity spec against `filterBookings`, clients, enquiries — fixes the unbounded-fetch performance cliff) → D cap→pager conversions (emails/privacy/operations/password-requests) → E structure for static-long lists (roles page — the user's named example — density/disclosure, never pagination). **Standing rule added to Part 0: no unbounded list queries, ever.** Verification proves multi-page behaviour via a temporary page-size-3 override — NO production seeding. **Zero migrations.** Ripples: C-09 helpers written pagination-ready (Step 5 note), C-02 series page caps its visit lists (Step 16 note). **Brief + plan written 2026-07-16** — `redesign/briefs/C-16-data-growth-pagination-brief.md` + `redesign/plans/C-phase/C-16-data-growth-pagination-plan.md`. **Sequencing:** after C-09/C-05/C-07; hard-before C-10 (pagination changes page heights; the overlap audit runs after). Implementation ⏳ pending. |
| C-17 | Google Analytics (GA4) on customer pages | ✅ | 2026-08-04 | 2026-08-04 | `d5425ec` | C-B | **SHIPPED 2026-08-04 — C-C plan #17 of 22. Co-ships with C-18, which follows immediately.** Two phases: A `05f251e` env-gated GA4 tag + public mount + `.env.example` doc · B `e545f38` `booking_request_submitted` fire-once conversion event · leak fix `d5425ec`. Gate: tsc 0 · lint 59E/7W same six files · build clean 52 routes · vitest **5 failed / 1820 passed (1825)**, the five inherited by identity. Bundle ceiling **NOT RUN** (no stored baseline; supplementary check showed gzipped First Load JS byte-identical with GA env on vs off). **⚠️ THE PLAN SPECIFIED A CREDENTIAL LEAK, and the FULL-tier verifier caught it.** Plan Step 2 mounts GA on `/booking/**` — but `/booking/manage` carries the customer's **booking-management bearer token in the query string** (`manage/page.tsx:18,46`), and GA4's `page_location` sends the full URL to Google. The codebase already redacts that value before Sentry (`sentry-scrubbing.ts:4`), so the mount would have handed Google a credential the project deliberately withholds from its own error tracker — and **C-18's consent gate would not have fixed it**, since a customer who accepts cookies leaks it anyway. **Owner decision 2026-08-04: drop the mount.** `src/app/booking/layout.tsx` deleted; **§2's NEW-file list and §3 gate item 2 are superseded — gtag must be ABSENT from `/booking/manage`, not present.** A recursive guard test now fails if anything under `src/app/booking/` references `GoogleAnalytics` (disclosed limit: string match, so an aliased re-export or raw inline `<Script>` would evade it). A sweep for a second instance found none — no `(public)` route reads `searchParams` at all. **Phase B's fire-once guarantee was chased into framer-motion's own source:** a `useRef` guard is per-instance, so an `AnimatePresence` remount would double-count the one metric this plan produces; `useReducedMotion` proved to be a `useState` lazy initialiser with no listener, the `"success"` key is stable, `currentStep` isn't persisted and the URL uses `replaceState` — no path reaches a new mount without a new submission. Coverage gap recorded: no test exercises a genuine remount, only StrictMode's double-invoke. **⛔ Two Zone-2 items outstanding by design, both riding the deferred deploy:** setting `NEXT_PUBLIC_GA_MEASUREMENT_ID` in the Cloudflare **build** environment (`NEXT_PUBLIC_*` is inlined at build time — a runtime-only dashboard setting is insufficient), and the production deploy + live GA Realtime session (needs Owner GA access; no offline substitute). ⏸ Also open: Owner confirmation that `G-WM8BCYG060` is the intended property. Detail in `redesign/per-page-progress/C-17-google-analytics-progress.md`. — *Original planning note:* **NEW 2026-07-16 (plan-refinement phase)** from user direction — GA4 tag `G-WM8BCYG060` on the customer-facing surfaces only. Smallest plan of the band: env-gated `GoogleAnalytics` component (`next/script`, `afterInteractive`, renders nothing outside production — dev/Playwright never pollute analytics), mounted in `(public)/layout.tsx` + a thin new `booking/layout.tsx` for `/booking/manage`; **admin never tracked** (gate asserts absence from admin HTML). Phase B: one `booking_request_submitted` conversion event on the booking success screen (`PreparedStep`), fire-once, zero PII. No new packages, zero migrations; one Cloudflare env var (build-time-inlined — pipeline injection verified). **Consent: C-18 written same day** — C-18 rewrites this component into the consent-gated loader (basic Consent Mode v2); **co-ship C-17 + C-18** (deploying C-17 alone = non-consented collection, a recorded user-accepted gap now superseded). **Brief + plan written 2026-07-16, amended same day** — `redesign/briefs/C-17-google-analytics-brief.md` + `redesign/plans/C-phase/C-17-google-analytics-plan.md`. **Sequencing:** the C-17+C-18 pair is fully independent (like C-14); ships anytime; branch confirmed with user at impl (public layouts diverge ~9 lines from the frontend line). Implementation ⏳ pending. |
| C-18 | Cookie consent & PECR compliance | ✅ | 2026-08-04 | 2026-08-04 | `f38e56a` | C-B, C-17 | **SHIPPED 2026-08-04 — C-C plan #18 of 22. Co-shipped with C-17.** 29 commits, `70e2103..f38e56a`, all seven phases plus a Phase 0 that was not in the plan and a final fix round. Gate: tsc 0 · build clean **54/54 static**, every public route `○`/`●` · vitest **5 failed / 1975 passed (1980)**, the five inherited by identity, zero new · eslint 59E/7W same six files. **⛔ ONE Zone-2 action, Owner-approved and applied:** migration `20260804182200_c18_consent_events` (`032bdca`). **⚠️ THE PRE-FLIGHT FOUND AN ACTIVE PRODUCTION CREDENTIAL LEAK.** `/booking/manage?token=<bearer>` carries a customer's booking-management credential in the query string, and Sentry Session Replay was capturing and transmitting it in **four** channels. Established from the installed package source, not inferred: `beforeSend` **never runs on Replay data** (`client._processEvent()` gates it behind `isErrorEvent()`; Replay's `sendReplayRequest()` bypasses it), so the project's own `scrubSentryEvent` — which *does* redact `manage.*token` — never saw replay payloads. Owner confirmed `NEXT_PUBLIC_SENTRY_DSN` **is** set in production, so this was live. Fixed at `09b2e26` before any C-18 feature work. A correction surfaced before implementing: consent-gating Replay would **not** have fixed it — a visitor who accepts analytics has not consented to their own booking credential going to a third party. **⚠️ PHASE A SHIPPED TWELVE FALSE PUBLIC STATEMENTS, all caught, across five fix rounds.** Every one described the finished plan rather than the shipped code — five consent promises with no consent system, three controls that did not exist, two facts contradicted by source, two self-contradictions (one *introduced by the fix for the previous one*). **The last three were all non-prose strings** — a heading, a button label, a `<meta>` description — that three prose-focused reviews walked past; the closing sweep read all 21 non-prose strings standing alone. A string-matching test was written and then **removed** for proving only that two sentences sat near each other while the thing they described did not exist; a PHASE D OBLIGATION comment requiring a real gate plus a test-that-the-gate-exists replaced it. **A third false claim was found at closeout and fixed:** `maintenance-modal-seen` was flagged `dormant` because every pass read the Owner's working copy (`MAINTENANCE_MODE = false`) rather than HEAD (`true`) — so a public legal page told visitors a storage item was switched off when any deploy would write it. **The consent-proof log was verified live end to end**, six real rows including two withdrawals each joining to their grant by `consent_id`. That check was load-bearing: Step 10 locks the route to **always 204**, so a failing insert is externally indistinguishable from a working one — and the brief's SQL had **no GRANT**, which would have left `service_role` without INSERT (tables created by `postgres` get only `Dxtm`), reproducing C-04a's silent failure exactly. `GRANT INSERT` was added to the migration after inspecting `pg_default_acl` live. **Owner decisions (9, all recorded):** Replay registered *and* consent-gated, then later switched **off on `/admin` entirely** (a consent gate there is unsatisfiable — staff never see a banner — and admin holds the clinical screens); consent read **client-side** not via `cookies()`, which had opted 15 prerendered public pages out of static generation *and* would have served one visitor's consent state to the next from an edge cache; `rahma-booking-contact-v1` classified `functional` with a **real** gate on read, write and withdrawal, because a toggle that did not gate would be a control that lies. **Bundle ceiling breached and ratified:** ≈5.19 kB against +5 kB, likely ≈6.7 kB in truth — Phase C's term was ratio-derived and that shortcut underestimates by ~38%; no pre-C-18 baseline exists anywhere in the programme (C-16 and C-17 both record the gate NOT RUN) and establishing one needs a prohibited package install. **Residuals recorded, not buried:** the `Meta.data.href` channel has no regression test and a Sentry upgrade could silently reopen it; session-mode Replay flushes on withdrawal either way; the replay chunk still downloads pre-consent; `/booking/manage`'s cookies link was source-verified, not click-tested, because reaching it needs a real customer's token. Detail in `redesign/per-page-progress/C-18-cookie-consent-progress.md`. — *Original planning note:* **NEW 2026-07-16 (plan-refinement phase)** from user direction (9 explicit requirements) + verified legal research: DUAA 2025's PECR statistics exception is IN FORCE (5 Feb 2026, SI 2026/82) but stock GA4 fails its conditions → opt-in consent still required; ICO final guidance (29 Apr 2026) mandates reject-parity/granularity/easy withdrawal, recommends 6-month consent lifetime; PECR penalties uplifted to £17.5m/4%. In-house consent layer (no third-party CMP): registry-driven single source (`cookie-registry.ts` → banner toggles + panel table + new `/cookies` notice page), **basic Consent Mode v2** (default-denied all four params before anything Google; gtag not injected until granted — regulator test: zero pre-consent Google requests), banner in the `--rahma-*` public design language (Accept/Reject parity by construction, no pre-ticks, no cookie wall, responsive 375→1440, reduced-motion, focus-trapped panel), **consent PROOF via new `consent_events` table** (Zone-2 migration — timestamped pseudonymous log of version/options/choice/withdrawals, no PII, RLS deny-all + service-role route), footer "Cookie settings" withdrawal (deletes `_ga*`, logs, reloads), 6-month re-prompt + version-bump re-prompt. **Standing cookie/tag-registry rule added to Part 0.** Rewrites C-17's component into the consent-gated loader — **co-ship with C-17.** Flagged (not scoped): the site has NO privacy policy page — recommended next compliance item. **Brief + plan written 2026-07-16** — `redesign/briefs/C-18-cookie-consent-brief.md` + `redesign/plans/C-phase/C-18-cookie-consent-plan.md`. Implementation ⏳ pending (pairs with C-17). |
| C-19 | Privacy policy page | ✅ | 2026-08-09 | 2026-08-09 | `ab80687` | C-B | **SHIPPED 2026-08-09.** One new file, two commits: `e70bef8` page + evidence · `ab80687` fix round. Gate: tsc 0 · lint 59E/7W same six files · vitest **5 failed / 2014 passed (2019)**, the five inherited by identity · contrast 7.09:1 body / 5.18:1 links · 65ch measure holds at 375 and 1280 · nine deep-link anchors verified live. **Bundle/build gate NOT RUN** (builds banned for agents this session). **All three ⏸ copy inputs were answered and verified before implementation** — controller `RAHMATHERAPY LIMITED` #16769945 cross-checked against Companies House, contact reused from `contact.ts` rather than retyped, no postal address. **⚠️ CLOSEOUT VERIFICATION FAILED FIRST TIME — two BLOCKING under-disclosures on a public legal page, both real, both confirmed twice.** The page omitted the required **`city`** field (the booking API collects four location fields, not three; the form renders a dedicated "City / Town" input distinct from "Area / County") and scoped gender collection to *"anyone else included"* when a self-booking also collects the **booker's own** gender as `clientGender`. Root cause named by the adversarial reviewer: plan Step 2's required booking-schema cross-check ran, but **not exhaustively** against the current `details` object. Fixed at `ab80687` and re-verified with a genuinely exhaustive field-by-field mapping across **both** `bookingRequestSchema` and `manualBookingSchema`, checked in **both directions** — zero MISSING, **zero OVER-DISCLOSED** (over-correction being the obvious hazard of a hurried fix round). **Three items deferred to `OWNER-ACTION-BACKLOG.md`, all outside the Owner-locked one-file scope:** Sentry's `SENSITIVE_KEY_PATTERN` not key-matching `notes`/`participantNotes` (latent — all 7 capture sites enumerated, none sends those fields, so the page's scrubbing claim is accurate today); no `<h1>` on legal/utility pages (sitewide, lives in `SectionHeading.tsx`, `/cookies` has the identical gap); and the `<title>` em-dash vs the sitewide pipe (the plan specified that string verbatim — Owner's call, not silently overridden). **Standing caveat now public: no code enforces the published retention periods** — no pruning process exists for bookings, clients or enquiries, so 7 years is a manually-kept commitment. The page states it as policy and claims no automatic deletion; verified in both passes. Detail in `redesign/per-page-progress/C-19-privacy-policy-page-progress.md`. — *Original planning note:* **NEW 2026-07-16 (plan-refinement phase)** from user direction — **deliberately minimal (user-locked scope): ONE new page, nothing else touched.** `src/app/(public)/privacy/page.tsx` in the public design language: who we are, what the booking form actually collects (incl. health notes customers choose to share), plain-words purposes + lawful bases, named processors (Supabase/Resend/Cloudflare/Sentry/Google), transfers one-liner (swap-ready wording), concrete retention (7-year insurance default, user confirms), rights + withdrawal, raise-a-concern + ICO contact, no automated decisions, last-updated stamp. No migrations, no packages, no edits to booking flow/footer/other pages (linking left for later). Backed by 2026-07-16 legal research (Art 13 checklist; explicit-consent route for health notes; DUAA complaints-right mention; ICO Tier-1 fee flagged as a business action). **Brief + plan written 2026-07-16** — `redesign/briefs/C-19-privacy-policy-page-brief.md` + `redesign/plans/C-phase/C-19-privacy-policy-page-plan.md`. Fully independent; ships anytime; 1 commit. Implementation ⏳ pending. |
| C-20 | Address autocomplete (Google Places) on both booking forms | ⏳ (brief + plan ✅) | — | — | — | C-B | **NEW 2026-07-16 (plan-refinement phase)** from user direction (Google Maps/Places key supplied) — location autocomplete on the **admin create-booking form** and the **customer booking form**. Audit: both collect the same 4 fields (`address`/`postcode`/`city`/`area`) but bind differently (customer react-hook-form, admin useState) — one shared form-library-agnostic `AddressAutocompleteField` emits parsed `AddressParts`; each host applies them its own way. **Critical detail:** customer-side `setValue(..., {shouldValidate:true})` so the covered-area notice re-evaluates on the filled city. Deviations from the user's reference snippet (documented, deliberate): `next/script` lazy-load on first focus instead of the CDN component library; env-var key; **UK component mapping** (`postal_town`→city, `administrative_area_level_2`→area — the snippet's US model gives wrong/empty UK values); no `window.alert`. Plus UK-restricted address-only suggestions, Places session tokens (cost), and a plain-input fallback when the key/script is unavailable (tested). **Blocking sign-off items:** Cloud Console key restriction (referrers + APIs) — **done 2026-07-16** (`rahmatherapy.uk` + wildcard + localhost, 3 APIs) — and a rotation decision (the key was shared in plaintext); C-18 registry entry + consent classification (functional-on-interaction recommended). **AMENDED 2026-07-16 (verified billing research):** the reference snippet's `fields: [...,'name']` would have put every lookup in the **Pro** tier (free allowance 10,000→5,000/month, price $5→$17 per 1,000) — plan now mandates **Essentials fields only (`address_components` + `geometry`), never `name`/`displayName`**, plus ~300 ms debounce (abandoned sessions bill per request: ~7,200/month debounced vs ~14,400 un-debounced against 10,000 free), and a post-deploy Metrics check that accounts for the 90-day trial credit masking real overage. Billing account with a card is mandatory even for free-tier use; budgets only email (no hard stop), Maps quotas are per-minute — so the referrer restriction is the load-bearing cost control. No migration, no new package. **Brief + plan written 2026-07-16** — `redesign/briefs/C-20-address-autocomplete-brief.md` + `redesign/plans/C-phase/C-20-address-autocomplete-plan.md`. Independent; 5 commits. Implementation ⏳ pending. |
| C-21 | Canonical domain fix (SEO) | ✅ | 2026-07-27 | 2026-07-27 | `21915d0` | C-B | **SHIPPED 2026-07-27 — C-C plan #1 of 22.** Commits `38ff24c` (plan Steps 1/2/2a/3a/3b/4) + `21915d0` (Owner-approved fix round: structured-data URLs match the served `trailingSlash: true` form; homepage entity + breadcrumb "Home" → `/home/`). Gate: lint 59E/7W identity-exact · tsc 0 · vitest 6 failed/488 passed/494 (guard adds 3 passing specs) · build clean · 12-page rendered output all `https://rahmatherapy.uk/…` with zero `.co.uk`/`.com` · census `.uk`=1 · no visual regression. ⛔ Step 1a closed with no action — Owner confirms prod `NEXT_PUBLIC_SITE_URL` already correct in Cloudflare. **Deferred:** 5 of 6 original public pages emit no `<link rel="canonical">` at all (the brief's `metadataBase` premise was factually wrong) — Owner deferred to a follow-up, pairs with the sitemap/robots gap. See `redesign/per-page-progress/C-21-canonical-domain-fix-progress.md`. — *Original planning note:* **NEW 2026-07-16 (plan-refinement phase)** — defect found while restricting the C-20 Maps key: the live domain is **`rahmatherapy.uk`**, which appears **nowhere in the codebase**; instead two wrong domains are hard-coded — `rahmatherapy.co.uk` (12 refs incl. `layout.tsx:9` **metadataBase**, which builds every canonical + OG image URL, and structured data on about/services/reviews/faqs) and `rahmatherapy.com` (8 refs incl. **`home/page.tsx:24` homepage JSON-LD** — the highest-value page; the other 7 are harmless admin placeholders). Root cause: no single source of truth for the site URL. **Second defect found in the same sweep (user-confirmed 2026-07-16): the published contact email `hello@rahmatherapy.co.uk` DOES NOT EXIST** — customers emailing the site's contact address reach nobody. Real address: **`rahmatherapy@outlook.com`**. Fix: introduce `SITE_URL` in `src/content/site/`, derive every absolute URL from it, correct the value, **replace the clinic contact address with the live one** (contact.ts + email-preview fixture) while keeping per-person staff-email placeholders as neutral examples (a "type your email" field must not suggest the owner's address), cosmetic sweep so no third domain survives, plus an **anti-drift test** asserting zero wrong-domain literals in `src/` and that the contact address stays the live one. Verification is rendered-output-based (curl the canonical/OG/JSON-LD on all 6 public pages), not just grep. No migration, no packages, no visual change. Flagged not scoped: **no `sitemap.ts`/`robots.ts` exists**. **Brief + plan written 2026-07-16** — `redesign/briefs/C-21-canonical-domain-fix-brief.md` + `redesign/plans/C-phase/C-21-canonical-domain-fix-plan.md`. Independent; 1 commit; ship early (wrong canonicals compound while indexed). Implementation ✅ complete (see shipped note above). |
| C-22 | Public booking form abuse protection | ✅ | 2026-07-27 | 2026-07-27 | `e4544bb` | C-B | **SHIPPED 2026-07-27 — C-C plan #2 of 22.** Commits `ceb028d` Phase A honeypot · `a63de0b` Phase B rate limiting · `ef1e201` test gaps · `e4544bb` Owner-approved limiter fixes. **Mechanism: SQLite-backed Durable Object** (`RateLimiter`) — Owner-decided on evidence after the plan's two preferred options were proven incapable of its windows (Workers binding caps `period` at 10/60s; Workers Free WAF gives 1 rule at 10s). Shipped limits: booking **5/10min** (plan said 3 — Owner-approved, counts attempts not successes) + 10/day; availability endpoints 120/10min + 1000/day. Gate: lint 59E/7W identity · tsc 0 · vitest 6 failed/519 passed/525 · build clean · **§3.3 honeypot silent drop PASSED against the live route with zero production footprint** (bookings/clients/email-events all unchanged) · honeypot a11y 6/6 incl. CDP autofill with positive control. **Gate items deliberately NOT RUN, not silently ticked:** §3.2 + §3.7 (need production writes + real emails — Owner approved §3.3 only); §3.5/§3.5a (structurally impossible pre-deploy — the limiter fails open twice over under `next dev`; Owner decision: post-deploy). **⚠️ The next `wrangler deploy` applies C-22's DO migration as a side effect — the C-01/C-04a cron-activation approval must say so.** See `redesign/per-page-progress/C-22-booking-form-abuse-protection-progress.md`. — *Original planning note:* **NEW 2026-07-16 (plan-refinement phase)** from a gap analysis the user actioned — verified: **zero** rate limiting / CAPTCHA / honeypot anywhere in `src/`, while `POST /api/bookings` is public and each call writes DB rows **and sends two emails** (spam → junk bookings + burnt Resend quota + sender-reputation damage that lands real confirmations in spam). **User-locked scope: public customer booking form ONLY; honeypot + per-IP rate limiting only** (no CAPTCHA/Turnstile — documented as the escalation step if needed; no admin/enquiries changes). Honeypot: off-screen decoy hidden from humans AND assistive tech (`aria-hidden` + `tabIndex={-1}` + `autoComplete="off"` — the classic honeypot a11y trap is an explicit gate item), server **silently drops with a success-shaped 200** so operators learn nothing. Rate limit: `CF-Connecting-IP` (never X-Forwarded-For), burst 3/10min + sustained 10/day (deliberately generous — shared/NAT IPs are normal), rejects **before** any DB or email work, 429 copy always offers the clinic phone, **fails open** when the header is absent (dev). Mechanism decision matrix (Cloudflare rate-limit binding preferred → dashboard WAF rule → Durable Object; Supabase-table counter rejected). No migration, no package. **Brief + plan written 2026-07-16** — `redesign/briefs/C-22-booking-form-abuse-protection-brief.md` + `redesign/plans/C-phase/C-22-booking-form-abuse-protection-plan.md`. Independent; 3 commits; small. Implementation ✅ complete (see shipped note above; shipped as 4 code commits — 2 were verifier/Owner-driven fix rounds). |
| C-23 | Availability-aware calendar on the admin create-booking page | ⏳ (brief + plan ✅) | — | — | — | C-B | **NEW 2026-07-16 (plan-refinement phase)** from user direction — bring the public flow's "no-guessing availability" to `/admin/bookings/new`: the calendar marks which days have matching therapist availability instead of staff discovering it one click at a time. **Reuses the shared engine** (user requirement — no duplicated logic). **Phase A ports `availability.ts` + `/api/availability/month` from `redesign/start-state` (0325838) to master byte-identically** — verified conflict-free (master untouched since the fork; start-state's only change is that commit), so the port *de-risks* the eventual start-state→master merge rather than adding to it. Refactor shape: load-context → batched load-day-records → pure compute-day, so a month costs one round-trip set, and `calculateAvailableSlots` keeps its exact contract. Phase B adds an **additive, defaulted options bag** (`ignoreBookingWindow`, `ignorePublicPause`) + a new authenticated admin month route, because the public route's customer-window and public-pause guards would blank the admin calendar. **Audit findings that reshaped the naive port:** the admin form has ONE shared `bookingDate`/`startTime` rendered by THREE mutually-exclusive branches (single/same-gender, mixed-gender, fallback) — so **one calendar with two marker sets** for mixed-gender cohorts, never two dates; and because `overrideAvailability` + per-cohort skips exist, the calendar **informs but never disables** (unlike public). Deliberately NOT ported: auto-select-first-available and auto-hop-month (they would fight an operator who has a requested date in hand). **Governing constraint: change nothing that already works** — brief §2 carries an explicit non-removal list, and three blocking gates (port fidelity / identical submitted payload / non-removal audit). No migration, no new package (`react-day-picker` already used in admin at `CalendarDatePopover.tsx`). **Brief + plan written 2026-07-16** — `redesign/briefs/C-23-admin-availability-calendar-brief.md` + `redesign/plans/C-phase/C-23-admin-availability-calendar-plan.md`. Independent; 6 commits. Coordination: C-14 (same engine file), C-20/C-06/C-02 (same admin form, different regions). Implementation ⏳ pending. |
| ~~C-PRIVACY~~ | ~~Privacy GDPR fulfilment sprint~~ | **NOT WRITTEN** | — | — | — | — | Deferred per Q2. Honesty fix folded into C-06. Full GDPR sprint (B-89 ICO breach + B-90 SLA) deferred to separate compliance band. **(2026-07-16 note: C-18 covers the cookie/PECR slice; C-19 covers the public privacy notice page; B-89/B-90 + operational complaint handling remain here.)** |
| C-12+ | TBD — audit-surfaced items | ⏳ | — | — | — | C-B | Inventory in C-B-DECISIONS §3: heterogeneous FAKE markers (non-filter), B-89 ICO breach, B-90 SLA, Owner-from-Admin dashboard split (if/when needed), dashboard data-layer mixed-group marker (per C-13 §5.11), per-participant audit log entries, group bulk operations. |

**State legend:** ⏳ pending · 🔨 in progress · ✅ complete · ⚠️ blocked · ⏸ paused

---

## Programme-level final gates (Band C completion)

To be ticked once C-C ships all plans:

- [ ] All 11 user-prioritised items addressed (C-01 → C-11).
- [ ] All C-A.1 audit-surfaced items either addressed in C-12+, deferred to a later band (with rationale), or dismissed (with rationale).
- [ ] Production-readiness table in Part 1 has every ⚠️ PARTIAL and ❌ UNKNOWN row resolved to ✅ READY or moved to a future band with a written reason.
- [ ] HANDOFF-2026-05-21.md §1.16+ entries per shipped plan.
- [ ] Per-item progress files complete.
- [ ] Bundle deltas all within budget per `SHARED-IMPLEMENTATION-NOTES.md` §5 (Band B budget envelope still applies; new deltas tracked here).
- [ ] No new persistent Sentry error classes vs the post-Band-B baseline.
- [ ] Vitest baseline (485 / 491 at start of Band C) preserved; new specs all pass.
- [ ] All Band C migrations applied + verified.
- [ ] Production DB cleanup completed (test rows removed; identified in Part 3).
- [ ] Programme-level Phase 8 hand-off written.

---

## Hand-off

**Next session opens here:** read this file end-to-end. Pick a starting point — recommended sequence is C-A.1 (per-page audit) starting with the high-priority surfaces. Each per-page audit file lands one at a time; cross-page workflow audits begin only after the per-page set is complete; role-day walks begin only after the workflow set is complete. Then C-B writes plans against the consolidated findings. Then C-C ships.

**If the user wants to skip the audit phase and go straight to one of the 11 user-prioritised items**, that's allowed — each item is independent enough — but the audit will surface adjacent issues that affect plan scope, so doing them out of order risks rework. Audit first is the recommended discipline.

---

*End of Band C master plan. Update this file as audit findings land, plans get written, items ship, and the checklist fills in. This file is the single source of truth for "where are we in Band C?".*

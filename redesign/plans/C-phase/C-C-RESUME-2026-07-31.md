# C-C Resume Brief — 2026-07-31 (fresh-context orchestrator takeover)

**Read AFTER `C-C-EXECUTION-PROTOCOL.md` (which is binding in full, §2.8 medium-throughput mode included). This file gives the verified position and your first actions. If git or the progress files disagree with anything here, THEY win — then STOP and tell the Owner.**

## Verified position (git-verified 2026-07-31)

- **SHIPPED 8/22** — C-21, C-22, C-06, C-04a, C-05, C-01, C-FIELDWORK, C-11. All closeouts complete.
- **Migrations applied:** c06_client_crud_hardening · c04a_scheduled_emails (+ the c04a grant fix) · c01_review_email_infrastructure · c11_theme_preference · c08_tighten_email_delivery_events_rls.
- **C-08 IN FLIGHT:** Phases A+B shipped (4 new templates; overrides threaded through the 8 legacy senders; RLS tightening). **INTERRUPT CHECKPOINT at Phase C step 8** (commit `2ffc3d9`).
- **Cloudflare deploy decision:** already made and recorded in the progress files — honour it as recorded; do NOT re-open it with the Owner.
- **Baselines:** inherited vitest/lint failure lists BY IDENTITY come from the LATEST progress file (C-08's, else C-11's) — never from any plan's hardcoded text (protocol §0 precedence rule).
- **Remaining after C-08, strictly in order:** C-15 → C-13 → C-02 → C-09 → C-03 → C-07 → C-16 → C-17+C-18 → C-19 → C-20 → C-23 → C-14 → C-10.
- **Drift checkpoint #1** PASS (`40eccc6`); next checkpoints per protocol §2.6/§4.

## First actions, in order

1. Protocol §0 bootstrap + programme pre-flight, reported in chat.
2. Read C-08's interrupt checkpoint + progress file AND `redesign/per-page-progress/OWNER-ACTION-BACKLOG.md` — live cautions, especially: **plan C-08 §3.3's RBAC expectation is WRONG** — production intentionally grants resend to Coordinator/Therapist with scoping already implemented (`emails/page.tsx:158-175`); it must NOT be "corrected" into the code to match stale plan text.
3. Verify the checkpoint's parent SHA per protocol §3; resolve any uncommitted plan-scope work per the ungraceful-loss rule.
4. Resume **C-08 at Phase C step 8**; complete its remaining phases + closeout before anything else. Then proceed strictly per §4.

## Standing facts

- `src/lib/maintenance.ts` is the OWNER's uncommitted change (`MAINTENANCE_MODE=false`) — never stage/commit/revert it; excluded from all isolation checks.
- Owner-performed Playwright role-sweeps/screenshots are a standing deferral policy — keep appending them to `OWNER-ACTION-BACKLOG.md` with checklists, exactly as prior plans did.
- `booking_restored_client` has no `templates-data.ts` entry — the registration fix belongs to **C-15** (already in the backlog).
- The single-agent addendum (`C-C-SINGLE-AGENT-ADDENDUM.md`, pointed at by `AGENTS.md`) applies ONLY to no-subagent harnesses — you have workflows; ignore its execution-loop overrides.
- §5 model routing, applied to what remains: implementers default `sonnet`; `opus` only for the genuinely hard phases of **C-02 / C-23 / C-14**, the drift checkpoints, and twice-failed-phase escalations; mechanical phases inside those plans drop to sonnet (logged). Every dispatch carries an explicit `model` parameter — you run on Opus 5, so inheritance means Opus.

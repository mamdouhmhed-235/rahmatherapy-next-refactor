# Harden recommendations — `/admin/calendar`

Generated by Phase 6 harden pass (2026-05-16).

## State coverage map (brief §6 + recipe Step 9 edge cases)

| State | Shipped | Where in code |
|---|---|---|
| Default — week view, today centred | YES | `page.tsx` — `view === "week"` branch, week strip auto-marks today via `isToday` |
| Day view, mid-week date | YES | `DayAgenda` — time-rail gutter (07:00–21:00 default, span-expanded) + offset cards |
| Empty day | YES | `CalendarEmptyState` with `scope="day"` — "All quiet" / "No bookings in this range. Quiet days are healthy days." / "Create a booking" Secondary (role-gated) |
| Empty week | YES | `CalendarEmptyState` with `scope="week"` |
| Empty sidebar (Unassigned) | YES | `UnassignedPanel` empty branch — "Every visit has a therapist." quiet line (no illustration per brief) |
| Empty sidebar (Claimable today, Therapist) | YES | `ClaimableTodayPanel` empty branch — "No claimable visits match your profile right now." |
| Concurrent bookings (day view) | YES | `detectConcurrentGroups()` + Attention-icon chip per card + `role="status" aria-live="polite"` banner above day panel |
| Concurrent bookings (week view) | YES | Same detection in `PerDatePanel` |
| Print | YES | `print:hidden` on nav/control rail/sidebar; `PrintHeader` (`print:block`); per-date `print:break-inside-avoid`; cards with outline pills + no shadow + Strong-Notch border |
| Malformed `?date=` URL | YES | `isValidISODate` + Pending-family banner "That date doesn't look right. Showing today instead." |
| Unknown `?staffId=` URL | YES | `staffIdValid` check + Pending-family banner "That therapist isn't in your team. Showing everyone." |
| Therapist scope chrome | YES | `therapistOnly` branch: staff combobox → "Your schedule" Soft Slate label; sidebar → `ClaimableTodayPanel`; payment chip suppressed via `canSeePayment && !therapistOnly` |
| Denied scope | YES | `CalendarAccessDenied` → `AdminAccessDenied` with brief copy; no raw `view_bookings_all or view_bookings_assigned` permission identifier (`permission` prop is not passed) |

## Deferred to Phase 7 (gauntlet) or future polish

| State | Reason for defer | Defer target |
|---|---|---|
| Loading (skeleton) | Requires `app/admin/calendar/loading.tsx` net-new file outside recipe scope; recipe Step 9 hardens existing route; Next.js server-rendered page completes before paint, so user-perceived loading is the layout segment loading.tsx. Adding skeleton requires shared scaffold. | Phase 7 |
| Load failure (Cancelled banner with "Try again") | Would need `error.tsx` route handler. Without one, Next.js surfaces a generic boundary; for Phase 7, an `app/admin/calendar/error.tsx` route file should render Cancelled banner per brief Copy. | Phase 7 |
| Date-picked toast ("Showing {weekday}, {day month}.") | Requires Sonner integration in `CalendarDatePopover.tsx` — additional client dep + side effect. Listed as informational in brief Toasts section, low impact. | Phase 7 |
| Therapist week-view empty-day grouping ("Mon, Wed, Fri — no visits") | Brief §10 Open Question #1, flagged for Phase 6 confirmation. Currently all roles render empty days as dashed-border row per consistency. | Phase 7 |
| Pixel-parity reuse of shared `BookingListCard` | Brief 01 `00-shared-components` session still incomplete; current calendar card is composition-mirror with own data shape. | Phase 7 / Brief 01 follow-up |

## Notes

- All concurrent-detection groups display as `role="status" aria-live="polite"`; the per-card icon-only Concurrent indicator has an sr-only label for screen readers.
- Print outline-pill treatment uses `print:border print:border-[oklch(42%_0.025_80)] print:bg-transparent` on the chip backgrounds.
- The denied component renders no raw permission identifier (carried fix from `00-shared-components`).

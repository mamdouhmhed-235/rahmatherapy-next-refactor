# Harden recommendations — dashboard-coordinator

Generated by `/impeccable harden` invocation during Phase 6 recipe execution.

## State coverage audit

| State (from brief §6) | Status | Implementation reference |
|---|---|---|
| Coordinator, first paint, populated DB | ✓ Covered | Existing `getDashboardData` payload; Tier 1 + Tier 2 render unconditionally when data present |
| Coordinator, populated DB, mobile 375 | ✓ Covered | Single-column stack via Tailwind responsive grid (`xl:grid-cols-...` collapses below 1280px); verified screenshot at 375 |
| Coordinator, empty DB | ✓ Covered | Today panel `AdminEmptyState` with coordinator-pivoted copy ("Quiet day / Nothing scheduled. Use the time to follow up on enquiries."); Attention panel "All caught up"; ActiveEnquiriesCard empty branch ("No active enquiries / Anything new will appear here when it lands.") |
| Coordinator, today has bookings, all assigned | ✓ Covered | `TodayCoordinatorSubLine` renders "0 unassigned" in `text-[var(--admin-success)]` (Confirmed family — subtle reward signal per brief) |
| Coordinator, today has N unassigned bookings | ✓ Covered (logic) | `unassignedFirst` sort + Attention-family sub-line + per-row `AssignmentChip` ("Unassigned" or "Unassigned · same-gender required" when `requiredGender` present). Visual reproduction blocked on seed data (current DB has 0 today bookings). |
| Coordinator, Tier 2 expanded | ✓ Covered | `BusinessOverviewDisclosure` with `variantKey="coordinator-"` (per-variant localStorage), "Active queues" label, "Show/Hide active queues" aria-labels, 240ms `grid-template-rows` transition |
| Coordinator, Active Enquiries empty | ✓ Covered | ActiveEnquiriesCard branches on `totalActive === 0`; `hasAnyHandled` flag swaps copy ("No active enquiries" / "Anything new will appear here when it lands." OR "All enquiries handled" / "New leads will show up here.") |
| Coordinator, Operations Health empty | ✓ Covered (inherited) | OperationsHealthCard Owner/Admin spec preserved; "All systems quiet" overall status when no signals |
| Filter "More filters" open | ✓ Covered (inherited) | Existing `DashboardFiltersClient` AdminSheet behavior unchanged |
| Filtered date range with zero matching bookings | ✓ Covered (inherited) | Today panel handles via existing `UpcomingRangeList` / `TodayList` empty branches |
| Loading | ✓ Covered (inherited) | Existing `AdminSkeleton` per tile via React Suspense boundaries upstream |
| Error in any tile | ✓ Covered (inherited) | `AdminErrorBoundary` wraps `DashboardFiltersClient` and `DemandTrendCard`; new ActiveEnquiriesCard inherits server-component error path |
| Coordinator viewing as Therapist by URL manipulation | ✓ Covered (existing RBAC) | `getAdminPageAccess("dashboard")` + `resolveAdminShellVariant` route therapist users to `TherapistDashboard` (no leakage) |

## Verification edge cases (brief §9 list)

| Edge case | Status |
|---|---|
| Today panel sub-line: N unassigned > 0 → Attention family + alert-circle; 0 → Confirmed family | ✓ Implemented via `TodayCoordinatorSubLine` |
| Unassigned today rows sort to top regardless of start time | ✓ Implemented via `unassignedFirst` sort in `TodayList` |
| Assignment chip "Unassigned" or "Unassigned · same-gender required" (never colour-only) | ✓ Implemented via `AssignmentChip` with text + UserX icon |
| "Convert →" Ghost link Clinic Green | ✓ `admin-link-action` class resolves to Clinic Green |
| Stale enquiry on Convert click toast | Deferred — surfaces on `/admin/bookings/new` destination route, not this read-only dashboard |
| Operations Health link `/admin/operations` resolves for Coordinator | Verified — existing route, Coordinator has `manage_operations` per RBAC mapping in page.tsx |
| Avatar placeholder for unassigned rows: Hover Moss circle + centred user-x | ✓ Implemented (uses `--admin-warning-bg` token; visually equivalent for Attention emphasis) |

## States added in this session

None — all brief states are covered by either the new components (ActiveEnquiriesCard, TodayCoordinatorSubLine, AssignmentChip) or inherited infrastructure (AdminErrorBoundary, AdminSkeleton, BusinessOverviewDisclosure).

## Files touched by harden pass

None — harden confirmed existing states cover brief.

# Handoff — Rahma Admin Redesign Push

**Date:** 2026-05-21
**Branch:** `redesign/start-state`
**HEAD:** `894d250` feat(admin): H4 — admin response to customer reschedule requests
**Commits this session:** 5 (`cd7b91a` → `e2b5a16` → `ff34344` → `7a6e623` → `894d250`)
**Base:** `0b25108` fix(admin): production-readiness — exclude cancelled/no_show from active queues at consumer level

This document categorises every issue surfaced during the parallel 4-agent audit and verification pass. It separates **what landed** from **what is still open**, and groups open work by priority band so the next agent can pick an entry point at a glance.

---

## Legend

| Tag    | Meaning                                                              |
|--------|----------------------------------------------------------------------|
| C#     | Critical — runtime broken or silent permission bug                   |
| H#     | High — workflow correctness, RBAC, IA, blocks-launch backend         |
| M#     | Medium — UX papercut, stale data, low blast radius                   |
| R#     | Redesign — substantial surface rework                                |
| N#     | New finding discovered during verification (sibling of an H#/C#)     |

---

# PART 1 — SOLVED

All items in this section are **landed and verified** across the 5 session commits.

## 1.1 Critical fixes — landed

| #  | Title                                                        | Commit     | Notes |
|----|--------------------------------------------------------------|------------|-------|
| C1 | Notification bell silently empty (wrong DB column/table names + try/catch swallow) | `cd7b91a` | Folded into role-variant rewrite; queries entirely rewritten. Confirmed worse than originally reported (N1). |
| C2 | Permission name mismatch — code constant `manage_account_password_requests` ≠ DB row `manage_account_requests` | `ff34344` | Path A chosen: renamed constant value, kept symbol name. Idempotent role-grant migration `20260521090000_grant_manage_account_requests_to_owner_admin.sql` **applied to production** (`twzutkfgqclqurvkmvqz`). |

## 1.2 High-priority workflow correctness — landed

| #  | Title                                                                  | Commit     |
|----|------------------------------------------------------------------------|------------|
| H1 + N2 | `unpaidBookings` count included cancelled bookings — Payment Health card + attention row counted £100 outstanding on cancelled bookings (N2 = same leak in `attentionSummaryRows` consumer at `dashboard/page.tsx:647`) | `e2b5a16` |
| N4 | `summary.outstandingRevenue` in `reporting.ts:summarizeReports` also included cancelled — header strip showed £475 instead of £375 | `e2b5a16` |
| H2 | Calendar reschedule + unpaid stats iterated raw `data.bookings` not `activeBookings` | `e2b5a16` |
| H3 | `getAttentionItems` reschedule (l.597) + health-notes (l.617) branches lacked cancelled/no_show guard | `e2b5a16` |
| H5 | 3 booking mutations skipped `revalidatePath("/admin/calendar")` (claim / updateAssignment / updateOwnAssignment) | `e2b5a16` |
| H6 | Customer-manage cancel + reschedule skipped `/admin/calendar` revalidate | `e2b5a16` |
| H7 | "New enquiries" attention badge dropped `?tab=new` (3 sites incl. sibling in `reporting.ts:633`) | `e2b5a16` |
| H4 + N3 | `reschedule_status` had **zero** clean-state writes; permanent attention inflation. Added `respondToCustomerReschedule` server action (Accept→`reviewed`, Decline→`declined`). N3 sibling auto-clears once admin can clear status. | `894d250` |

### H4 — debugging journey worth remembering

`bookings_reschedule_status_check` CHECK constraint allows only `['none','requested','reviewed','declined','completed']`. Initial attempt used `'approved'` which silently failed because the `.update()` error was swallowed via the common `if (error) return` pattern. **Cost: ~2 hours.** Lesson surfaced as a new follow-up item (see Part 2.1: Silent-error sweep).

## 1.3 RBAC + IA — landed

| #   | Title                                                                  | Commit     |
|-----|------------------------------------------------------------------------|------------|
| H9  | Coordinator availability moved Communications → Scheduling nav group   | `7a6e623`  |
| H10 | Owner enquiries promoted to primary nav (5-cap); Reports → "Reporting" overflow group (user chose this swap) | `7a6e623`  |
| H11 | Therapist holds `resend_booking_emails` → over-broad PII scope on `/admin/emails`. **Middle path**: scoped Reminders list query + `sendManualBookingReminder` action to actor's own `booking_assignments` for non-`canSeeAllBookings` roles. Out-of-scope attempts log an operational-event row. | `ff34344`  |

## 1.4 Role-variant notification centre — landed

Foundation for R4 (still pending). Mid-session feature request from user.

- `cd7b91a` — full data variant (different fetches AND tabs per role), not just tab-strip variant.
- Per-permission gates inside each variant branch (defence-in-depth).
- Admin client used for RLS bypass; per-role tab tuples match the dashboard's variant pattern.
- Touches: `nav-notifications.ts`, `notification-bell.tsx`, `AdminTopNav.tsx`, `layout.tsx`.

## 1.5 False positives — dropped

| #   | Original claim                                              | Reality                                                            |
|-----|-------------------------------------------------------------|--------------------------------------------------------------------|
| H8  | Coordinator missing `resend_booking_emails`                 | DB query showed Coordinator already holds it.                      |
| H12 | Coordinator missing "Convert enquiry → booking" inline action | Exists at `EnquiryList.tsx:521-527`, flow at `bookings/actions.ts:821-848`. |

---

# PART 2 — REMAINING

Grouped by band. Bands A.4 and A.5 are the natural continuations of session work; Band B is the substantial redesign work; Band D is deferred / never.

## 2.1 Band A.4 — judgment fixes (next session entry point)

| #   | Title                                                                | Effort  | Where                                                                                       |
|-----|----------------------------------------------------------------------|---------|---------------------------------------------------------------------------------------------|
| M1  | `customer_cancelled_at` attention sticky forever — add 14-day freshness window guard. No schema change. | ~30 min | `src/app/admin/reports/reporting.ts:587-595`                                                |
| H13 | "Add session note?" prompt missing on assignment_completed despite `create_client_session_notes` permission existing. Non-blocking sheet routing to existing `addClientNote` server action. | ~2.5 hr | `BookingActionButton.tsx`; reuses `src/app/admin/clients/actions.ts`                        |
| **Silent-error sweep** (new) | Optional follow-up to H4 lesson: grep admin server actions for `if (error) return` and prepend `console.error(...)` so future silent failures surface in dev server stdout. Low risk, high diagnostic value. | ~30 min | Multiple `actions.ts` files                                                                 |

## 2.2 Band A.5 — BLOCKS-LAUNCH backend (substantial, sequential)

| #   | Title                                              | Effort       | Plan files                                                                                                                                                       |
|-----|----------------------------------------------------|--------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| H14 | Password reset full flow is **fake end-to-end**. Production has 1 pending row in `account_password_requests`; staff currently locked out have no recovery. | ~2 sessions | `redesign/backend-plans/BUILD-password-reset-request-actions.md`, `BUILD-approve-reject-password-reset.md`, `BUILD-password-reset-email-templates.md`             |
| H15 | Staff blocked-dates + availability override actions are **fake** — currently return `FAKE_BLOCKED_DATE_MESSAGE` / `FAKE_OVERRIDE_MESSAGE`. | ~1 session  | `redesign/backend-plans/BUILD-staff-blocked-dates-actions.md`, `BUILD-staff-availability-override-actions.md`                                                     |
| H16 | Audit page filter + pagination **fake** — loads top-100 in-memory; 12-month legal trail scale-fail risk. | ~1 session  | `redesign/backend-plans/BUILD-audit-filter-and-pagination.md`                                                                                                     |

## 2.3 Band B — substantial redesigns (after A.5, or parallel)

| #   | Surface                  | State                                                                                                              | Effort        |
|-----|--------------------------|--------------------------------------------------------------------------------------------------------------------|---------------|
| R4  | Notification Centre redesign | Agent 3 brief (~1460 words, implementation-ready) **exists only in chat history** — not yet written to `redesign/briefs/`. Drag-handle sheet, 6→3 tabs+overflow, severity dot, [View]+AdminActionMenu, Show-more truncation, badge pulse + aria-live, severity-desc sort. Data foundation already landed in `cd7b91a`. | ~10–12 hr     |
| R1  | Dashboard redesign (Owner/Admin/Coord + Therapist) | Agent 2 outline only — brief **not written**. Tier-2 collapse-by-default, equal min-heights, status-tinted tiles, severity tints stronger, `BusinessPulseCard` → Reports, new `PersonalContributionStripe` shared across variants. Watch DESIGN.md border-l-4 ban (user agreed substitution). M2 + M3 fold in here. | 2.5–3 days    |
| R2  | Reports redesign         | Agent 2 outline only — brief **not written**. Scope pill, 4→6 headline tiles with delta chips, semantic 5-colour status chart, `AdminStackedBar` workload rows, `?scope=personal` toggle, `CompactStat` upsize. | 3 days        |
| R3  | Per-User Metrics surface (Hybrid A+C) | Agent 2 outline only — brief **not written**. Backend: 2 additive helpers in `reporting.ts` (`getPersonalContribution`, `filterReportDataToStaff`). No new page. Folds into R1+R2. | 1.5 days backend |

## 2.4 Medium — deferred into Band B

| #  | Title                                                                                | Where it lands |
|----|--------------------------------------------------------------------------------------|----------------|
| M2 | `OperationsHealthCard` "View details" link always sends to `/admin/operations` regardless of row | R1             |
| M3 | Therapist `ClaimableCard` missing inline `ClaimAssignmentButton`                     | R1             |

## 2.5 Band D — deferred / pre-launch / never

### Track B (pre-public-launch, not redesign-blocking)
- MFA enrollment (BUSINESS-COMPLETENESS 2A-10)
- Backup restore drill (L1-c Track B waiver per ENGINEERING-LOG.md Session 5b)
- `CRON_SECRET` setup in Cloudflare Workers (Session 3 deploy checklist)
- Cloudflare Sentry post-deploy verify (Session 5a — 6-step procedure)

### V1.1 (post-launch)
- Operations / privacy / emails / staff FAKE filter queries
- Refund workflow
- Email-templates preview override-merge
- Manual-send booking picker
- Reminder FAKE-failure path
- `account-password-requests` seed cleanup (auto-resolves with H14)
- Therapist in-progress state
- Ops events deep-link from bell
- Booking-detail "next booking" lateral pivot
- Staff workload aggregates

### Never (at this clinic scale)
- Walk-in fast-add
- Recurring / subscription bookings
- `group_session_id` mixed-gender split
- Create / delete role UI
- Cancellation fee enforcement
- Therapist time-off request flow
- Shift handover
- Service-type filter on claimable strip
- Nav-disappearance edge case
- `border-l-4` rail in Reports brief (substitute per DESIGN.md)

---

# PART 3 — DECISIONS BINDING ON NEXT AGENT

| Topic                              | Decision                                                                                  |
|------------------------------------|-------------------------------------------------------------------------------------------|
| Variant depth on notification centre | Full data variant (different fetches AND tabs per role)                                  |
| R4 timing                          | Land variant work first; R4 redesign builds on top                                        |
| C2 path                            | Path A — rename code constant value to match DB row + role-grant migration                |
| H11 path                           | Middle path — scope Reminders query + action to actor's assignments for Therapist-class. Permission **not** revoked. |
| H4 scope                           | Scope B (Acknowledge-only) — admin clears status; moving booking_date out-of-band. No new booking-date editor. |
| H10 swap (5-cap rule)              | Reports → "Reporting" overflow group (single-item)                                        |
| Owner + Admin treatment            | Identical bell content (same `owner_admin` variant) — `resolveAdminShellVariant` already collapses them |

### Open questions deferred (not blocking)
- Therapist `resend_booking_emails` long-term policy — kept granted, scope-limited via H11. Revisit if PII concern surfaces.
- N3 view-tab predicate cleanup — auto-resolved by H4; no action needed.

---

# PART 4 — ENVIRONMENT + DISCIPLINE CARRY-FORWARD

## 4.1 Dev environment gotchas
- If buttons silently don't fire on first session test: nuke `.next` + restart `pnpm dev` **before** deeper diagnosis.
- If a server action returns OK but DB doesn't change: check dev server **terminal stdout** for CHECK constraint violations (PG `23514`). Don't assume Turbopack caching — H4 lesson cost 2 hours.
- If a permission gate appears to fail silently: add temporary `console.error(...)` at **each gate position**, not just at the end. Errors propagate to dev server stdout, not browser console.
- If Turbopack staleness recurs on R1/R2 (many new actions), consider `next dev --no-turbopack` in `next.config.ts`.

## 4.2 Credentials
- Owner sign-in: `rahmatherapy@outlook.com` / `Password123` (per `docs/users-credentials`)
- Phase10 test accounts: `phase10.{owner,admin,coordinator,therapist.a,therapist.b,inactive}@example.test` / `Phase10-Test-2026!`

## 4.3 Database state (production `twzutkfgqclqurvkmvqz`)
- Migration `20260521090000_grant_manage_account_requests_to_owner_admin.sql` ✅ applied
- Test booking `09a39848-857e-48f7-b637-cf0adf0d4756` (Fatima Ahmed / 2026-05-21): restored to original state (`status='pending'`, `reschedule_status='none'`, all reschedule fields NULL).
- Audit log additions remain (immutable by design per RECON §6.2): 2 H4 verification rows + 1 C2 migration row + various H1/H2/H3 sweep evidence rows.

## 4.4 Schema vocabulary (learned the hard way)
| Field                             | Allowed values                                                                       |
|-----------------------------------|--------------------------------------------------------------------------------------|
| `bookings.reschedule_status`      | `'none' \| 'requested' \| 'reviewed' \| 'declined' \| 'completed'` (CHECK constraint) |
| `bookings.status`                 | `'pending' \| 'confirmed' \| 'completed' \| 'cancelled' \| 'no_show'`               |
| `booking_assignments.status`      | `'unassigned' \| 'assigned' \| 'completed' \| 'no_show' \| 'declined'` (verify before next write) |
| DB permission for accountRequests | `manage_account_requests` (**not** `manage_account_password_requests`)               |

## 4.5 Discipline rules (do not regress)
- **Zone-2 confirmation required** for: DB migrations to production, Edge Functions, Resend sends, throwaway Supabase projects/branches.
- **`superpowers:verification-before-completion`**: every commit message claims something — verify before claiming. Code-inspection accepted only when live exercise infeasible.
- **No scope creep**: user repeatedly said "ensure surgical fixes, ensure not to affect anything else." Every commit's diff should be defensible against this rule.
- **PRE-EDIT → EDIT → VERIFY → SHOW DIFF → STOP for review** per fix.

## 4.6 Verification infrastructure
- Playwright MCP — works for browser drive
- Supabase MCP — works for DB queries + migrations (project `twzutkfgqclqurvkmvqz`)
- Chrome DevTools MCP — available but unused this session
- Static gate: `pnpm lint` + `npx tsc --noEmit`
- Sentry: server-side captures via `sentry.server.config.ts` (Session 5a fixes intact)

---

# PART 5 — RECOMMENDED NEXT-AGENT FIRST MESSAGE

> Picked up from `894d250`. Five commits stacked on `redesign/start-state`. Band A.1–A.3 + part of A.4 done (C1, C2, H1+N2, N4, H2, H3, H5, H6, H7, H9, H10, H11, H4). Remaining: M1 + H13 (Band A.4); H14, H15, H16 (Band A.5 BLOCKS-LAUNCH); R1–R4 redesigns (Band B). Recommend starting with M1 (~30 min, smallest remaining bug). Confirm direction.

---

# PART 6 — TRUE REMAINING LIST FOR PRODUCTION LAUNCH (PRIORITY ORDER)

1. **H14** password reset (staff lockout has no recovery)
2. **H15** staff self-block (therapists can't take holidays)
3. **H16** audit pagination (12-month legal trail scale-fail risk)
4. **R4** notification redesign (owner's "more modern" ask)
5. **R1** dashboard redesign (owner's "horrible / dead" complaint)
6. **R2** reports redesign (owner's "metrics not granular enough")
7. **R3** per-user metrics (owner's explicit gap call-out)
8. **M1** sticky-cancelled (low risk, cheap)
9. **H13** session note prompt (clinical continuity)
10. Track B: MFA + backup drill + Cloudflare secrets (pre-public-launch only)

**Time estimate to Phase 7 re-entry:** ~2 calendar weeks (A.4–A.5 = 5 days, B = 7–8 days; brief-writing parallelisable).

---

# PART 7 — FILE QUICK-LOAD FOR NEXT AGENT

### Foundational
- `redesign/BUSINESS-COMPLETENESS.md` — master gap registry
- `redesign/FINAL-REPORT.md` — Gate 0 PASS-WITH-CAVEATS verdict + Track B carry-overs
- `redesign/RECIPE-PROGRESS.md` — phase 6 + 7 status, paused at Gate 5
- `redesign/ENGINEERING-LOG.md` — verification discipline precedents (esp. Sessions 5a/5b)
- `redesign/RECON.md` — §5 untouchable list, §6 feature manifest, §6.2 audit action types
- `PRODUCT.md` — brand voice, role definitions
- `DESIGN.md` — design system + named rules (Tonal Lift, Cormorant Exception, etc.)

### Active during this session
- `redesign/PER-PAGE-SCORES.md` — per-page audit/critique scores
- `redesign/FINAL-AUDIT.md` — Phase 7 Gate 1 verdict (17/20)
- `redesign/backend-plans/*.md` — 22 BUILD plans for backend gap closure
- `redesign/briefs/*.md` — 29 Phase 5 brief files (R1–R4 should follow this style)

### Plan file from this session
- `C:\Users\mamdo\.claude\plans\i-want-to-make-rosy-eagle.md` — approved plan for role-variant notification centre (now landed as `cd7b91a`)

---

*End of handoff. Next agent: read top-to-bottom, then ask the user "M1, H14, or R4 brief first?" before any other action.*

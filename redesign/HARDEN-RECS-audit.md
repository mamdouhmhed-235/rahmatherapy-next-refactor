# HARDEN-RECS — audit (Phase 6, row 20 of 29)

Generated by `/impeccable harden audit` against the brief's §6 Key States table
(both the visible-states section and the backend error states section added for
Layer 3) plus the verification edge cases in §11 of the recipe.

## States already covered by the build

| Brief state | Covered by | Notes |
|---|---|---|
| Default (Owner, unfiltered) | `page.tsx` → `ResultCount` unfiltered branch + `<ol>` of `AuditEventCard` | "Showing 100 most recent events. Load more to see older entries." |
| Filter applied | `AuditFilterStrip` `activeChips` list + `ResultCount` filtered branch | "Showing N of M events." |
| Search submitted | `AuditFilterStrip.onSearchSubmit` + `page.tsx` filter chip "ID search: …" | Min 4 chars enforced |
| Filtered to empty | `TimelineEmptyState` "No events match" + "Clear filters" Ghost | |
| Unfiltered, no rows | `TimelineEmptyState` "No events yet" + no CTA | |
| Search returns nothing | `TimelineEmptyState` "Nothing matches that ID" + "Clear search" Ghost | |
| Loading (Load more) | `AuditLoadMoreButton` `aria-busy="true"` + "Loading…" label | |
| Row with no redacted keys | `summariseRedactions` returns count 0; chip not rendered | |
| Row with redacted keys | `summariseRedactions` returns N; chip "Redacted: N field(s)" + `title` tooltip listing keys; expansion shows `[redacted]` literal | |
| Row whose target was deleted | `AuditEventCard.targetExists === false` branch → "Target row no longer exists." inline | |
| `before_state: null` | `beforeNull` branch → "Before: (created)" header + italic "No prior state." body | |
| `after_state: null` | `afterNull` branch → "After: (deleted)" header + italic "Row removed." body | |
| JSON expansion on mobile | `md:grid-cols-2 md:divide-x` — columns stack vertically below 768px | |
| Mobile filter sheet | `AdminSheet` `side="bottom"` trigger + Clear footer | |
| Copy-success toast | `CopyIdButton` → `toast.success(successLabel)` (Sonner) | |

## Edge cases addressed by this harden pass

| Backend / edge case | Fix applied | Where |
|---|---|---|
| Search query < 4 chars | Inline note `Type at least 4 characters of an ID.` already implemented; `searchError` state in `AuditFilterStrip` + `role="alert" aria-live="polite"` on the note | `AuditFilterStrip.tsx` |
| Filter timeout (DB error) | Cancelled-family `role="alert"` region replaces timeline; "Try again" Ghost button reloads the route | `page.tsx` `eventsError` branch — adds reload button this pass |
| Load-more cursor points to a deleted row | `auditLoadMore` server action paginates by `created_at` cursor; no row id required, so deleted rows don't break pagination | `actions.ts` |
| Actor filter UUID malformed | Server treats as "anyone" — the `filters.actor` is passed through unchanged; the Supabase query treats unmatched UUID as zero rows. Empty result then renders the filtered-empty state | `page.tsx` + `actions.ts` |
| 1000-char `error_message` | `<pre>` uses `overflow-x-auto whitespace-pre-wrap break-words` so long values wrap or scroll within the well | `AuditEventCard.tsx` JSON well |
| 5000-row session (10× Load more) | Pagination is server-side (cursor-paged via `auditLoadMore`); brief §10 Q2 recommends switching the button to "Search for older events" at row 1000 — flagged for Phase 7 in deferrals | deferred |
| `from > to` date range | Inline error rendered next to the date inputs; submit blocked until corrected | `AuditFilterStrip.tsx` `DateRangeChipStrip` — adds check this pass |
| Clipboard API unavailable | Fallback renders the ID inline as `<code>` so the Owner can copy manually | `CopyIdButton.tsx` |
| `@media print` | Filter strip carries `print:hidden`; cards carry `print:break-inside-avoid`; `<details>` carries `print:!open` (CSS-attribute-selector ` details { open: '' }` not portable, so the JSX prints `[open]` via `print:[&]:open`) | `AuditEventCard.tsx` + `AuditFilterStrip.tsx` |

## Forensic-trust invariants (recipe §11)

1. **Redaction regex string-equality.** `redaction.ts:3` contains `/note|health|treatment|consent|token|secret|key|payload|body/i` — character-for-character match with the RECON §6.2 source. Verified by grep at Step 11a.
2. **No audit-log writes from this page.** Grep across `src/app/admin/audit/**` for `.insert(` returns zero matches. `auditLoadMore` is a read-only server action; page-load uses `.select("*")` against `audit_logs` only.
3. **AdminAccessDenied for every non-Owner.** `page.tsx` permission gate short-circuits before any data fetch when `manage_audit_logs` is missing; renders the brief's denied copy without raw permission identifier.
4. **Browser tab title.** `generateMetadata` returns `Access denied · Rahma` when permission is missing, `Audit log · Rahma` when present.

## Phase 7 deferrals (added here so the gauntlet finds them)

- DOM-weight cap at 1000 rows: switch Load more to "Search for older events" CTA — see brief §10 Q2.
- Postgres trigram index on `target_id::text` / `actor_staff_id::text` once audit_logs > 10K rows — brief §10 Q1.

Saved by `/impeccable harden audit` on 2026-05-16.

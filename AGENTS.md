# Agent instructions — Rahma Therapy repo

The **Band C implementation programme closed on 2026-08-09**. The live document is `redesign/HANDOFF-2026-08-13-IMPLEMENTATION-9.md` — read it first, end to end. If you are working on ANY implementation task here, the following are BINDING before any action:

1. Read `redesign/plans/C-phase/C-C-EXECUTION-PROTOCOL.md` end to end (hard rules, plan order, baselines, STOP semantics).
2. If your harness has no subagents/workflows: also read `redesign/plans/C-phase/C-C-SINGLE-AGENT-ADDENDUM.md` — it adapts the execution loop and tooling and records the current programme position.
3. Position of record is git (`git log --oneline --grep="shipped"`, `--grep="feat(redesign):"`) + `redesign/HANDOFF-2026-08-13-IMPLEMENTATION-9.md` — never memory or assumptions. ⚠️ `redesign/per-page-progress/` records the **Band C** programme and has been frozen since 2026-08-09; `OWNER-ACTION-BACKLOG.md` inside it is still live. ⛔ **This line says where truth lives. It is NOT a never-touch rule** — an audit on 2026-08-17 misread it as one and nearly preserved 57 files on that basis. The never-touch list is the paragraph below, and nothing else.

Non-negotiables even for one-off tasks: never `git push`; never `git add .`/`-A`; never stash/restore/checkout to "clean" the intentionally-dirty working tree; never touch `src/lib/maintenance.ts` (removed by `3eb2939`, which is **unpushed** — the file is still present on `origin/master`, so this clause still binds until Phase 12 ships), `redesign/audits/**`, `C-B-DECISIONS.md`, or live customer data (booking `9d55ce2a`, any client not matching `*.example.test` / `Phase10*` / `Audit Test*`); the production Supabase DB and the public availability endpoints are LIVE customer surfaces — all DB writes require the Owner's explicit per-action approval in chat (⛔ HARD-STOP flow in the protocol).
